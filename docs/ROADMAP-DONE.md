# DDtoolkit 路线图 · 已完成条目详录

> **这份文件是 `docs/TODO.md` 拆出来的历史部分**（2026-09-13 整理，无内容删减）。
>
> 分工：
> - `docs/TODO.md` —— **要干什么**（待提需求收集区 / 未完成项 / backlog / 远期构想 / 明确不做 / 门禁基线）；
> - 本文件 —— **干过什么、当初为什么这么定**：已落地条目的原始需求、验收标准、实现注意，
>   以及**需求清单的落地部分**与**批次 → devlog 索引**。追溯设计决策的由来时看这里。
>
> 维护约定（2026-09-13 更新）：**已落地的需求条目从 `TODO.md` §0 移进本文件**（原文照抄 +
> 落地结论 + devlog 指向），TODO 那边只留未完成/待展示的；新条目仍写在 TODO。
>
> 行数口径：本文件的行数按当时记录原样保留（历史上混用过总行数与非空行数，差约 5%）；
> 2026-09-13 起新记录统一用**总行数**（等价 `wc -l`）。

---

## R13：直播预约记录到直播日历 ✅ 2026-09-15（devlog/088）

> 用户原文（编号按约定重排）：**R13**「动态中包括的直播预约记录到直播日历中」，优先级 低。

**落地结论**：数据链路**早就存在**（动态抓取时 `fetcher._extract_reservation` 落
`posts.body_json.reservation` → `VtuberEventRepo.future_reservations()` 解析时刻/标题 →
`GET /vtuber/{id}/future-reservations`），缺的只是前端没接 —— 所以本批**后端零改动**
（方案里原打算加"全部 V 聚合端点"，核实后确认多余：日历本就是单 V 的）。

**前端四块**：

1. `api.futureReservations(vtuberId, days)` + `types.UpcomingReservation`（与
   `ReservationInfo` 分清楚：后者是动态原始形状，前者是服务端解析结果）；
2. `components/live/useReservations.ts`：**V 级**取数（预约跨账号共用），刻意**不塞进
   `useLiveSessions`**（那份 effect 顺序是契约，devlog/057）；失败**不清空**已有数据；
3. `utils/reservationDays.ts`（纯逻辑，9 条单测）：按天分组（与场次同一 `dayKeyIso` 口径）、
   **徽章优先级 场次类型 > 预约 > 休息/待定**、展示行（最早一条 + 人数 + 多条提示）；
4. `LiveCalendar`：格子加 `.lc-resv`（**不动九类色系**，只在左缘加一道强调粉竖条）、
   计数槽放**预约人数**、有场次的日子预约压成一行小字、hover 浮层顶部单列预约块
   （**只有预约没有场次的日子也能 hover 查看** —— 否则"预约"徽章点不出任何东西）。

**验收**：pytest **401 passed**（+1 路由契约用例）· vitest **153 passed**（+9）·
`ui_probe --reservations` **反向验证过**（把徽章优先级去掉 → 立刻红："预约格徽章是 '待定'"）。

⚠️ **日历签名基线已重取**：`48519bae…` → **`50b78ec0…`**。差异**不是本批代码**——
先问了仓库层"V15 有没有未来预约"（**0 条**，所以我的渲染分支根本没触发），
再对日历格才发现当晚 V15 新落库两场直播（14 日从「休息」变成一场、15 日出现两场）。
判断数据漂移 vs 代码漂移的方法已写进 `docs/DEV-LOOP.md`。

---

## 未登录可用范围（guest mode）✅ 2026-09-15（devlog/086）

> 用户口径：「我希望用户在没有登陆的情况下也可以尽可能使用所有功能，并明确告知这些功能存在限制。」
> 决策由用户授权我定：**做**，但只开检索路径、内容抓取一律要求登录。

**边界（两态实测，`scripts/capability_matrix.py` + `tests/fixtures/capability_matrix.json`）**：

| 能力 | 未登录 | 依据 |
|---|---|---|
| 本地浏览/搜索/筛选/归档、档案视图 | ✅ | 纯本地 |
| 第三方历史（danmakus / zeroroku） | ✅ | 公开接口 |
| 添加 V 的检索（名称模糊搜 / UID 直查） | ✅ | 匿名 `nav` 也下发 `wbi_img`；`search/type` 匿名 `code=0` |
| 账号信息 / 粉丝数 / 直播状态 | ⚠️ 间歇 | `acc/info` 一次成功一次 `-352`；`stat`/`live` 稳定 |
| 抓投稿与动态内容 | ❌ 要登录 | 匿名 `arc/search` → `-352` → **`HTTP 412 request was banned`**；动态流**直接 412**（IP 级、会持续，**不连累登录态**） |
| 微博内容 | ❌ 要登录 | 匿名 `mymblog` → `302` 登录页 |

**落地四块**：① `wbi` 匿名签名（`allow_anonymous`，只给检索路径；默认严格口径不变）；
② `services/capabilities.py` 能力表（三态 + 用户说明 + 实测依据）+ `GET /capabilities`；
③ **内容抓取闸门**（未登录时 `async_fetch_posts`/`async_fetch_first_screen` 一次请求都不发、
动态名单整条跳过、5 个内容端点 **403**；账号信息与归档不挡）；
④ 前端未登录提示（顶栏「未登录 · N 项受限」入口 + 说明窗先列"现在能做什么"再列受限项 +
`去登录`；批量浮窗与添加 V 浮窗逐项标注，**只标不藏**）。

**验收**：pytest **400 passed**（+24）· `smoke_upstream --cold` **4 ok / 1 skip / 0 FAIL**（匿名能搜到、
内容端点 403、匿名 uid 被拒时 503 而非 404）· 暖态冒烟 **5 ok**（登录态不受影响）·
`ui_probe --capabilities` **绿且反向验证过**（入口一藏即红）· 能力表与实测的**双向契约**用例。

---

## 需求清单：R11（2026-09-15 提报，同日落地）

> 原文照抄：
> 「现在添加v只能从候选池中添加，但是这样很多新v就没办法得知了，所以添加v功能应该除候选池
> 同样也可以直接从b站中直接获取，不论是uid还是名称模糊搜索，先给我一个实现方案，确认后再执行」

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R11 | 添加 V 除候选池外，**也要能直接从 B 站获取**（uid 精确 或 名称模糊搜索） | 新 V（池快照里没有的）也能加进来 | 高 | ✅ devlog/083：① 后端新增 `services/bili_search.py` + `GET /vtuber/bili/search`（uid→`acc/info` 精确；否则 `wbi/search/type` 模糊搜；预算 0.8s 串行 / 20 次每分 / 5 分钟缓存 / 最多 3 页）；② `POST /vtuber/adopt` 加 `source`：池外通道**必须服务端自己打一次 `acc/info` 校验**（客户端给的名字不算数，非 bilibili 平台 400，"没问到"回 503 而不是 404）；③ `GET /vtuber/pool/search` 合并 `vtubers.csv` + `thirdparty_vtubers` 两个来源；④ 前端 `AddVtuberDialog` 双来源 —— 本地输入即防抖、**B 站只在显式触发时检索**（回车/按钮），uid 输入换「按 UID 添加」、已入库行置灰"已订阅"、上游失败原样展示 `hint`；⑤ 新增探针 `--add-v`（敲键不打上游 / 行可命中 / UID 换档）。<br>⚠️ 需要 **B 站登录态**（WBI 密钥要走 `nav`，未登录 -101）：真机复核时更正了"不需要登录"的错误结论，并把未登录从 500 修成 `not_logged_in` + 提示（devlog/083 §十） |

> **用户确认的四个口径**（方案阶段拍板，实现严格照办）：
> ① B 站检索 = **显式触发**（不做按键防抖直连上游）；② 点结果行 = **直接收录**（不插预览卡）；
> ③ 本地检索 = **csv 候选池 + `thirdparty_vtubers` 索引合并**；④ 翻页**最多 3 页**。

> **两条值得回看的经验**：
> 1. **B 站搜索接口的"静默 0 条"**：缺搜索页请求头时 `code=0` 但结果为空 ——
>    这与"没有这个人"在界面上长得**一模一样**。凡是上游可能"安静地失败"的地方，
>    都要有一条"如实报错"的路径（本批落到 `error`/`hint` + 探针断言）。
> 2. **子资源端点的路由顺序**：`/vtuber/bili-search` 被先注册的 `/vtuber/{vtuber_id}` 吃掉
>    （422 `int_parsing`）→ 改成两段式 `/vtuber/bili/search`。加端点前先看有没有 `{param}` 兄弟。

---

## 需求清单：R12–R17（2026-09-15 提报，同日全部落地）

> 原文照抄用户写法（"一句话需求 / 期望效果"）。⚠️ 编号说明：用户原写 **R1–R4**，
> 与已落地的 R1–R11 重号 → 由我重编为 **R12–R15**（用户已确认，devlog/085 的受理记录见
> `docs/TODO.md` §0 的「编号说明」）；**R16 / R17** 是随后追加的两条（截图驱动的界面要求）。

| # | 原文需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R12 | 把顶栏状态胶囊改造成统一的消息通知控件和类似灵动岛的多功能控件 | ① 各类关键信息的呈现 ② 优雅流畅的动画效果 ③ 后续可能的自定义内容，例如空闲时轮播经典语录 | 中 | **R12a**（devlog/089）：`utils/notificationHub`（优先级 alert>progress>report>message + 过期/常驻/命名规则，12 单测；「自动节拍不占顶栏」升级为具名规则+反向用例）+ `StatusIsland` 四态（面板 portal+fixed）+ 六类信息源（含后端 `fetch-status.rate_limit`，9 处冷却点统一走 `_cooldown_for_rate_limit`）+ 完成报告改常驻条目。**R12b**（devlog/090）：面板入场 `si-panel-in` 220ms / chevron 翻转 / 计数徽章 pop（`prefers-reduced-motion` 保留淡入与瞬时翻转）+ 空闲语录轮播（`utils/idleQuotes` 15 单测、时钟自带且只在空闲时开、`registerIdleProvider` 扩展点） |
| R13 | 动态中包括的直播预约记录到直播日历中 | — | 低 | 后端**零改动**（核实：per-V 的 `GET /vtuber/{id}/future-reservations` 已存在，聚合端点多余）· `api.futureReservations` + `hooks/useReservations`（失败不清空）+ `utils/reservationDays`（按天分组 / 徽章优先级 **场次>预约>休息·待定** / 展示行，9 单测）· 日历格 `.lc-resv`（不动九类色系 / 计数槽放预约人数）+ hover 浮层预约块 · 探针 `--reservations` **自己种一条明天的预约**（devlog/088） |
| R14 | 设置界面：最左侧工具栏底端给一个齿轮按钮，点开进入设置的独立弹窗，可以设置各种参数，例如主题、后端、抓取频率等等 | — | 中 | **R14a**（devlog/091）：齿轮 → 独立设置弹窗 + `app/core/runtime_settings.py` 覆盖层（**19 个可热更键** + 10 条只读理由）+ `GET/PUT /settings` + `POST /settings/reset`（白名单/类型/闭区间/跨字段上限≥下限，越界 400 带中文原因）· 读取路径在 `Settings.__getattribute__` 拦截（实例属性 > 覆盖层 > 类属性）· **零迁移**（复用 `app_meta`，前缀 `settings.`）。**R14b**（devlog/092）：「浅色 / 跟随系统」偏好（`prefs.theme`）+ `utils/theme`（解析/写 `html[data-theme]`/真跟随系统/该说的说明，10 单测）+ 深色**钩子**（`:root[data-theme='dark']` 空块 + `DARK_IMPLEMENTED`，并有**跨语言契约**钉住"界面上那句说明"必须与钩子状态一致） |
| R15 | 一些前端改造：顶栏左侧标题改为粗体、list 视图搜索栏右侧筛选按钮文字布局中、card 视图的平台徽标添加按钮在鼠标不 hover 时自动隐藏并且也不占位，让徽章紧贴下方分割线 | — | 中/低/高（依次） | 三条都**先量后改**（devlog/087）：① 标题 `400 → 700`（只改字重，探针量出文本宽 114.3/150）；② 筛选钮 —— 量出"文字本身左右各 21.5px（差 0）"，真正偏的是被钉在最右角的 caret（组中心右偏 9.25px）；③ `.pill-add` 未 hover 净占位 0（徽标→分割线 **57 → 10px**）且不可命中，hover 展开回 37px。护栏 `ui_probe --polish`。<br>⚠️ 其中 ② **后来被 R16 取代**（见下）——R15 那版让"文字+箭头"这一组居中，文字本身仍偏 5.3px |
| R16 | list 视图中的筛选按钮的样式跟随左栏工具栏中的筛选按钮（附两张截图：左栏「默认」= 参照，list「筛选」= 待改） | 两枚浮片看起来一样 | 中 | 先建**量化护栏**：新探针 `--filter-pill` 把两枚浮片逐项量出来对账 → 真凶是 caret 留在流内必然挤偏文字（实测 −5.3px）。改法：`.pfilter-btn` 换成侧栏那套**配方**（caret **绝对定位右上角** + 字 13 + 内距 8 + `line-height:1`），**宽高照旧 68×30**（用户当场补的口径：「保持同一行中元素的和谐」→ 与同行搜索框齐平）。探针逼出一个构造性缺陷：合成超长文案下 caret 距文字只剩 0.6px ⇒ 文案外套 `.pf-label`（左右各 11px **对称**留白），文字仍是几何中心、长文案让浮片自己长、caret 恒留 9.4px。契约分 STYLE（逐项必须相等）/ SIZE（各行其是）两桶；`--polish` 断言随之收紧（文字偏移 ≤1px、caret 必须 absolute）（devlog/093） |
| R17 | 设置窗口参照参考图做成「左侧栏分类 + 右侧主要内容」的形式（只粗糙参考布局，细节设计内容按照项目规范来） | 左分类 + 右内容；本项目当场拍板：**不做右上动作行**、**导航 6 项照后端分组** | 中 | 两栏 760×min(600,100vh−72)（头/脚驻留，与 `.lc-dlg` 同构）· 左栏 168 近白底 + 右栏纯白（镜像外壳层次）· 导航**数据驱动**（由 `specs[].group` 生成：外观首/关于尾，后端加一组界面自动多一项，探针与 API 逐项对账）· 选中态沿用「左缘 3px 竖条 + 浅粉底」· **切页不丢草稿** + 圆点标未保存页 · 跨字段冲突**同源预校验**落在出问题那一行 · 「恢复全部默认」补上主题（修掉 R14a 的遗留不一致）+ 新增「恢复本类默认」· 外观改三张卡片（深色**只标不藏**）· 字段控件零改动 · 单测 +20、探针扩到 13 步（devlog/094） |

---

## 需求清单：R18（2026-09-15 提报，同日落地）

> 原文照抄：「我希望可以关闭前端界面隐藏到系统托盘，这个时候后台抓取照样进行，
> 但不用渲染前端」。**用户当场拍板两条口径**：① 首次点 ✕ 问一次、之后按选择记住；
> ② P1（隐藏 + 停表）与 P2（深休眠省内存）**一起做**。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R18 | 关闭前端界面 → **隐藏到系统托盘**，后台抓取照常进行，**不用渲染前端** | 点 ✕ 窗口消失、托盘有图标；期间后台继续抓；点托盘图标恢复；托盘可直接退出 | 中 | **改造前**：✕ = `close()` → 窗口销毁 → Tauri 无窗口即退出 → Job Object 连带杀 sidecar（`lib.rs` 只**打印** CloseRequested，不拦截）⇒ 关闭即全退。<br>**Rust**：`tauri/tray-icon` feature + 托盘（左键唤回 / 菜单「显示主界面·后台运行中·退出」）· 拦 `CloseRequested` → `prevent_close` + `hide` + `set_skip_taskbar(true)` + `emit(shell:hidden)` · `RunEvent::ExitRequested` 在非主动退出时 **`prevent_exit()`**（深休眠销毁 WebView 也会走到这里，这条是必须的）· 退出**唯一路径** = 托盘「退出」→ 唤回窗口 + `emit(shell:quit-requested)` → 前端确认（抓取中写明会中断本轮）→ `invoke(quit_app)` → 置 `QUITTING` 再 `app.exit(0)` → 走既有 `RunEvent::Exit` 清理（⚠️ **R20 补正**：托盘退出**不再依赖前端** —— Rust 直接判 `manual_running`，无任务即 `exit(0)`，有任务才唤回窗口走这条确认链；见下方索引 097 行）· **深休眠**：隐藏满 10 分钟销毁 WebView（`DDTOOLKIT_TRAY_SLEEP_SECONDS` 可覆盖，仅供测试），唤回**重建窗口** + 加载 `index.html?restored=1`（SPA 深链接在资源协议下会 404）· single-instance 回调改成 `show + unminimize + set_focus`（原来只 `set_focus`，隐藏时等于"点了没反应"）。<br>**前端**：`utils/shellLifecycle`（**同步**可见性源 + `shell:hidden/shown` 订阅 + 浏览器/探针退化 + dev 钩子）· `hooks/useShellHidden`（**停表**：顶栏抓取轮询与 60s 登录态轮询、状态岛 6s 空闲轮播；**恢复**：立刻补一轮再恢复定时器）· `utils/shellState`（关闭语义三态 `ask/tray/quit` + 深休眠现场持久化，**路径与视图都过校验**、12 小时过期）· `utils/shellBridge`（Tauri 命令 + 浏览器退化）· `components/CloseActionDialog`（首次询问：两个选项各自写清后果 + 记住选择）· 设置「外观」页可改（`prefs.close_action`）。<br>**护栏**：新探针 `--tray-suspend`（三段对照：可见基线在跑 → 隐藏后请求与轮播都停 → 唤回立刻补一轮；**第一段是灵魂**，否则卡死的应用也能"通过"）· vitest +17（`shellState` 11 / `shellLifecycle` 6）· `cargo build` 通过 · 人工验收清单见 devlog/095。<br>⚠️ **探针抓到两个真 bug**（都写进代码注释与 DEV-LOOP）：① 停表判据读了 React 状态 —— 状态有一帧延迟，定时器可能恰好落在那道缝里 → 改读同步源 `isShellHidden()`；② 只在"排程"时判不够 —— 那一发是**还可见时**排下的 10s 后定时器，隐藏后照样触发（实测漏网时刻 `22063`，隐藏发生在 `14349`）⇒ **"排程"与"触发"两处都要判** |

---


## 需求清单：R19–R24（2026-09-15/16 提报，均已落地）

> 原文照抄用户写法（"一句话需求 / 期望效果"）。这六条按用户 2026-09-16 口径
> 「**完成了的从待提需求中移除**」从 `docs/TODO.md` §0 **移**来此处 —— 原文、期望与落地结论
> 都留在本节，§0 只留未落地项（现为 R25）。
> 本批同时补上了 devlog/098 §三 留的那条缺口（当时 R19/R20 在本文只有索引行、没有需求清单章节）。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R19 | 顶栏状态栏空置的时候轮播的语录集暂时下线，等之后库中真有了条目再上线 | 空闲时顶栏只说状态，不转占位语录 | 低 | ✅ devlog/096：`IDLE_CAROUSEL_ENABLED = false` —— 池子 / 取模逻辑 / `registerIdleProvider` 扩展点**全部保留**（单测用 `enabled: true` 覆盖着测），关掉时连 6s 定时器都不开；新增 `data-idle-carousel` 作开关的**单一事实来源**，探针判据整体反向（恒为状态文案 + 索引恒 0 + 池内无进度词照旧断言）。**下线不是删掉**：将来接弹幕热词/名场面时把开关翻 `true` 即可（同批要改探针那组"必须轮播"的断言） |
| R20 | 修两处实测问题：① 选了「最小化到托盘」后**托盘右键退出也退不掉**；② 设置窗口渲染问题（选项胶囊被压成竖排单字、`**粗体**` 标记原样显示） | 托盘退出必须真退出；设置窗口排版与文案干净 | 高 | ✅ devlog/097：① **根因** —— 托盘「退出」只 `emit(shell:quit-requested)` 等前端确认，而**深休眠/前端未加载时没人接这个事件**（那时托盘是唯一出口）⇒ 退出路径改成**不依赖前端**：Rust 直接问后端 `fetch-status.manual_running`，没任务在跑就 `exit(0)`，有任务才唤回窗口走确认链（`cargo test` 2 条钉住判据）；② 胶囊控件改 `.aps-row-stack`（说明一行、控件整行）+ `.aps-radio{white-space:nowrap}`，并加**文案扫描用例**（SPECS / 只读表 / prefs 的 label·unit·effect·note·why·option 出现 `**` 或反引号就红）；新探针 `--close-ask`（ask 弹框 → 记住 → 隐藏 → 再点不再问） |
| R21 | 设置窗口与同类二级弹窗改版（用户给了 4 张参考图）：① 左侧分类精简到 **4 项** ② 抓取设置**只留关键项**并按用途分组 ③ **文字排版更醒目** ④ 数字输入框改整行 stepper ⑤ 所有二级独立弹窗右下角按钮改圆角浮片 ⑥ 开关改"稍大药丸内嵌滑块 + **去掉外框底色** + 浮片质感" | 没有专业背景的用户也能看懂每一项在调什么；视觉与项目浮片语言统一 | 高 | ✅ 分三批：**批 1**（devlog/100）导航 **6 → 4 项**（外观 / 抓取设置 / 数据源 / 关于）+ 抓取设置按用途分 5 小组 + 19 个热更键分两档（关键项 11 在明面、调优 8 收进页尾「高级」默认收起，**一个键都没删**），两级分组**全部由后端下发**（界面只排版）；**批 2**（devlog/101）排版落成数字（字段名 14px/600 · 说明 12px/行高 1.5 · 行内距 9px · 控件列 150px）+ 数字框改整行步进条（**中间仍是真 input**，键盘能直接敲）+ 开关拆掉外层胶囊壳、滑块 32×18；**批 3**（devlog/102）六个二级弹窗页脚统一 `<FloatPill>`（**没写一行新 CSS**，复用 §C2 配方）+ vitest 完整性扫描（弹窗清单冻结，新增弹窗必须被判一次） |
| R22 | 数据放在 C 盘会不会挤爆：**A** 图片缓存加上限 + LRU 淘汰 · **B** 占用可见（关于页）+ 数据目录可迁移到其他盘 · **D** 库维护（删数据真的还盘）· C = `posts.raw_json` 瘦身（**用户口径：暂缓**） | 用户知道谁在占地方、能自己决定数据放哪 | 高 | ✅ 全部分批落地：**A+D**（devlog/103）`IMG_CACHE_MAX_MB`（默认 300，环境变量可覆盖）+ `prune_cache`（**命中刷新 mtime ⇒ 近似 LRU**，热图不会被误删）+ 新模块 `services/db_maintenance.py`（体检 `dir_stats` / `auto_vacuum=INCREMENTAL` **带 512MB 门槛** / 回收 `freelist`）· **B1**（104）`GET /settings/storage` + 两个清理动作（清缓存 / 整理库）+ 关于页「存储占用」面板 + 低空间提醒（<5GB，同机 3 天一次）· **B2a–B2e**（105–112）数据目录**指针**（与数据目录平级的固定位置、原子写、坏指针回退默认并把原因带给界面）→ 启动优先级（**环境变量 > 指针 > 默认**）与便携版判定 → 迁移核心（规划 / 复制 / **逐文件校验**）→ 接线（选目录 → 停后端 → 复制 → 校验 → 写指针 → 探活，任一步失败**回滚**）→ **三次真机失败修复**（109 脏目标目录改用 `DDToolkit-data-<epoch>` / 110 不复制 `-shm` 且探活窗口不能贴着 `busy_timeout` / 111 `canonicalize` 的 `\\?\` 前缀让后端打不开库）+ 壳侧文件日志与停后端加固（112）；**真机迁移已验证通过** |
| R23 | 应用内从 GitHub 获取更新包：签名密钥 · 更新源 · 发布链 `latest.json` · 关于页「检查更新」 · 启动静默检查 · 便携版不自我更新 · 代理兜底 | 用户不必手动下载安装包 | 高 | ✅ 四批（devlog/113–116）：**a** minisign 密钥对（私钥在**仓库外**）+ `createUpdaterArtifacts` + `plugins.updater`（endpoints + pubkey）+ 发布链生成 `latest.json`（纯函数 + 6 条契约用例，**url 必须版本化**）+ `verify` 四条清单校验（把"签名缺失/字段写错"挡在发版前）· **b** 关于页「应用更新」面板（检查更新 / 下载并重启安装 / 进度 / 失败红字 / 打开发布页）+ 便携版与浏览器**优雅退化** + 启动 15s 静默查一次（发现新版本只发一条状态岛提示，**不自动下载**）· **c** 代理兜底（`reqwest` 已带 `system-proxy`；失败后探本机常见代理端口，只设**本进程**环境变量重试一次）· **d** 错误三分类（`remote` = 远端还没发布 **不试代理** / `network` = 才试代理 / `other` 原样抛）。⚠️ **端到端验证要等第一个 Release 建好**（此前「检查更新」必然报 remote 类失败） |
| R24 | 托盘状态下减少资源占用 | 后台挂着时更省内存/电 | 中 | ✅ **按用户口径收窄后落地**（devlog/117）：**T1** 看门狗退避（没隐藏 5s / 距阈值 >60s 30s / 快到点 1s，抽成纯函数 `watchdog_nap` + 1 用例四态）· **不再启动预热 jieba 词典**（那本前缀词典常驻 **56.4MB** 只换一次性 0.7s ⇒ 删掉 `create_task(_warm_tokenizer())`）；**「隐藏后降频」明确不做** —— 用户 2026-09-16 口径"**即使挂在后台也要尽可能快地获取更新**"（这是 R25 消息推送的前提），已写进 `ARCHITECTURE.md` §3.10 第 4 条作为设计约束 |

---

## 需求清单：R27（2026-09-16 提报，同日落地）

> **原文照抄**（用户口径"按你安排的流程来"；R27 是 devlog/124 盘点的 Q2-A，用户点头全按建议默认）：
> 「**风控冷却要"记得住、会升级"**：触发风控的冷却窗口目前**只在内存**（重启 / 应用内更新后
> 立刻恢复满速）、固定 10 分钟不升级、解禁后也没有减速恢复期」。
> **用户当场定两条口径**：① 梯度与恢复期**全按建议默认**（10 → 20 → 40 分钟、封顶 60 分钟、
> 连续 6 小时无命中归零、解禁后减速恢复期）；② 启动时若仍在冷却窗口内 ⇒
> **自动档等完剩余时间，手动抓取仍可越权**（显式意图优先）。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R27 | 风控冷却要"记得住、会升级"：冷却窗口只在内存（重启/更新后立刻满速）、固定 10 分钟不升级、解禁后没有恢复期 | 被限流后不会"重启继续敲"；连续撞墙时自动拉开间隔 | 高 | ✅ devlog/125。**新模块 `app/services/rate_limit.py`**：不可变 `State`（平台 / until / reason / hits / last_hit_at / ended_at）+ 纯策略（`escalated_seconds` 1→base / 2→2×base / ≥3→4×base 封顶 60 分钟 · `decayed_hits` 6h 归零 · `register_hit` · `mark_ended` · `ramp_factor` / `ramp_scale` 取最保守）+ `app_meta` 读写（键 `ratelimit.<平台>`，坏数据跳过、写失败只记日志）。<br>**调度接线**：`scheduler` 的 `_rate_limit_until/_reason` 两个模块级变量 → `_rl_states: dict[平台, State]`；`_note_rate_limit(reason, seconds, platform)` 返回**本次实际时长**（`seconds` 降级为基准）；8 个冷却调用点全部传入平台（`acc.platform` / 字面量 `bilibili`）—— 期间被既有用例抓到**真 bug**：账号循环里 `db.close()` 之后 `acc` 已 detached，再读 `acc.platform` 抛 `DetachedInstanceError` ⇒ 改成**关会话前先取出平台名**；`_cooldown_for_rate_limit` 睡够后 `mark_ended` 记恢复期起点并落库。<br>**启动不遗忘**：`start_scheduler()`（lifespan 先于调度线程）读回状态并逐平台告警，`_tier_loop` 再兜一次。<br>**自动档跳过、手动档不跳**：`_active_dynamics_lanes` 把冷却中的平台加进 `skipped`（与"微博未登录"同一种处理），新增 `_filter_cooling_accounts(accounts, auto=)` 供账号流用（抽成函数就是为了能单测"手动不受影响"这条口径）。<br>**恢复期**：`_dynamics_next_due` 里把轮间隔按 `1/ramp` 拉开（≈半预算）—— 放在**间隔**上而不是改共享的预算上限，免得连带影响手动档排期。<br>**状态契约**：`rate_limit_status()` 从"单值"改为**取剩余最长的那个平台**，字段 `{active, reason, seconds_left}` **保持兼容**并新增 `platform` / `hits`（顶栏状态岛零改动）。<br>**护栏 12 条**（`tests/test_rate_limit.py`）：梯度与封顶 / 6h 归零 / `register_hit` 升级后回落 / `mark_ended` 开恢复期 / `ramp_scale` 取最保守 / JSON 往返与坏数据 / **写库→清内存→读回（重启不遗忘）** / 第 2 次命中翻倍并被状态报出 / **按平台隔离**（B 站冷却不连累微博）/ auto 过滤 vs 手动不过滤 / 冷却平台整条动态名单跳过 / 冷却过期后不再拦；既有 `test_fetch_status_exposes_rate_limit_cooldown` 同步改成新契约（并把落库打成空操作，显示与落库分开测）。<br>**文档**：`ARCHITECTURE.md` §3.5 冷却行重写、缺口行改为只剩 R28；`backend-fetch-pipeline.md` §7.2 加"600s 只是基准"的口径 + 启动级一行；`GLOSSARY.md` 新增「风控冷却」术语行。⚠️ 梯度/上限/归零/恢复期是**代码常量**，不外露成设置项（同 R22 的判断），要调改 `rate_limit.py` 顶部常量 |

---

## 需求清单：R28（2026-09-16 提报，同日落地）

> **原文照抄**（用户口径"按你安排的流程来"；R28 是 devlog/124 盘点的 Q2-B/C，用户点头按建议默认）：
> 「**动态流闲下来时别硬跑满**：连续 N 轮无新帖就把轮间拉长（有新帖 / 手动动作 / 开播立刻恢复）；
> 另把 `_PlatformBudget` 的"预算至少装得下一轮"改成**一次性**而不是永久抬高上限」。
> **用户当场定两条口径**：① 退避梯度 **2 / 5 / 10 分钟**（连续 3 / 6 / 12 轮无新帖）；
> ② 预算改成**真上限**。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R28 | 动态流闲下来时别硬跑满：无新帖就拉长轮间；预算逃逸口改成一次性 | 安静用户的日请求量大幅下降；稳态速率不再随账号数无限上抬 | 中 | ✅ devlog/127。**① 预算当真上限**：新增纯函数 `_dynamics_round_budget_seconds(cost, rpm)` —— 一轮装得下不干预，装不下则下一轮至少等 `(n / rpm) × 60s`；`_dynamics_next_due` 把它并进 `interval` 的 max 链。于是稳态 = `rpm` 次/分钟（用例直接断言 `30 / interval × 60 == 12.0`），**账号数不再抬高均速**（想更快就在设置里调高 `DYNAMICS_BUDGET_RPM`）。<br>**② 空闲退避**：常量 `DYNAMICS_IDLE_LADDER = ((12, 600), (6, 300), (3, 120))` + 纯函数 `dynamics_idle_floor(streak)` + `_note_dynamics_round(stored)`（只在**正常跑完**的轮里计数：跳过/异常不算"库很安静"）+ `note_dynamics_activity(why)`；`_dynamics_next_due` 把档位下限套在 `since + floor` 上（与既有周期下限同源）。**三个恢复点**：抓到新帖（`_note_dynamics_round` 里 stored>0）、**手动抓取**（`async_fetch_posts` / `async_update_unarchived_posts` 入口）、**T0 检测到开播**（`live_sweep_core` 的 0→1 直播边沿）。<br>⚠️ **与 R25 的关系写进了代码注释与文档**：开播检测走 T0（1 请求/分钟，本批不动它）⇒ **退避不影响推送时效**，别用"推送要快"整条否掉。<br>**护栏 9 条**（`tests/test_dynamics_backoff.py`）：档位分档与封顶 / 计数递增与清零 / 手动清零 / 预算函数只在超 rpm 时生效且取最严重平台 / 口径断言（稳态=rpm）/ `_dynamics_next_due` 真的被档位影响（3/6/12 轮 → ≥2/5/10 分钟）/ 账号数超 rpm 时轮间隔被拉长 / **手动入口确实调了清零**（借内容闸门在请求前返回）/ **开播边沿确实清零**（stub 批量接口）。<br>**顺带**：`tests/conftest.py` 的隔离夹具扩到 `_dynamics_idle_streak`（R27 那批只隔离了风控状态；不隔离会跨用例串台）。<br>**文档**：`ARCHITECTURE` §3.5 新增"动态流节流"行、把"已知缺口"改成**两条缺口均已收口** · `backend-fetch-pipeline` §4.3 的轮期公式与"预算按需自适应"段落改写（写明抬升只对"这一轮"生效）· `GLOSSARY` 新增「动态流节流 / dynamics pacing」行 |

---

## 需求清单：R26（2026-09-16 提报，同日全部落地）

> **原文照抄**（起因：用户问"我们用来发出请求的头信息是不是硬编码写死的，这样如果程序被其他人
> 使用了，这个头信息是否容易被风控"）：① **设备号 cookie 名写错**；② 补齐真浏览器会发的
> `sec-ch-ua` / `Sec-Fetch-*`，去掉脚本痕迹 `Connection: keep-alive`，评估开 HTTP/2；
> ③ UA 永久冻结在 Edge 150；④ `runner.py` 的机器人 UA 版本停在 `0.5.2`。
> **用户 2026-09-16 定两条口径**：② 的**只做零风险两项**（去 `Connection`、刷新 UA），
> **不补 client hints、不开 HTTP/2**。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R26 | 请求头与设备指纹卫生：设备号名字、脚本痕迹头、UA 版本、机器人 UA | 去掉跨用户完全一致的"工具签名"；每个安装有自己的设备身份 | 中 | ✅ 分两批：**①**（devlog/126）**设备指纹**——真机 A/B 证实服务端不认 `bvuid3`（带它时仍会铸一枚新 `buvid3` 回来）⇒ `cookie_str` 改发 `buvid3`/`buvid4`，`_ATTR_MAP` 两个来源名都认、`_apply_cookies` 定成"规范名赢"，并新增 `ensure_device_ids()` 照浏览器调 `x/frontend/finger/spi` **按安装领一份**落 `.env`（绝不写死；只补缺不覆盖）。<br>**②③④**（devlog/128）**请求头卫生**——新增**零依赖**模块 `app/core/useragent.py` 作 UA **单一来源**（`UA_MAJOR = "153"`，2026-09 的真实稳定版；盘点时全仓曾有 **4 个大版本** 131/150/126/150），B 站 `BASE_HEADERS` 去掉 `Connection: keep-alive`（浏览器在 h1.1 下不显式发它）、改用共享 UA，`bili_search` / `img_proxy` / `weibo` / `weibo_auth` / `danmakus` 全部改为引用同一份常量，`runner.py` 的自报家门 UA 改从 `settings.VERSION` 取（原来写死 0.5.2）。<br>**明确不做**（用户口径，理由写进模块说明）：**client hints**（GREASE 串只能靠猜，猜错的组合比不补更像脚本）· **HTTP/2**（httpx 要加 `h2` 依赖 + 重打后端产物，收益不明）。<br>**口径**：UA **发版时刷新 `UA_MAJOR` 一处**（`app/core/useragent.py` 顶部）。<br>**护栏 5 条**（`tests/test_user_agent.py`）：**结构化扫描**——`app/` 下只有 `core/useragent.py` 允许出现 UA 字面量（防止再长出第 5 个版本号）· Chrome/Edge 版本自洽且同源 · B 站头用共享 UA 且**不含** `Connection`、其余四个头一个不少 · 六个消费方都指向同一常量 · `runner` 的 UA 含真实 `settings.VERSION` |

---

## 需求清单：R29（2026-09-16 提报，同日落地）

> **原文照抄**（用户口径"按你安排的流程来"；R29 是 devlog/124 盘点的 Q2-D）：
> 「**风控状态要让托盘用户看得见**：进入 / 解除冷却时发一条系统通知（现在只做进了顶栏状态岛，
> 收进托盘后没人看界面）」。
> **用户当场在三个方案里选了 A**：**托盘提示**（零新依赖），而不是 Windows 系统通知
> （要加 `tauri-plugin-notification` + capability）或归到 R25 一起做。
> ⚠️ 共同代价已在选型时说明：**动壳 ⇒ 已定稿的 v1.0.2 产物里不会有它**，
> 照 R22 收口那次的处理 —— **代码先落地，跟下一个版本发**。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R29 | 风控状态要让托盘用户看得见（现在只做进了状态岛，收进托盘后没人看界面） | 后台挂着时也知道"被限流了、正在等" | 中 | ✅ devlog/129。**壳侧**：新增命令 `set_tray_status(text)` —— 改托盘 tooltip 与菜单项 `status` 的文案（`TrayIcon` **没有** `menu()` getter ⇒ 建菜单时把 `MenuItem` 句柄存进托管状态 `TrayStatusItem`）；文案是纯函数 `tray_status_texts()`（`None`/空串/全空白 ⇒ 复位「后台运行中」），失败只写 `logs/shell.log`。**前端**：`utils/trayStatus.ts`（纯文案：`风控冷却中 · B 站 · 剩余 8 分钟`，第 2 次起追加 `连续第 N 次`；不足 1 分钟显示 1 分钟）+ `shellBridge.setTrayStatus`（非 Tauri 静默退化）+ `hooks/useTrayStatus`（可见时吃顶栏那条 2s 轮询、**隐藏时自带 60s 心跳**）。<br>**关键取舍**：R18 的口径是"隐藏即停表"（探针 `--tray-suspend` 就守着它），而托盘提示恰恰要在隐藏时更新 ⇒ hook **隐藏瞬间刻意不打第一发**（那时界面刚同步过、且探针看的正是"隐藏后有没有请求"），第一节拍放到 60s 后；该例外写进 `ARCHITECTURE.md` §3.10 第 2 条与探针 help。<br>**护栏**：`cargo test` +1（文案复位/裁空白）· vitest +6（文案 5 条：不在冷却返回 null / 平台与分钟 / 不足 1 分钟 / 次数 / 平台代号映射；桥接 1 条：非 Tauri 静默不抛）· **实跑 `ui_probe --tray-suspend` 通过**（可见 1 次 → 隐藏 0 次、轮播停 → 唤回补 1 次 ⇒ R18 判据未被破坏）。<br>**用户可见面**：README 的 FAQ「关掉窗口后还在抓吗」补了一句托盘提示 |

---

## 需求清单：R30（2026-09-16 提报，同日落地）

> **原文照抄**（用户在"总结防风机控手段"之后追加的两条之一）：
> 「**夜间降频这一项可以改为定时时段降频**，因为即使 vtuber 全天都会开播，
> 但**用户不会全天醒着**，所以做成**可配置项**」。
> **用户当场定两条口径**：① **默认关闭**，由用户显式开启并指定时刻；
> ② **只降动态流**（T0 保持 60s）。
> 另一条追加问题「HTTP/2 有实际收益吗」→ **实测后判定不做**，数字记在
> `app/core/useragent.py` 与 `docs/ARCHITECTURE.md` §3.5（不单列需求号）。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R30 | 可配置的「静默时段」降频：在用户指定的时段里把抓取节奏降下来 | 睡觉时段少发请求，又不牺牲醒着时的时效 | 中 | ✅ devlog/130。**纯逻辑**：`quiet_hours_active(now_local, enabled, start, end)`（**含开始不含结束**；`start > end` 按**跨午夜**；**`start == end` = 不生效** —— 避免误设成"整天静默"）+ `quiet_dynamics_floor()` + `quiet_hours_status()`（`fetch-status.quiet_hours` 给出 `{enabled, active, start, end, dynamics_min_seconds}`）+ 时段进出各记一条日志（状态翻转时才记，事后能对账"为什么这几小时变慢了"）。<br>**接线**：`_dynamics_next_due` 里把静默下限与 R28 空闲档**取更保守的那个**（`max`）。<br>**设置项 4 个**（`config.py` 类属性 + `runtime_settings.SPECS`，都可热更）：`QUIET_HOURS_ENABLED`（默认 **False**）· `QUIET_HOURS_START`（3）· `QUIET_HOURS_END`（9）· `QUIET_HOURS_DYNAMICS_MIN_SECONDS`（900，收在「高级」）；前三项进**可见白名单**（这是用户该看到的决策），并**复用既有小组「定期动态轮询」**（页内小组数仍 5，探针对账不用改）。<br>**护栏 9 条**（`tests/test_quiet_hours.py`）：关闭时永不生效 / `START == END` 不当成整天静默 / `[start, end)` 边界 / 跨午夜 / 下限只在时段内非 0 / 快照字段自洽（不写死 active）/ `_dynamics_next_due` 真的被静默下限影响 / 与 R28 档位取更保守 / **源码级护栏：`_live_poller_loop` 里不许出现 quiet 标识**（T0 被"顺手"降下来就会让日历场次时间变粗）。<br>**同步**：`test_runtime_settings.py` 的白名单与默认值对账（8→11 可见、8→9 高级、19→23 默认值）· `UI-MAP.md` 的设置页计数与新增说明 · `ARCHITECTURE` §3.5 新行 · `backend-fetch-pipeline` §4.3 一行 · `GLOSSARY` 新增「静默时段」 |

---

## 需求清单：R31（2026-09-16 提报，同日落地）

> **原文照抄**（用户在"切换卡顿正常吗"的实测结论之后的选择）：
> 三个选项是 **A 缩短 `EXIT_MS`** / **B 连点跳过退场** / **C 交叉淡化**，
> 用户选 **「B + 轻微 A（推荐）」** = **连点直接落地** + **退场 200ms → 150ms**（不做 C）。
> 背景数字见 devlog/132：视图切换 210ms、V 切换 225–230ms、连点 270ms、长任务 0 条
> ⇒ 时间几乎全在刻意退场上，而连点 270 ≈ 60 + 210 说明**第二次点击又等了一整轮退场**。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R31 | 切换手感：连点不该重播退场，退场本身也略短一点 | 连点立刻到位；单次切换少等 50ms，且不出现「正在加载」闪帧 | 中 | ✅ devlog/133。**规则**：`utils/sceneStep.ts::planSceneStep`（纯函数）—— `exiting && !exitingIsForTarget` ⇒ 新目标**直接提交**；**例外**：换 V 而数据没预取好仍要 `wait-prefetch`（不能提交空场景）；退场是**为当前目标**播的则继续等它自己的定时器。**`EXIT_MS` 200 → 150** + `.scene-exit` 动画 `0.18s → 0.13s`（恒短 20ms）。<br>**踩到的坑（探针 20s 抓到）**：第一版把"退场态"直接当连点判 ⇒ `setScene(exiting:true)` 让 effect 自跑一遍、当场提交 ⇒ **退场被整个跳过**（单次切换 210ms → **15ms**）⇒ 引入 `exitForRef`（退场属于谁）+ 入参 `exitingIsForTarget` 修掉。<br>**护栏两条**：vitest **+11**（规则穷举 + 「`.scene-exit` 动画必须短于 `EXIT_MS`」的布局契约）· `ui_probe --switch-perf` 新增硬判据 **`exitReplayed`**（连点必须**比单次更快**，否则判失败；改前反向验证过：连点 270 ≥ 单次中位 210 ⇒ **红**）。<br>**实测（同机 dev，1280 宽）**：视图 210 → **165ms** · V 切换 230 → **170ms** · 连点 270 → **75ms** · 长任务 0 条；`--scene` 原护栏复跑通过（提交 250 → 200ms、中途失败 0）。<br>⚠️ v1.0.2 产物已冻结，这条手感改动**进下一个版本** |

---

## 需求清单：R32（2026-09-17 提报，同日落地）

> **原文照抄**（用户问）：「我这个应用在较低性能的机器上运行的怎样，或者最低限度要求怎样的性能」。
> 我按"整棵进程树"实测了一轮后给了三个落地选项，**用户选 B**：
> 「**README「最低配置 + 实测占用」+ 固化体检脚本**」；顺带对那 215 个测试残留目录（504MB）选了
> 「**让用例自己清场 + 现在清一次**」。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R32 | 低配机器上到底跑得怎样、最低要什么配置？（把结论写进 README 并能复跑） | 使用者能自己判断"我这台能不能跑"；数字不会随版本漂移失真 | 中 | ✅ devlog/134。**先量**：整棵进程树（壳 + WebView2 六进程 + 后端 + conhost）= 空闲 **243–257MB**、**194–199 线程**、**4301–4582 句柄**；启动 **热 2.2–2.9s / 首启 3.1–4.4s**（后端自报占大半，其中 `import app.main` 855–1207ms）；空闲 CPU **0.86–1.19s/60s（1.4–2.0% 单核）**；**托盘深休眠后 9→3 进程、230–251 → 91.5–92.5MB（省 139–158）**；**单核亲和代理 ×1.25–1.74**。<br>**落地**：① `scripts/perf_report.py`（零依赖：两轮启动 + 进程树 + 空闲 CPU + 深休眠 + 亲和代理；`--json`/`--keep`；跑完自清）+ `tests/test_perf_report.py` 5 条纯函数护栏；② README 新增**最低/舒服配置表**与实测占用 + "内存紧张就收托盘"；③ `ARCHITECTURE.md` **§3.13**（完整表 + 代理指标口径 + **诚实边界：没在真低配机/软件渲染/真实库上量过**）；④ `DEV-LOOP.md` / `GLOSSARY.md` 同步。<br>**三个"尺子坑"都留档**：端口取最后一条 boot 行（多轮日志是追加的）· 亲和实验必须先热身（空库迁移单核 742→4951ms 会淹掉信号）· **PowerShell 终止错误 rc=0 + 空 stdout 被打成一排 0** ⇒ 采样改成"总是输出 JSON + ok/error/tree/rows"，**量不到就说量不到**（这条差点让我把"应用深休眠后整个退出"当成真 bug，靠壳自己的 stdout 才澄清）。<br>**顺带清场**：`src/testtmp.rs::TempRoot`（`Drop` 自删，含"路径被文件占住"那条路），三处用例改用它 ⇒ `cargo test` 前后 `%TEMP%\ddtk-*` **新增 0 个**；215 个旧残留（504MB）已清。<br>门禁：pytest **508**（+5）· cargo **31**（+3）· vitest 277 · tsc 0 · eslint 0 · 语法 **106** · doc_check 0 FAIL · `perf_report.py` 实跑通过 |

---

## 需求清单：R33（2026-09-17 提报，同日落地）

> **原文照抄**（用户两条，各附截图）：
> **①** 「窗口的四个角上还是有几个像素点的白边，虽然我只截了一个角但四个角都有」；
> **②** 「如果在 card 视图右上角自定义弹窗中设置过签名和头像，那么 vtuber 左栏应该也对应这个签名和头像」。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R33 | ① 窗口四角的白边去掉；② 左栏跟着「档案设置」里的签名与头像走 | 边角干净；设置过的东西在哪儿看都一样 | 中 | ✅ devlog/135。<br>**① 白边**：`.app-shell` 只有 4px 圆角、且没有任何 border/shadow ⇒ 白只可能来自圆角抗锯齿**背后那一层**。逐像素量到"底色按 ~85% alpha 压在白底上"（`#FFA2B4`→`#FDB3C1`、`#4B5A6F`→`#657183`）⇒ **WebView 自己的背景是白的**（`transparent(true)` 只让窗口透明）。先把 DWM 排掉（`DWM_CORNER_PREFERENCE=DONOTROUND` + `BORDER_COLOR=NONE` + `SWP_FRAMECHANGED` 后**数值一字不变**）⇒ 修法：`setup()` 里 `set_background_color(Color(0,0,0,0))`（该 API 同时设窗口与 WebView 两层）+ `rebuild_main_window()` 的 builder 加 `.background_color(...)`。修后角上像素变成"与桌面同温的暗色"，白混子消失（前后像素表 + 放大图留档）。<br>**② 左栏跟随档案设置**：根因是左栏自己拼了一条 `bili.avatar_path ?? bili.avatar_url` + `bili.sign`（**只看平台字段**），而卡片走 `sign_override → 来源账号 → 主账号` 与 `vtubers.avatar` ⇒ 抽 `utils/avatarSource.ts::resolveAvatar`（与 `signSource.ts` 对称），**卡片与左栏同源**；左栏签名改用 `resolveSign`。<br>**护栏**：vitest **+6**（`avatarSource.test.ts`：自定义优先/空白归一/缓存优先/URL 回退/全空 undefined）+ **第 16 个探针 `--profile-sync`**（往副本 DB 种 `sign_override`+`avatar` → 断言**左栏那一行实际渲染**的签名与头像 URL，并与卡片交叉对账 + **无 override 的对照组**不许被污染），**反向验证过**（改回旧写法：签名与头像两条同时红）· 两个"尺子坑"留档：**虚拟时间下图片加载不完 ⇒ Radix 不挂 `<img>`** ⇒ 给左栏 `Avatar` 与 `.hero` 挂 `data-src`/`data-avatar-src`（**为可测性存在**，同 `[data-hover]` 先例）；**种的头像要挑非 B 站账号**，否则旧代码"恰好"取到同一枚 ⇒ 断言假绿。<br>⚠️ v1.0.2 产物已冻结 ⇒ 两条都进下一个版本 |

---

## 需求清单：R34（2026-09-17 提报，同日落地）

> **原文照抄**（用户接 R33 之后的两条，附 Clash Verge 截图）：
> **①** 「还能不能让四角变得更加锐利一点，因为现在四个角看起来像是有点虚化」；
> **②** 「窗口如果使用了 Windows 的分屏吸附功能是否可以让圆角变方来填充满角落……
> 这个应用的圆角和分屏吸附的角落填充就做的很好，**有没有原生的方案**可以直接解决这个问题」。
> 我先给原因 + 三个方案（A 调半径 / B 交给 DWM / C 弧上压实），用户选「**先考虑 windows 原生方案**」。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R34 | 圆角更锐利 + 吸附时角落填满，且走**原生**方案 | 角的观感与系统窗口一致；吸附/最大化由 OS 处理，不用我们维护判定 | 中 | ✅ devlog/136。**先量**：中段边缘是 1px 硬边（⇒ 不是渲染糊、不是 DPI 缩放），"虚"只出在角上 —— **4px 弧 + 透明窗 + 桌面照片底**，抗锯齿那几个半透明像素直接和花底混色。<br>**方案 A（对比图已出）**：4/0/6/8/10px 五种半径放大 8× 对比 ⇒ 0px 绝对锐利、8px 与 Win11 系统窗口同量级。<br>**采用 B（原生）**：`DWMWA_WINDOW_CORNER_PREFERENCE=ROUND` + `DWMWA_BORDER_COLOR=NONE` + CSS 半径归零。实测三态：浮动 = 系统 ~8px 圆角（平滑无混色）；**吸附（真按 Win+←，窗口变 `(0,30)-(960,1080)`）= 四角全方、完全填满**（连屏幕中间那两个角也是方的 ⇒ 整窗状态判定）；最大化 = 方。<br>**⚠️ 落地坑**：属性设在 `setup()`（窗口 `visible:false`）会被随后的显示流程冲掉（日志说 `= true`，角却是方的）⇒ 改挂 **`present_window`（`show()` 之后）**，顺带天然覆盖深休眠唤醒的重建路径。<br>**Win10 回退**：`DwmSetWindowAttribute(33)` 在 Win10 失败 ⇒ **HRESULT 当能力探测**，失败保留 CSS 半径（兜底 4 → **8px**），不会变成裸方角；判据只认圆角那次（描边色在旧 Win11 build 上可能不支持）。<br>**护栏**：`cargo test` **+1**（判据纯函数）+ 默认探针三档断言（浏览器里 8px / dev 钩子模拟 DWM 时 0px），**反向验证过**（改成 4px 即判红）。<br>**已知缺口**：`transparent: true` 保留（Win10 回退路径靠它切角）⇒ 没有系统阴影；吸附那条落地后只做了推理（`Win+←` 被 DSH 窗口抢焦点），留给用户在真机确认 |

---

## 需求清单：R35（2026-09-17 提报，同日落地）

> **原文照抄**（用户接 R31–R34 之后的新需求，devlog/137 先做了**只分析不写码**的受理）：
> 「有些 v 会将动态置顶来展示周表或舰礼相关的内容，所以**将抓取到的置顶动态同样置顶**，
> 并且**每日的动态轮询都覆盖它**，保证修改及时被捕捉到」。
> 开工前拍了两个口径（用户都选了推荐项）：① **列表摆法** = 排最前只出现一次
> （`order by is_pinned desc, published_at desc`；不另开 `pinned` 数组，筛选/搜索行为才自洽）；
> ② **刷新力度** = feed 每轮免费刷 + 详情每 6h 最多一次（`PINNED_DETAIL_REFRESH_HOURS` 可调，
> `0`=每轮都拉、负数=只刷列表页字段）。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R35 | 抓到的置顶动态同样置顶，且每轮轮询都覆盖它 | 周表/舰礼不用翻旧帖找；作者改了能及时看到 | 中 | ✅ devlog/139。**先核实再动手**（devlog/137 的结论：后端早就认出置顶，只用来"别让增量停止漏帖"）⇒ 三个缺口各自收口：<br>**① 落库** —— 迁移 `f005`：`posts.is_pinned`（NOT NULL 默认 0）+ `posts.pinned_refreshed_at` + 索引 `ix_posts_platform_uid_pinned`，`MIGRATION_HEAD` 同步 `f004 → f005`。<br>**② 每轮刷新** —— 根因是 `_safe_store_post` 遇唯一约束**只 rollback + 跳过、永不更新既存行**，所以置顶帖内容永远停在首次抓到的版本。新增 `scheduler._refresh_pinned_post()`：列表页字段（标题/摘要/封面/互动数）**每轮免费刷**（0 额外请求）；详情档（opus 全文/专栏 delta）按 6h 节流，**只有真拿到详情才盖时间戳**（失败保留 feed 级刷新 + warn，下轮重试 —— 失败若盖戳就要等一整个窗口，看着像"作者没改过"）。详情合并口径与新帖**共用** `_enrich_dynamic_item()`（原本内联 70 行，两处各写一遍必然漂移）。<br>**③ 撤销** —— `PostRepo.sync_pinned()` 一次 UPDATE 完成"标记新置顶 + 撤销已取消"；**只在第一页解析成功后调用**（平台只把置顶放首页，空集合 = 当前没有置顶），且等本页新帖先落库再同步。风控/网络失败那轮**绝不调**（否则一次失败抹掉全部置顶标记）—— 这条写进了 `ARCHITECTURE.md` 不变量 12。<br>**④ 排序与徽章** —— `paginated` 改 `is_pinned desc, published_at desc`（`by_uid` 保持纯时间序，它喂时间线）；前端 `.post-card-pin`（Pin 图标 + 「置顶」粉底 pill）+ `.post-card.is-pinned` 一圈粉描边（**叠在浮片阴影上而非画左边条**：卡片是封面+正文横排，左边条会被封面吃掉）。<br>**护栏**：`tests/test_pinned_posts.py` **12 条**（A 集合同步 / B 排序与跨页不重复 / C 纯判定 + 刷新两档 + 失败不盖戳 + **B 站与微博两条循环的接线**），**反向验证 7/7 全红**；第 **17** 个探针 `ui_probe.py --pinned`（种一条 **2020 年时间戳**的置顶帖 + 一条当下对照帖 ⇒ 排序失效必掉到末尾），**反向验证 2/2 全红**。<br>**两个尺子坑留档**：① 探针第一版跑在有凭据副本上 ⇒ 后端一起来就抓动态流、`sync_pinned` 把种下的假置顶帖当"已取消"处理（顺带**真机验证了链路**：真上游的置顶专栏被抓到并标上）⇒ 改用**未登录副本**；② 视图钮的名字在 `title` 上不在 `textContent` 上，按文本找按钮一个都找不到。<br>**三个小口径定论**：置顶帖不单独分页（只占第 1 页头部一次）/ 取消置顶静默撤销、不进墓碑 / 数据视图与导出本轮不体现置顶。<br>**同步**：`ARCHITECTURE`（迁移链表 + 不变量 12）/ `backend-repositories-and-routers`（posts 列 + 迁移链 + `PostRepo` 方法 + 排序口径）/ `backend-fetch-pipeline` §5.4 / `GLOSSARY`（置顶动态行）/ `UI-MAP`（卡片置顶标记行）/ `DEV-LOOP`（`--pinned`）· 门禁：pytest **508 → 520** · vitest 283 · cargo 32 · tsc 0 · eslint 0 · `doc_check` 0 FAIL · 探针实跑通过。⚠️ v1.0.2 产物已冻结 ⇒ 进下一个版本 |

---

## 需求清单：R36（2026-09-17 提报，同日落地）

> **原文照抄**（用户接 R35 之后，附场次详情弹窗截图 + 红圈）：「红圈圈出来的区域在上有数据还没抓取下来
> 的时候用一张卡片填充，不要空着，内容是几个展示不同信息的胶囊，同样在上游数据没抓取下来的时候右侧
> 这个卡片高度固定，防止数据一抓到窗口长度变化」。
> 开工前拍了三个口径（用户都选了推荐项 / 第三项给了自定义）：① 胶囊**只放四枚本地就有的值、分双行**；
> ② 未到位处一律**同尺寸骨架**，只有"上游慢/失败"才出文字；③ 只在「未到位 → 到位」这段保证零变化
> （不做"弹窗恒定高"—— 那会让内容短的老场次凭空多一大块空白）。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R36 | 上游数据没回来时：左列空区域用胶囊卡填充、右列卡片高度固定 | 打开即完整、不跳动、不出现"加载中…" | 中 | ✅ devlog/140。**先量**：`.lc-dlg` 高度是**内容驱动**（只有 `max-height`），实测上游落地时 **窗 533 → 557px、两列区 207 → 276px**（右列凭空多出 4 行 A 组指标）—— 用户看到的"窗口长度变化"就是它。<br>**① 左列补卡**：`.lc-dlg-left`（封面 + `.lc-dlg-glance` 纵向排）→ 「本场速览」2×2 四枚胶囊（时长 / 峰值在线 / 弹幕数 / 收益），**第一帧就有值**（本地字段），口径抽到 `components/live/sessionGlance.ts` + **6 条单测**（四枚恒定、顺序、空值口径；收益 `null`→`—`、`0`→`¥0`）。<br>**② 未到位处换骨架**：右列 `.lc-dlg-metrics` **恒定四行**（骨架 → 真值）+ `min-height:109px` 给第五行「在线排名」预留；弹幕信息 `.lc-dlg-slot--danmaku`（两行骨架 + `.lc-skel--cloud` 210px，与 `MosaicCloud` 的 `boxH` 一致）、直播动态 `.lc-dlg-slot--evts` 各套一层槽定 `min-height` ⇒ **未到位/到位/没有/失败四种形态占同一块地方**；「上游响应较慢…」这类提示改挂**标题行**（`.lc-dlg-sec-note`，不占额外行高）。⚠️ 代价是**故意留白**：数据比预留少时卡片底部空一点，换来"打开后窗长不跳"。<br>**③ 护栏**：`ui_probe.py --archive` **连采两格**（点格子后 120ms / 稳定后）比 **滚动内容高** + 窗高 + 两列区 + 右列卡 + 速览卡，另断几何（速览卡紧贴封面下方 12px、同宽 264、四枚胶囊 2×2 不越界）+ **交叉对账**（胶囊与右列同一字段必须一致）。**反向验证 3/3 全红**。<br>**两个尺子坑留档**：① 后端对上游有 **10 分钟缓存** ⇒ 不拦的话第二次跑同一场次"两格都采到位态"（判据空转），探针改为自己把 `/upstream` 压后 2.5s **造**未到位态；② **窗高在内容顶到 `max-height` 后恒等** ⇒ 只比窗高是假绿（反向验证实测：把预留高度改成 0，窗高仍然相等），有牙口的是**滚动内容高**。<br>**同步**：`UI-MAP` B3（三条落点 + 规矩）/ `FRONTEND-ARCH`（新纯模块一行）/ `DEV-LOOP`（`--archive` R36 段与两个坑）· 门禁：vitest **283 → 289** · tsc 0 · eslint 0 · 探针默认三档 + `--archive` 实跑通过 |

---

## 需求清单：R37（2026-09-17 提报，P1 同日落地）

> **原文照抄**（用户接 R36 之后）：「档案卡视图……我们把它称作**档案视图**，然后把那个有直播日历和
> 粉丝趋势的视图叫做**数据视图**……以卡片为基本单位，用户可以编辑卡片的大小、位置、排布，卡片内容
> 由用户自定义，例如有纪念日、优质投稿、大事记、时间线等等，然后支持拓展和自定义」。
> 分批：**P1 命名 + 网格骨架 + 2 种卡片（只读）** → P2 拖拽缩放 + 落库 → P3 扩展点 + 自定义卡片。
> 开工前四个选型全部拍板（用户都选推荐项）：**自研** CSS Grid · 布局存**新表 `profile_cards`** ·
> 碰撞**推开** + 编辑/阅读态**分离** + 窄窗**单列降级** · 扩展点 = **前端卡片注册表**。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R37-P1 | 档案视图 = 卡片画布（命名 + 只读网格 + 纪念日/优质投稿两张卡） | 每个 V 的档案页由卡片组成，之后能自己排布 | 中 | ✅ devlog/141。**先核实**：`profile` 视图当时是「档案卡改版中」占位页（`ProfileView`/`ProfileCard` 已无引用）⇒ 本批是**换掉占位**而不是迁移旧布局。<br>**① 命名**：光条 title「档案」→**数据视图（直播日历 / 粉丝趋势）**、「档案卡」→**档案视图（卡片画布）**；探针视图枚举与 `clickView` 前缀同步（它按 title 前缀点钮，漏改就点不中）。<br>**② 只读画布**：`components/profile/ProfileBoardView.tsx` + `layoutModel.ts`（12 列 × 84px 行；`defaultLayout` 书架式填行 / `clampCard` / `normalizeLayout` **向下推开**消重叠 / `toSingleColumn` 窄窗降级 / `gridStyle` / `cardHeightPx`），**17 条单测**；容器宽用 `ResizeObserver` 量（阈值 560 由**实测**定：窗口 1100/1280/1440 下容器 498/678/838 ⇒ 1100 单列、后两档 12 列，两分支都真的走到）。<br>**③ 卡片注册表**：`cardRegistry.ts`（`registerCardKind({kind,title,defaultSize,render})`，重复 kind **抛错**、顺序 = 注册顺序）+ `cards/index.tsx` 注册点 —— 视图**不认识**任何具体卡片（加卡片不用改视图），**4 条单测**。<br>**④ 两张内置卡**：纪念日（`anniversary.ts`：宽容解析 `2000-05-20`/`5月20日`/`05-20`、倒计时、2/29 平年按 3/1、解析不出就说「未记录」，**14 条**）+ 优质投稿（`topPosts.ts`：排除墓碑 / 优先投稿 / 播放为主点赞兜底 / 同分按时间倒序 + 「按什么排 · 共几条」，**9 条**；卡片自己取数、点一行开帖子详情抽屉）。<br>**护栏**：默认探针三档新增 `_assert_board`（卡片**实渲染高 = 模型像素高** / 不重叠 / 不越界 / **窄窗单列** / 卡片内容非空 —— 空态文案也算内容，但必须是明说的那种），**反向验证 5/5 全红**（撤高度、阈值定成 100、两卡重叠、两张卡各"什么都不渲染"）。<br>**已知边界**：P1 只读（无拖拽/缩放/编辑态、布局来自默认排布）。**P2 拆两半**：**P2a 已落地（devlog/142）** = 迁移 `f006` + `profile_cards` 表 + `ProfileCardRepo`（整版替换）+ `GET/PUT /vtuber/{id}/profile-cards`（越界 422 不夹取）+ `purge` 同步 + 15 条用例；**P2b 已落地（devlog/144）** = 读布局 + 编辑态（手柄 / 网格辅助线 / 重置默认 / 完成）+ 指针拖拽缩放（`moveCard`/`resizeCard` 的**推开**口径、宽上限按右侧剩余空间 `12 - x`）+ 松手整版 PUT（失败回滚到上一版）+ 探针 `--board`（合成指针拖拽 → 格位对账 → **去后端核对已落库**）；**P3 拆两半**：**P3a 已落地（devlog/145）** = 大事记卡接上 `vtuber_events`（扩展点真跑通一次，加卡只写了模块 + 组件 + 注册一行，视图一行没改）；**P3b 已落地（devlog/149）** = 用户自己增删卡片（编辑态 `+ 添加卡片` 菜单列出「已注册但不在板上」的 kind + 每张卡上的 `×`；落点 = `firstFreeSlot` 第一个放得下的空位、尺寸 = 注册表默认尺寸；**不弹删除确认** —— 内容都在库里，拿下来的只是卡片，随时能加回来；**本批按用户 2026-09-18 拍板只做内置卡增删**，自定义内容（文本 / 外链卡 + `config_json`）留到下一批）。<br>**同步**：`UI-MAP` B1.0/B1.3/B1.4（画布四行 + 命名改定）/ `GLOSSARY`（数据视图 · 档案视图 · 卡片注册表三行）/ `README`（功能表 + 去掉"占位页"那句）/ `FRONTEND-ARCH`（新模块一行）· 门禁：vitest **289 → 336** · tsc 0 · eslint 0 · 默认探针三档通过 |

| R37-P4 | 档案卡**贴纸化 + 手势动效 + 边缘自动滚动**（P4a–P4d，2026-09-18 连续四批） | 卡片有"贴纸感"、拖拽跟手、拖到边缘能自动滚 | 中 | ✅ **P4a**（devlog/146）12px 浮起阴影 + 贴纸角标（每卡一枚 / 22px / 右上象限 / 有白环）+ 三张卡重排；**P4b**（devlog/147）手势相位机 `motion.ts`（**18 条单测**）+ 长按拿起（`--ease-pop`，**全站唯一允许的过冲**）/ 跟手 1:1 / 落位收敛；**P4c**（devlog/148）退避 FLIP（**让位与归位都动画**，`flip.ts` **10 条**）+ 连续 px 缩放、跨格吸附；**P4d**（devlog/151）拖到容器上下 64px 触发区自动滚动。<br>**顺带修两个真 bug**（都是新护栏先红抓出来的）：`cellsFromPx` 列/行口径不一致（拖一点点就跨格）、"拖出去再拖回来模型不回退"。<br>**尺子补一课**：量几何前先 `settleTransforms()`（把在飞的过渡 finish 到终点），否则 rect 量到的是动画起点 —— 同族教训已提炼进 `DEV-LOOP.md`（"虚拟时间下 CSS 过渡不推进"）。 |

---

## 需求清单：R1–R10（2026-09-13 提报，同日落地）

> 用户当日一次性提了 10 条（§0 收集区的第一批），**现已全部落地**（R9 分两步：devlog/075
> 撤出内联展示 → devlog/080 落地独立的历史弹窗）。
> 下表**原文照抄**用户写法（"一句话需求 / 期望效果"），便于日后回看当初的意图。

| # | 一句话需求 | 期望效果 | 优先级 | 落地结论 |
|---|---|---|---|---|
| R1 | card 视图的档案设置弹窗还是存在 bug，头像设置的第一个选中后实际展示的是无头像时的默认图，并且签名锁定的逻辑好像反过来了 | 修复 | 低 | ✅ devlog/066：头像改走 `ProxyImage` 三态链（图床无 Referer 直连实测 403 ⇒ 此前回落默认图）；头像与锁定**点击即写入**（库里 `locked_fields`/`vtubers.avatar` 实测全空 = 从未落库）+ 写入后回灌父级（否则重开按旧值播种，看着就像"反了"） |
| R2 | 直播日历好像在定时动态抓取任务完成后会闪动，排查原因 | 修复 | 中 | ✅ devlog/066（①）+ devlog/080（②）：① 根因 = `loading` 一来就把 42 格整块换成加载态，而每轮 `fetch-idle`（动态流 ~80s）都会触发 reload → 网格消失重建（趋势卡同款，还白扔 ECharts 初始化）→ 改为**只在无数据时**显示加载态 + 有旧数据时刷新失败给红字小注；② `fetch-idle` 带 `kind`，趋势图只在 `account`/`external` 时刷新（日历三类都要，理由见 §1.1 收尾表） |
| R3 | 直播日历和粉丝趋势的右上角标注出数据来源 | 例如"数据来自 danmakus" | 低 | ✅ devlog/066：两卡本就有 `.card-src-note` 槽位，改为**按实际数据来源**出文案（日历看场次 `source`、趋势看点的来源），tooltip 写清口径 |
| R4 | 在一些自抓取的数据存在的情况下优先使用自抓取数据 | 例如粉丝趋势中的粉丝数记录，优先使用自己记录的快照因为第三方可能不完全 | 中 | ✅ devlog/066（按用户选的 B）：合并口径抽成 `utils/fanTrend.mergeTrendDays`（**同日 self 优先、第三方只补空洞、逐日生效**）+ 8 条断言；图表拆两条线：self 实线 / 第三方回填虚线。⚠️ 改前行为**正好相反**（后写覆盖让第三方盖掉当天 self） |
| R5 | list 视图中搜索框右侧的筛选按钮文字不居中 | 修复 | 低 | ✅ devlog/066：`.pfilter-btn` 为 caret 留的不对称内距（caret 其实是绝对定位、不占流）→ 改对称 `0 16px` |
| R6 | 综合档中动态抓取分账号并行，并调整抓取频率 | 即先按平台分类取抓取任务名单，然后并行根据名单进行抓取，一轮抓取时间如果小于一分钟则休息至一分钟，如果大于则休息时间保证不触及上限 | 高 | ✅ devlog/070：①`per_platform=DYNAMICS_CONCURRENCY`（当时默认 3）；②新增 `DYNAMICS_MIN_CYCLE_SECONDS=60`，到期改为「轮**开始** + 60s」；③平台内请求间隔上移为**平台级起跑闸门**。真机：旧 30.2s/轮·周期 47~75s → 新 25.7s/轮·周期稳定 60s、请求数同为 7/轮、0 风控。<br>⚠️ **名单口径被 R10 修正**（R6 只抓"每 V 主账号"，微博账号因此从未进过 60s 轮）；起跑闸门又在 devlog/076 因跨事件循环炸过一次 |
| R7 | card 视图中的直播徽标可以点击直接拉起浏览器进入直播间 | hover 有动画 | 低 | ✅ devlog/066：`live_url` 优先、否则 B 站 `room_id` 拼；**未开播也可点**（用户定）；hover 抬起+描边，`reduced-motion` 下只留描边 |
| R8 | 粉丝趋势卡片中右侧的档位按钮根据设计令牌重构 |  | 低 | ✅ devlog/066：描边改 `var(--c-border)`（原写死值与之同值）、容器补 `--sel-bg`、选中改 `--c-primary-deep`（本仓"激活=粉底白字"口径）；几何不动 |
| R9 | 曾用名/曾用签名应归入「账号信息历史快照」，**先不展示**；且要的是"V **在平台上**曾经用过的"值，不是本地手改入库的字符串 | 档案设置窗口里那两处展示撤掉 | 中 | ✅ devlog/075 **撤出内联展示**（删 `.vd-former`/`.vd-acc-former`；`PUT /account` 不再记账）→ devlog/080 **正式落地展示入口**：档案设置 →「已订阅账号」每行新增**历史钮** → `AccountHistoryDialog`（`.ah-*`）：曾用名/曾用签名（**只含平台侧旧值**，按账号过滤 + 其它账号条数提示）＋ 账号信息快照（`stat-snapshots` 60 条，粉丝数/直播状态/开播标题/来源标注）。纯逻辑 `utils/accountHistory.ts`（6 断言）；`--settings` 探针加 4 项，实测曾用值 1 行、快照 60 行 |
| R10 | 动态抓取名单应是**库里所有 V 的所有平台账号**按平台分组，名单之间并行、**名单内部串行**（例：B 站名单 3 个、微博名单 2 个），频率仍标定一分钟 | 修 R6 的理解偏差 | 高 | ✅ devlog/078：`_dynamics_lanes()` 取代主账号口径（`_primary_accounts()` 只留给第三方历史回填）；名单内间隔自适应摊平 `gap=clamp((50s−N×1.5s)/N, 2s, 5s)`（短名单命中 5s 上限）；`DYNAMICS_CONCURRENCY` 默认改 1（保留为紧急开关）；微博未登录时整条名单跳过。实测（7 个 V / 10 个账号）：一轮 40~44s、请求 `{bilibili: 8, weibo: 2}`、0 风控、周期 60~70s。<br>补正见 devlog/079（登录态改由抓取失败标记 + 更正"微博已过期"的误报） |> **同日另两件 §1.1 收尾**（都不是"需求"，是 TODO §1.1 的待办项，2026-09-13 落地）：
>
> | 项 | 结论 |
> |---|---|
> | **§1.1-d `PostsPage` 场景切换机抽 hook** | ✅ devlog/080：先补护栏（`ui_probe --scene`），**并把 devlog/071 的悬案反转** —— 当时"切 V 永久停在 `scene-exit`"是**探针读了点击前的旧 DOM 节点**，机器实际 250ms 就提交完了（`--dump-dom` 给不了真实时间，但虚拟时间对本护栏够用）。护栏就位后抽出 `hooks/useSceneTransition.ts`（只搬不改：预取门控 + 原子提交 + 两个定时器 + 依赖数组逐行照抄；hook 不 import `api`，`prefetch`/`onCommit`/`onFail` 由页面注入）。`PostsPage` 该段 ≈160 行 → 三个小块；hero 与日历位级签名仍一致 |
> | **R2 可选第二步：`fetch-idle` 带 kind** | ✅ devlog/080：新增 `utils/fetchIdle.ts`（`account`/`posts`/`external` + 判定表 + 老事件无 detail 时按"全都算"）；`TopBar` 两处派发带 kind；**趋势图改吃 `trendTick`**（只在 account/external 时 +1），不再被每 60~80s 的动态流唤醒重取 + 重建 ECharts。⚠️ 与原文口径的修正：**日历三类都要**（动态流的 feed 直播卡片会落 `live_sessions`，实测日志 `直播场次入库 feed`），砍掉会让"刚开播"延迟数小时才出现在日历上 |

> **两条值得回看的经验**（都写进了对应 devlog）：
> 1. **R6 → R10 的返工**：需求原文里的"分账号并行"被我理解成"平台内并发"，而用户要的是
>    "名单内串行、名单间并行"。教训：**名单口径（抓哪些账号）要单独确认**，别从"并行"两个字推断；
> 2. **测试量错了轴**：R6 的 pacer 测试只在**一个** `asyncio.run` 里跑，对"跨事件循环"无感，
>    于是线上每轮 ERROR 却全绿（devlog/076）；同一教训在探针（几何断言看不见 `pointer-events`，
>    devlog/075）上重现过一次，**第三次**则是场景机护栏读了过期 DOM 节点（devlog/080）。

---

## 批次 → devlog 索引（原 `docs/TODO.md` §6.1，2026-09-13 移入）

> 想找"某一版/某一批做了什么"从这里入手；门禁基线（当前值）留在 `docs/TODO.md` §6.2。

| 版本 · 批次 | devlog |
|---|---|
| v0.5.0 平台化 + 统计快照 + 品牌整理 | 021 |
| v0.5.1 删除检测（墓碑） | 022 |
| v0.5.2 全文搜索深度升级（P2） | 023 |
| v0.6.0 P4 数据源分离 / P5 档案视图 / P6 界面优化 | 024–026 |
| 应用启动链 / 时效分层调度 / 二级界面重绘 / 档案卡精简 | 027–030 |
| v0.6.1 视图账号状态隔离 | 031 |
| v0.7.0 档案视图细化（P7 + 追加两项） | 032 |
| v0.8.0 直播日历重设计 | 033 |
| v0.9.0 月历按设计稿重构 | 034 |
| v0.9.1 场次内容管道 M1–M4 + 弹幕词云首版 + 滚动条定案 + 前端审计 | 035 |
| v0.9.1 修复：首启卡幕 / 布局滚动条 / LOGO 与视图定序 | 036–038 |
| v0.9.2 修复：列宽乱跳 / 外键回滚 / 加账号立即抓取 | 039–041 |
| v0.9.3 调度重构（综合档）+ 界面细节六项 + 小尺寸图标稿 | 042–043 |
| v0.9.4 收录提速（账号信息 ∥ 首屏 + 头像延后） | 044 |
| 增量漏帖修复（置顶帖打乱流序） | 045 |
| v0.9.5 P8-A 前端快赢 + P8-C 顶栏进度 | 046 |
| v0.9.6 P9-A 数据正确性（场次去重 / 视频合并 / 平台化类型） | 047 |
| v0.9.7 P8-B card 改造 + 档案设置窗口 | 048 |
| v0.9.8 P9-B 启动外部补抓 + 动态流自适应节奏 | 049 |
| v0.9.9 P10-A 筛选行收纳 + 双月历区间 | 050 |
| 顶栏展示策略 + 弹窗草稿保护 + UI-MAP 右栏重整 | 051 |
| 场次对外 id 定权（修"近期场次详情空白"） | 052 |
| 静态审计风险项修复（预算越界 / 旧库桥接 / 未定义令牌） | 053 |
| 静默失效三修（调度线程兜底 / 危险文案 / 探针假失败）+ 预算自适应 | 054 |
| 收尾四处缺口 + 词云抽出 | 055 |
| 前端 P2 分层收敛（PostsPage 纯逻辑 / useLiveSessions / LiveSessionDialog / 词云件） | 056–059 |
| 词云断供调查 → **更正为误判** | 060–061 |
| 弹幕「抓不下来」= 12s 超时误报（30s + 3 次重试） | 062 |
| 弹幕取数拆出详情端点（两段式加载 + 10 分钟缓存） | 063 |
| 上游取数可取消（切场次/关弹窗即 abort） | 064 |
| P2 分层收敛收尾：`PostsPage` 拆出三个视图件（1134 → 859） | 065 |
| 需求清单 R1–R5/R7/R8 落地（头像链路 / 防闪 / 来源标注 / self 优先 / 按钮居中 / 徽标可点 / 令牌化） | 066 |
| R1/R4 补充：档案设置全实时生效、头像规则、趋势来源入 hover | 067 |
| 「基本资料」整节删除（用户确认读法 B） | 067 §四 |
| 签名平台下拉栏 + §1.1 三项收尾（灌词接线 / 进度反馈 / 可取消） | 069 |
| 需求 R6：动态流分账号并发 + 60s 周期下限 | 070 |
| §1.1-d 场景机护栏尝试失败（为何仍未做 + 下次顺序） | 071 |
| 签名下拉栏改版（内嵌 chevron + 参与布局面板 + 渐隐横滚）+ `--settings` 探针 | 072 |
| 签名下拉栏浮层化（portal + fixed）+ 再点收起 + 面板等宽 | 073 |
| 签名来源与覆盖（**A3**）+ 字段锁定退役 + 曾用名/曾用签名（迁移 f004）+ 新表补进 purge | 074 |
| 签名下拉栏"**点不动**"根因修复（radix `body{pointer-events:none}`）+ 面板挂回内容体 + 曾用值撤出展示 | 075 |
| 日志排查：动态流每轮 ERROR = 起跑闸门持有 loop-bound `asyncio.Lock`（R6 回归）+ 顺清同形态隐患 | 076 |
| 日志按天轮转（`app.log.YYYY-MM-DD` ×7）+ 配置搬到 `core/logging_setup.py` 并补护栏 | 077 |
| 需求 R10：动态名单改为「全部账号按平台分组，名单间并行 / 名单内串行」+ 自适应间隔 | 078 |
| R10 补正：微博登录态改由抓取失败标记（去掉每分钟多余探测）+ 修 `check_valid` 并发双探测 | 079 |
| §1.1-d 场景机抽 hook（护栏先成 + **反转 071 的卡死结论**）+ R2② `fetch-idle` 带 kind + R9 账号信息历史弹窗 | 080 |
| 场次上游取数**单飞**（修"点一次详情发四个请求"）+ 观测日志 + 修一条日期炸弹用例 | 081 |
| v1.0.0 发布 | 082 |
| R11：添加 V 支持 **B 站直查**（uid / 名称模糊）+ 候选池与弹幕索引双来源 + 池外收录的**服务端校验**；新增探针 `--add-v`；顺带修掉顶栏断言的假失败（外部同步被误判成自动节拍占顶栏） | 083 |
| 一键发布脚本 `scripts/release.py`（版本同步→门禁→打版→产物校验→推送→Release）+ 上传脚本幂等化；实测澄清「打平判据」与「子资源路由」两个坑 | 084 |
| 管线复盘落地：常驻端到端冒烟 `smoke_upstream.py`（含冷进程模式）+ 真实产物 fixtures（真回包/真 installer 片段）+ 文档漂移门禁 `doc_check.py`；冷进程实测澄清「B 站检索需要登录」的口径（是 wbi 取键逻辑，不是平台要求） | 085 |
| 未登录能力矩阵 P0+P1 | 086 |
| R15 前端三处打磨 | 087 |
| R13 直播预约进日历 | 088 |
| R12a 顶栏状态岛 | 089 |
| R12b 状态岛动效 + 空闲轮播 | 090 |
| R14a 应用设置 | 091 |
| R14b 主题 + 深色钩子 | 092 |
| R16 list 筛选钮跟随侧栏筛选钮 | 093 |
| R17 设置窗口两栏改版 | 094 |
| R18 关闭窗口 → 隐藏到系统托盘 | 095 |
| R19 空闲语录轮播暂时下线 | 096 |
| R20 托盘退出修复 + 设置窗口渲染 | 097 |
| DSH 技能包接入 + 文档口径校正 | 098 |
| v1.0.2 发布 | 099 |
| R21 批 1 · 设置窗口信息架构精简 | 100 |
| R21 批 2 · 设置排版 + 数字步进条 + 开关改版 | 101 |
| R21 批 3 · 二级弹窗页脚统一浮片 | 102 |
| R22 A+D · 图片缓存上限与库维护 | 103 |
| R22 B1 · 存储占用可见 + 两个清理动作 + 低空间提醒 | 104 |
| R22 B2a · 数据目录指针与回退判定 | 105 |
| R22 B2b · 启动优先级与便携版判定 | 106 |
| R22 B2c · 迁移核心 | 107 |
| R22 B2d · 迁移接线 | 108 |
| R22 B2d 修复 · 目标目录不干净导致迁移必失败 | 109 |
| R22 B2d 修复二 · 不复制 `-shm`，探活窗口不再贴着 `busy_timeout` | 110 |
| R22 B2d 修复三 · `canonicalize` 的 `\\?\` 前缀让后端打不开库 | 111 |
| R22 B2e · 壳侧文件日志 + 停后端加固 | 112 |
| R23a · 应用内更新通道与发布链 | 113 |
| R23b · 更新界面与启动检查 | 114 |
| R23c · 更新检查的代理兜底 | 115 |
| R23d · 更新错误分类 | 116 |
| R24a · 看门狗退避 + 不再预热分词词典 + `aps-update` 探针断言 | 117 |
| 发布文档补更新产物与私钥保管 | 118 |
| README 重写为使用者视角 | 119 |
| 发布脚本续跑判据改为问远端 | 120 |
| 后端内存归因 + 词云去留评估 | 121 |
| 数据目录指针共用与失效指针收尾 | 122 |
| TODO 收集区清理 + R19–R24 需求清单补齐 | 123 |
| 请求头与风控面盘点 | 124 |
| R27 落地 · 风控冷却"记得住、会升级" | 125 |
| R26① 落地 · 设备指纹名字修正 + 按安装领号 | 126 |
| R28 落地 · 动态流空闲退避 + 预算真上限 | 127 |
| R26②③④ 落地 · 请求头卫生与 UA 单一来源 | 128 |
| R29 落地 · 风控冷却进托盘 | 129 |
| R30 落地 · 可配置静默时段 | 130 |
| 探针脚本语法事故 + 门禁补齐 | 131 |
| 切换性能实测 + 新探针 `--switch-perf` | 132 |
| R31 切换手感 · 退场只播一次 + EXIT_MS 150 | 133 |
| R32 低配体检 · 整机占用实测 + `perf_report.py` + 测试残留清场 | 134 |
| R33 四角白边 + 左栏跟随档案设置 | 135 |
| R34 圆角交给 Windows（DWM 原生）+ 半径对比 | 136 |
| R35–R37 三条新需求受理 | 137 |
| R38 状态胶囊动效规格 | 138 |
| R35 置顶动态落库与每轮刷新 | 139 |
| R36 场次详情：左列速览胶囊卡 + 未到位处同尺寸骨架 | 140 |
| R37-P1 档案视图：命名改定 + 只读卡片画布 + 两张内置卡片 | 141 |
| R37-P2a 档案视图布局落库 | 142 |
| 置顶帖标题解析 + 徽章右上角 | 143 |
| R37-P2b 档案视图可编辑：拖拽缩放 + 落库接线 | 144 |
| R37-P3a 大事记卡：把 vtuber_events 接上 | 145 |
| R37-P4a 档案卡贴纸化：从「方形表面层」到小组件式卡片 | 146 |
| R37-P4b 档案视图手势动效：长按拿起 · 跟手 1:1 · 落位收敛 | 147 |
| R37-P4c 档案视图动效 2：退避 FLIP · 让位与归位 · 连续缩放 | 148 |
| R37-P3b 增删卡片：把「有哪些卡片」也交给用户 | 149 |
| 拖动闪动修复：跟手补偿取错格位 + 拾起推动画布 | 150 |
| R37-P4d 档案视图边缘自动滚动：内容坐标位移 + 滚动跑道 | 151 |
| R39-A 日期面板加年份双箭头 | 152 |
| R39-B 设置→关于页排版重排 | 153 |
| R39-C 状态胶囊悬停呼出 + 面板居中 | 154 |
| R39-D 光条羽化 + 亮点指示器 + 顶部渐隐 | 155 |
| R39-B2 / D2 复验再收：关于页三段化 + 光条中心→边缘完整渐变 | 156 |
| R39-D3 光条改毛玻璃工具栏：从「调数值」到「换手段」 | 157 |
| R40 数据视图改牌堆：一次只显示一张卡 | 158 |
| R40b 牌堆成环 + 圆点自动隐藏 | 159 |
| R40c 签名同步补漏 + 弹窗排版 + 图片滚轮缩放 | 160 |
| R40d 图片查看器加"抓手"拖动 | c729ea3 |
| R40e 图片查看器缩放/抓手重写：锚点恒定居中 + 松手弹性约束 | e352fb2 |
| R42 三张档案卡改造：自定义纪念日 · 随机投稿 · 时间轴 | 162 |
| R43 第三方抓取耗时排查：纠正误判 + 账号级新鲜度跳过 | 163 |
| R39-D4 光条选中块改粉底：把「面」与「选中」拆开 | 164 |
| R44 第三方超时按接口分开 + 连续失败预算 | 165 |
| 文档数字门禁 + devlog 索引瘦身 | 166 |
| R38 批 1 motion 令牌化 + rise-in 重名修复 | 167 |
| 补两个缺口：TODO 已落地残留 + 规格现状断言 | 168 |
| R38 批 2 胶囊宽度形变 + 三条不变量 | 169 |

> **编号列怎么写**（2026-09-23 定）：
> - 有 devlog 的批次 → **裸编号**（`| 097 |`）；
> - **没有独立 devlog 的批次 → 提交短哈希**（`| c729ea3 |`）。那类批次的完整记录**就在 commit message 里** ——
>   R40d / R40e 是仅有的两例，它们的 message 与原本的索引行长度几乎 1:1（655 vs 661 / 815 vs 853 字符），
>   所以把细节抄进索引行是**纯重复**，已瘦身并改指提交；
> - 范围行（`| v0.6.0 … | 024–026 |`）与哈希格都**解析不到** `doc_check.py` 的正则 ⇒ 不计入"已回填"。
>   这是有意的：它们本来就没有可指的 devlog。**新批次请一行一个裸编号**，别新写范围行。
>
> ⚠️ **索引行只做索引**：第一列放短标签（一行以内），细节属于 `devlog/` 或 commit message。
> 2026-09-23 瘦身前这一列漂成了 600–1900 字符的巨型单元格，占了本文件 60%。

---


## 能力现状（截至 2026-09-13）→ 已移到 `docs/TODO.md` §2（本文件不再重复）

---

## v0.5.1 「删除检测（墓碑机制）」——本项目的灵魂功能 ✅（2026-09-05 落地，见 devlog/022）

**问题**：动态从平台消失后，库里只是"数据不再更新"，没有任何标记证明它被删了、何时发现被删。
争议取证时无法区分"还在只是没翻到"和"已被删除"，证据链断在最关键一环。

**需求**：
- Post 新增字段：
  - `last_seen_at` DateTime NULL（最近一次确认仍在线的时间）
  - `deleted_detected_at` DateTime NULL（墓碑：判定已删除的时刻）
- 判定逻辑（挂在 `POST /vtuber/update-posts` 更新未归档动态流程尾部）：
  1. 本次成功抓取返回的 post_id 集合为 S；
  2. 库中该账号未归档帖子 ∖ S = 缺席集合；
  3. 缺席帖子的 `last_seen_at` 早于上一次成功抓取时间 → 视为连续第二次缺席，写 `deleted_detected_at`；
     否则仅刷新失败计数/保持现状（**防单次分页边界或接口抖动误判**）
- 已归档帖子不参与判定（归档语义即"不再追新"）
- 墓碑一旦写入不可被后续抓取清除（除非人工干预）；若某"已删"帖子重新出现，保留墓碑并记录复活时间（可选字段，实现时定→**本次定案：保留墓碑不录复活时间，仅刷新 last_seen_at**）
- 前端：
  - 帖子卡片墓碑角标（灰化处理可选→**已做：封面灰度 + 「已删」角标**）
  - 归档过滤旁增加「已删除」chip（映射 `deleted_detected_at IS NOT NULL`→**本次长在 chips-tools 的独立 toggle，含计数**）
  - 详情抽屉显示时间线：发布时间 / 最后在线时间 / 删除发现时间（✅）

**验收**：
- [x] 模拟删除场景：库中造一条不在返回集合里的未归档帖 → 两轮更新后打上墓碑
- [x] 单次缺席不打墓碑（防误判用例）
- [x] 已归档帖缺席不受影响
- [x] 前端筛选与角标生效
- [x] 额外加固：增量窗口外缺席不判（停止帖边界）；page_limit/网络失败/风控中断不判；增量轮视频帖不判；无基线帖不判

### 实现注意（墓碑特性通用）

- **迁移链纪律**：新增 alembic 迁移后必须同步更新 `app/main.py` 的 `MIGRATION_HEAD`
  （tests 断言其与 alembic head 一致）
- 冷启动快路径依赖版本号判断，勿漏 bump
- 抓取链路改动后跑全量 `pytest`（当时基线 **265 passed**；当前基线见 `docs/TODO.md` §6.2）

---

## v0.5.x 后续小版本

### P2 全文搜索深度升级 ✅（2026-09-05 落地，见 devlog/023）

- 从 `body_json` 提取纯文本入新列 `body_text`（迁移 `e003` + 一次性回填脚本
  `scripts/backfill_post_body_text.py`，模式同 `backfill_post_published_at.py`；✅）
- `paginated` 的 `q` 扩展匹配 `body_text`；个人库量级（千条级）先 LIKE 即可，
  FTS5 暂缓（✅ 真实库 4354 帖实测毫秒级）
- 回填后新增帖子写入时同步提取（✅ 三处写入路径共用 `app/services/post_text.py`，
  `_safe_json_parse` 式容错、不引入 DOM 解析依赖）

### P3 JSONL 归档包导出 ⏸ → **未完成，条目已移到 `docs/TODO.md` §1.4**（含格式设计原文）

### P4 添加数据源并分离抓取逻辑 ✅（2026-09-05 落地 v0.6.0，见 devlog/024）

- 添加新的数据源例如danmakus.com、laplace.live、zeroroku.com，来获取一些已经被固定化的数据例如粉丝数变化趋势、直播场次、弹幕等等
  - ✅ zeroroku.com：粉丝历史（实测 2023 至今全量）+ 直播礼物日聚合，公开免鉴权
  - ✅ danmakus.com：vup-list（VTuber 索引：企划/公会/房间号，**透传 laplace vup-slim**）；
    直播场次/弹幕端点需登录（401），留 `DANMAKUS_TOKEN` 配置位后续启用
  - ⏸ laplace.live：暂无 API（经 danmakus 透传获取），留空壳
- ✅ **分离抓取逻辑**：新包 `app/services/externals/`（externals=第三方固定化数据只读拉取，
  platforms=平台实时抓取），注册表/适配器/执行器分层，源级+账号级错误隔离
- ✅ 先从变化频率角度考虑需要被展示的数据并且将在抓取频率的上限内进行计划尽可能实时的数据抓取
  - 粉丝历史/礼物：每日 3:00（变化频率天级；首跑全量回填 7433+3010 行）
  - VTuber 索引：每周一 3:30（低频索引，企划信息滞后 ≤1 周）
  - 全部让位式错峰（抓取任务进行中跳过），不对第三方站点加压
- ✅ 存储：粉丝历史并入 `account_stat_snapshots`（+source 列，P5 可视化单表复用）；
  `live_gift_days`（日聚合金额保精度）；`thirdparty_vtubers`（企划/公会索引）
- ✅ 读取端点：`/account/{id}/stat-snapshots?source=`、`/account/{id}/gift-days`、
  `/externals/vtubers?kw=`（候选池增强，P5 视图直接用）

### P5 新的信息展示视图 ✅（2026-09-05 落地 v0.6.0，见 devlog/025）

- 和card、list视图同级 → ✅ 第三视图「档案」（`view: 'archive'`，占用原日历占位钮）
- ✅ 展示粉丝变化曲线：shadcn Chart（recharts；用户定引用库）+ 服务端按天
  降采样端点 `/account/{id}/fan-trend`；双序列（self 直采实线 / zeroroku 回填虚线），
  实测曲线回溯至 2019-07（七海 8453→111 万粉）
- ✅ 所属公会（阵营、企划）：档案卡阵营 Select 编辑（建议=第三方索引 group_name +
  一键采纳写 faction）；企划·公会只读展示（`/externals/vtubers/by-uid`）
- ✅ 人物设定集：`vtuber.setting` 折叠展示
- ✅ 直播日历：月网格（翻月）；绿点=当日直播证据（self 快照断言）、
  满格=当日礼物日聚合（第三方）悬浮显示金额；`/account/{id}/live-sessions` 推导场次

### P6 界面外观和逻辑优化 ✅（2026-09-05 落地 v0.6.0，见 devlog/026）

- ✅ list视图中通过chips切换类型的时候会继承滚动位置，当滚动到深内容的时候切换类型会导致直接滚动到底部
  → 修复：筛选切换（type/搜索/时间/已删）= 用户意图重置，立即滚回顶部
  （此前「指纹缓存+恢复」实测不达预期已 revert，标准列表 UX 定案回顶）
- ✅ 帖子详情抽屉替换为中间对齐的独立窗口
  → Sheet 侧栏 → 居中 Dialog（720px，动效 scale 0.97 迁移自原右移规格）
- ✅ 所有二级窗口都一概依据设计token重绘
  → 自绘浮层（time-pop/filter-pop）去阴影、Dialog/Sheet/AlertDialog 基类
  shadow-lg→none、内部残留圆角清零（UI-MAP「二期待办」全部勾销；
  分页钮 N/A——无限滚动）

### P7 archive视图细化 ✅（2026-09-06 落地 v0.7.0，见 devlog/032）

> 2026-09-06 用户追加两项需求，尚未做（见下方「P7 未完」）。

- ✅ 我想再添加一个近期活动卡片，内容是vtuber的纪念日和近期的大型活动日期等等，需要显示：是什么、什么时候、还剩多少天
  → 「重要日期」卡（`UpcomingEventsCard`）：纪念日（生日/出道日年循环，行内编辑）+ 手动活动（vtuber_events 表增删）+ 自动预约（reservation 帖解析 desc1）；行格式=标题+日期+剩余天数胶囊
- ✅ 我想对直播日历卡片进行改造，需要在一个时间格内可以显示直播类型、直播标题、场次时间等
  → 格内=类型徽章（标题关键词推断）+ HH:mm + 单行标题（最多 2 条）；点击出浮层看全量（起止/时长/礼物）
- ✅ 布局修改参照这个docs/design/screenshots/Snipaste_2026-09-06_00-46-49.png，简单参考下，具体细节自己
  → 上行双列（重要日期窄 + 直播日历宽），趋势/档案全宽在下

#### P7 未完 → ✅（2026-09-06 追加项一并落地，见 devlog/032）

- ✅ 把档案这个卡片移走到另外新建的视图，用来专门展示vtuber的详细设定相关信息
  → 新 `profile` 视图（Fingerprint 图标，第五个视图钮）：档案卡（企划/公会/生日/出道/设定）+ 账号一览卡（头像/粉丝/房间/直播状态）；archive 视图只留 重要日期/直播日历/趋势
- ✅ 粉丝趋势卡片增强：
  1. ✅ 时间轴缩略图，并且支持缩放 → recharts Brush（底部缩略图 + 拖拽选区缩放 + 重置缩放按钮）
  2. ✅ 每天粉丝增减量以柱状图的形式呈现 → 「每日增减」模式（按天 diff，正增主粉 / 负增警示色）
  3. ✅ 灵活的操作逻辑和美观的动画 → 趋势/每日增减分段切换（切换重置缩放不残留），折线/柱状入场动画 400ms

#### P8 一些综合性的前端改动 —— 📋 已规划分三批（2026-09-10）

> 下段是用户 2026-09-10 手写原始条目的**可执行化版本**（含代码定位 / 改法 / 验收）。
> 原措辞可由 git 历史追溯；批内已落地的直接勾掉。

**P8-A｜零风险快赢（→ v0.9.5）**

- [x] **1 背景白色遮罩更淡**：`styles/posts.css` 新增 `.hero-backdrop.custom::after` 覆盖（alpha 减半；
      头像铺底态保持原值），`.custom` 原本只改父 opacity、遮罩全量保留 → 这就是「自定义背景仍发白」的原因。
      ✅ 已落地（视觉待实机确认）
- [x] **2 card 视图去掉企划/公会徽章**：删 hero 内 `.faction-badge` 块 + 死 CSS `.faction-badge`/`.pill-logo`
      （单点使用）。注：「公会」在 card 视图本就不存在，原本只有企划。✅ 已落地；
      **企划的编辑入口迁往 P8-B 的档案设置窗口**（原入口在 ProfileCard，见下条）
- [x] **5 档案视图（档案**卡** profile）改施工中占位**：`profile` 分支换 `.empty-state` 占位；
      `ProfileView/ProfileCard/AccountPicker` 文件**保留不删**，P8-B 要复用其企划 Select 与账号一览。
      ✅ 已落地（archive 视图＝直播日历/粉丝趋势，不受影响）
- [x] **7 list 滚动深度跨平台账号继承**：根因＝切账号只改 `selectedAccount`、`scene.acc` 不变 →
      `key={acc|view}` 不变 → `.os-scroll` 的 `scrollTop` 原样保留；P6-1 的回顶 effect 依赖数组
      里没有 `accountKey`。补 `accountKey`（+ `archived`）依赖与 `setShowTop(false)`。✅ 已落地
- [x] 顺带：把后端早已支持、前端一直没接线的**「已归档 N」chip** 放出来（与「已删」同款）✅ 已落地

**P8-B｜card 视图改造（→ v0.9.7）**

- [x] **3 平台徽章交互**：点击开账号主页（B 站 `accounts.url` 实测 0/8 → 前端兜底拼
      `space.bilibili.com/{uid}`；桌面端直接 `invoke('plugin:shell|open')`，**不加 npm 依赖**）；
      hover 尾部半透明「+」→ 复用抽出的 `AddAccountDialog`；
      长按 350ms 拖动重排（`pointerdown` + `elementFromPoint` + `data-pill-index`，
      零依赖）落库到新增的 `accounts.sort_order`（迁移 **f002**）+ `PUT /vtuber/{id}/account-order`。
      ✅ 已落地（devlog/048）
- [x] **4 「更换图片」→ 独立「档案设置」窗口** `VtuberSettingsDialog.tsx`：背景（上传/清除）、
      名称/企划/生日/出道日/设定（`PUT /vtuber/{id}`，前端类型补 `name`/`avatar`）、
      头像（账号候选 → 写远端 URL）、签名与**字段锁定**（`PUT /account/{id}`）、
      账号管理（`DELETE /account/{id}` 新封装 + 二次确认）；草稿 + 保存。
      ✅ 已落地；并承接 profile 下线后的企划编辑 / 设定 / 账号一览
- [x] 已定决策落实：`accounts.locked_fields`（迁移 f002）+ `scheduler._field_locked()`，
      抓取跳过锁定字段（昵称/签名/头像）—— 否则手改会被下次抓取覆盖

**P8-C｜顶栏抓取进度（→ v0.9.5）**

- [x] **6 状态胶囊显示「任务 - V名 - i/N」**（例：`动态更新中 - 明前奶绿 - 1/11`）：
      后端 `_status` 补 `task` / `vtuber_name` / `index` / `total`（账号流 + 帖子流的
      五个写入点；`_set_post_progress` / `_set_account_vtuber` / `_vtuber_name_of` 三个原语），
      `post.target` 语义三混的问题一并收敛（`target` 仍同步写入以兼容旧前端）；
      前端 `TopBar` 加 `TASK_TEXT` 映射与 `statusParts()` 拼接，V 名缺失时退回过程性文案。
      ✅ 已落地；真实路径实测（更新动态）：`task=update vtuber=明前奶绿 1/2 → 2/2`

#### P9 一些综合性的后端改动 —— 📋 已规划分两批（2026-09-10）

**P9-A｜数据正确性（→ v0.9.6）**

- [x] **1 直播场次重复（self 与 danmakus 同场算两场）**：表内去重**是好的**（实测 泽音 2026-01-24
      两条重叠 danmakus 记录已并成一场）；问题在 **self 快照并入** `_find_group(window=90min)`
      **只比 start 差、不看区间重叠** → 实测 4 个 B 站账号 / 20 条 self 场次里 **4 条本应合并却算两场**
      （明前奶绿 09-03「楚什么楚！」、七海 09-03→09-05 跨 40h 伪场次、弥月 09-09、泽音 09-09）。
      改法：区间重叠优先（含 12h 开放式假定上界）+ 双方进行中时「同标题 + ≤6h」+
      回填 `end_at` 的 24h 闸门。✅ 已落地；真实库副本复核 **self-only 4 → 0**（devlog/047）
- [x] **3 视频与投稿动态合并**：实测 **322 个 bvid 同时存在 `video` 与 `video_dynamic`**
      （video 423 / video_dynamic 369，其中 47 条动态带附言）。
      按定案：**只保留 `video` 一条 + 新增字段 `posts.note`（迁移 f001）**，动态附言以「UP 主附言」标注；
      抓取侧 `_absorb_video_dynamic` 命中 bvid 即不插动态（只补空缺附言），
      历史由 `scripts/merge_video_dynamics.py` 一次性归并。✅ 已落地；
      库副本实跑：删重复动态 **323** 条、写附言 **31** 条，复跑「可归并 0」= 幂等。
      **真实库复核（2026-09-13，只读）**：`video_dynamic` 剩 49 条、其中与 `video` 同 bvid 者 **0**
      （剩余的是"视频本身不在库内"的动态）、`posts.note` 附言 31 条 ⇒ **归并已完成**，
      该"用户侧动作"已从 `docs/TODO.md` §1.3 移出
- [x] **2 平台化帖子分类**：实测 B 站 `image 2453 / video 423 / repost 385 / video_dynamic 369 /
      text 352 / music 2 / article 2`，微博 `text 318 / image 227 / repost 18 / video 1` → **专栏/音乐是 B 站独占**；
      微博平台自动发帖（会员升级/签到/推广）单列 **`system`**（保守句式 + isAd，优先于媒体分类）。
      前端 `TYPE_GROUPS` 拆成 B 站/微博两套，按当前账号平台取用。✅ 已落地

**P9-B｜调度（→ v0.9.8）**

- [x] **4 启动时外部补抓（直播日历 / 粉丝趋势）**：新增 **`app_meta` 通用 KV 表（迁移 f003）**
      存 `external.startup.last_run`；启动链跑一次「每 V 主账号」补抓（独立守护线程 +
      `external` 状态胶囊），<24h 跳过、报错也写时间戳。
      实测：zeroroku `/history` 与 danmakus `/channel` **一次返回全量**（197KB / 单账号
      1349 场）→ **接口无 limit 参数**，「只比对最新几条」落在①账号级新鲜度跳过
      ②源幂等落库（实测一次补抓 fan_history 新增 12 行、live_sessions 新增 19 场）。
      ✅ 已落地（devlog/049）
- [x] **5 动态流自适应节奏（已定预算 12 req·min⁻¹）**：新原语 `_PlatformBudget`
      （按平台滑动窗口，平台间独立）；**轮前按估算记账、轮后补差额**（新帖详情）；
      下一轮到期间隔 = `max(MIN_GAP 30s, 预算等待) ± jitter 15s`；预算 ≤0 退回固定 15 分钟。
      实测 8 个主账号：一轮 8 请求 / 33s，相邻轮 **80s**（≈6 req·min⁻¹ 均值、轮内瞬时 ~14）。
      ✅ 已落地

#### P8/P9 实施顺序（2026-09-10 定稿）

| 版本 | 内容 | 工期 | 前置 |
|---|---|---|---|
| **v0.9.4** | 先落盘当前未提交批次（devlog/044 收录提速 + 045 微博漏帖修复 + 外部任务状态胶囊） | — | — |
| **v0.9.5** | P8-A（4 条快赢 + 归档 chip）+ P8-C（顶栏进度） | 1.5 天 | v0.9.4 |
| **v0.9.6** | P9-A（场次去重 / 视频合并 + `note` / 平台化类型；迁移 f001） | 1~1.5 天 | — |
| **v0.9.7** | P8-B（card 改造 + 档案设置弹窗 + profile 内容迁移；迁移 f002） | ✅ 已落地 | v0.9.5 |
| **v0.9.8** | P9-B（启动外部补抓 + 动态流 12/min 自适应；迁移 f003） | ✅ 已落地 | v0.9.6 |

> 迁移编号按**实际实施顺序**：f001 = `posts.note`（v0.9.6）→ f002 = accounts 排序 +
> 字段锁定（v0.9.7）→ f003 = `app_meta`（v0.9.8）。每次都必须同步
> `app/main.py::MIGRATION_HEAD`（GLOSSARY §8.3 的不变量）。
>
> **P8/P9 全部条目已落地**（2026-09-10）；后续版本号（v0.9.9 / v0.10）留给实机反馈与
> 收尾：P8-B 的交互手感、P9-A 的历史归并脚本执行、以及发布构建。
> **v0.9.9 已用掉**（P10-A 筛选行收纳 + 双月历，见下）→ 下一批可编 v0.10 或直接发版。

#### P10 列表筛选行收纳 + 时间范围日历 ✅（2026-09-10 落地 v0.9.9，见 devlog/050）

> 用户三选项已拍板：**草稿制**（确认后生效）/ **归档三态** / **月历常驻展开**。

**P10-A｜右上工具收敛为单钮（→ v0.9.9）**

- [x] **1 收纳**：`.chips-tools` 由「搜索 + 时间 + 已删 + 已归档」四件收敛为
      **搜索浮片 + 单一「筛选 ▾」钮**（`.post-filter-pop`），三分区
      **状态 / 归档 / 时间范围** + 底部 `重置`；即时生效项沿 §C3 侧栏弹窗口径，无「应用」钮。
      触发器 `筛选` / `筛选 · N` + `.on`，`title` 回显明细。✅ 已落地
- [x] **2 归档三态**：全部 / 仅未归档（接线 `is_archived=false`）/ 仅已归档+计数。✅ 已落地
- [x] **3 时间范围换双月历**（参照用户参考图）：左右面板独立翻月 + 表头一…日 + 恒 6×7 +
      区间色带（两端圆角）/ 端点粉底白字 / 今天环+点 + 预设六枚 + 粉底 `确认`。
      预设量纲**含今天**（近一周 = 今天-6d~今天）、月末夹取；选点规则与面板联动按方案；
      悬停预览未落定区间。✅ 已落地
- [x] **4 组件与样式落点**（零新依赖）：新增 `components/common/DateRangePicker.tsx` +
      `utils/dateRange.ts`（纯逻辑拆出，可脱离组件验证）+ `components/PostFilterPop.tsx`；
      `PostsPage` 删 `timePopOpen`/`timeWrapRef` 与 `.del-btn`/`.arch-btn`/`.time-pop` 全部 CSS
      （≈110 行）；四个筛选 state 与 `filterRef` 未动。
      分组语言（`.pop-group/.pop-label/.pop-chips/.pop-actions`）提为与侧栏共享。✅ 已落地
- [x] **5 验收**：`node scripts/check_date_range.mjs` 30 条断言全过（预设量纲 / 月末夹取 /
      本地解析 / 6×7 网格）；`scripts/ui_probe.py` **三档宽度 0 问题** + 矮窗（`--height 680`）
      0 问题；`tsc --noEmit` 0 错；`npm run build` 通过；`pytest` 258 基线不动。✅
- [x] **顺带修掉**：`archived` 漏在「切账号即重置」名单外（P8-A 遗留）→ 预取种子指纹错配。✅

> 落地要点（两处只有真机探针能抓到，已固化进探针断言）：`.posts-panel` 是 `overflow:hidden`，
> 弹窗越界＝**静默裁掉**左月历/底部按钮 —— ①弹窗需 `max-height: calc(100vh − 252px)` +
> 内部覆盖式滚动（矮窗兜底）；②按「窗口宽」推的窄窗降级会提前命中（`--window-size` 与
> `innerWidth` 差一个边框量）→ 改为按视口退让 `min(512px, 100vw − 574px)`，不再有第二种布局。

#### 顶栏展示策略 + 后台刷新不打断草稿 ✅（2026-09-10，devlog/051，无迁移）

- [x] **自动节拍静默**：`GET /vtuber/fetch-status` 每类带 `auto`（定时档发起）+
      顶层 `manual_running`（与手动端点 409 同源）；前端 `isQuietTask` 把
      **动态流轮询 / 自动账号流**排除出顶栏文案·容器·关窗确认；
      `fetch-idle` 边沿仍读原始 `running`（静默任务完成照样刷新卡片）。
      策略表见 UI-MAP §A1.1。✅ 已落地（+ 测试 `test_fetch_status_marks_auto_runs_and_manual_busy`）
- [x] **按钮禁用同源**：`fetchBusy` 改用 `manual_running`——此前动态流每轮把手动按钮
      禁用掉，而后端其实受理（手动优先抢占自动档）。✅ 已落地
- [x] **bug 修复：动态轮询期间改档案信息被重置**：`VtuberSettingsDialog` 的草稿播种
      改为**显式播种键**（打开态 + V id + 主账号 id），后台刷新换 `vtuber` 引用不再冲掉
      用户正在编辑的内容；关窗作废，下次打开取最新值。✅ 已落地
      （排查同类：`AddAccountDialog` 只在关窗时重置、`BatchFetchDialog` 无播种 effect，
      均安全）
- [x] **UI-MAP 右栏结构重整**：§B1 拆成「B1.0 四视图并列总览 → B1.0.1 共用外壳 →
      B1.1 cards / B1.2 list / B1.3 archive / B1.4 profile（各自子项）→ B1.5 跨视图联动」，
      四视图平级关系一眼可见。✅ 已落地

#### 未完成项总览 → **已移到 `docs/TODO.md` §1**（2026-09-13 重写为四组：可立刻动手 / 需先定口径 / 用户侧动作 / 已搁置）

> ✅ **目录改名 `http-test` → `ddtoolkit` 已完成**（仓库现为 `E:\work\Project\DDToolkit`；
> `4ede9cf` 落地一次性改名脚本 + 登录时计划任务，任务成功后自删）。
>
> 发布构建：v0.9.9 已发布（2026-09-13）；**v1.0.0 已发布**（2026-09-14，覆盖 devlog 060–081，
> [Release](https://github.com/Mrslippe/DDToolkit/releases/tag/v1.0.0)）。

---

## v0.8.0 – v0.9.x 已完成（直播日历重设计 → 内容管道期）

> 独立于 P1–P7 的「直播/档案完善」主线，持续进行中。

- ✅ **v0.8.0 直播日历重设计**（2026-09-06，devlog/033）：照参考图周行列表 → 9 类类型体系（`utils/liveType.ts` 前端兜底 + `app/services/live_type.py` 服务端主判）
- ✅ **v0.9.0 按设计稿（Frame101）重构月历**（2026-09-06，devlog/034）：870 定宽·6 行 7 列·类型色系格·今天粉描边·hover 浮层
- ✅ **v0.9.x M1–M4 内容管道**（后端闭环，本次未开 devlog 的批次）：
  - M1 场次推导：danmakus 主源 + self 快照 ±90min 合并（`source` 组合标记，段数合并中断续播）
  - M2 类型推断 v2：多信号（校正 override > 系列 > 标题评分 > 词库 > 分区 > 纪念日），`LiveSession.category/category_from`
  - M3 场次级详情端点：`GET /account/{id}/live-sessions/{liveId}`（场次 + 分类推断；**2026-09-13 拆出** `…/upstream` 承载第三方取数：弹幕词云 + 指标 + 动态，见 devlog/063）
  - M4 前端呈现：格内首场（时间+标题+N 场计数）、hover 浮层全量、**点击格子 → 详情弹窗**（直播信息 + 分类校正下拉 + 弹幕词云 + 直播动态 + 内容分析预留）、月份切换滑动动画
- ✅ **弹幕词云**（2026-09-07）：danmakus v2 live 词云 top40 → **增量摊铺加权 Voronoi 拼贴**（参考图形态：力导向站点滑动 + λ 面积精确，逐个入池、静止即停；历经 react-wordcloud/圆形域拼贴/圆形气泡簇等版本后由 user 重定需求定案）
- ✅ **趋势卡重写 ECharts 6.1**（2026-09-07）：canvas 自绘 + dataZoom slider/inside（滚轮缩放/拖动平移）+ 双轴（粉丝/日增）+ 1d/7d/30d 概览 + 容量档位（3m/6m/1y/all）—— **recharts 已退役**
- ✅ **滚动条标准定案**（2026-09-07）：不占宽 + 自动隐藏 + OverlayScroll 组件统一（12px/4px 全局 webkit 样式作为非覆盖容器的兜底）；全应用滚动容器已全部接入/对齐（含侧栏迁移、type-chips/cat-pop 隐藏化）
- ✅ **前端系统性审计整理**（2026-09-07）：UI-MAP 全文重写对齐代码；死 API 封装（events/future-reservations/giftDays）与类型删除；`@types/d3-hierarchy` 清理；`--pill-fg` 令牌名统一；图表色值集中 `utils/chartTheme.ts`；次级色统一 `--c-text-sub`
- ✅ **前端架构体检 + P0/P1 收敛**（2026-09，见 `docs/FRONTEND-ARCH.md`）：判定**不需要**独立组件库（单消费方 + 已有 shadcn 层）。
  - P0：删 3 个零引用 ui 件（`sheet`/`toggle`/`toggle-group`，272 行）+ 骨架屏死 CSS 与 `.archive-error`/`.profile-card-head|title|sub`（68 行）；图片三态合并；新增 `.gitattributes` 统一 LF
  - P1：新建 `components/common/` 四个共享件——`ProxyImage`（原 SmartImage + 灯箱第三份状态机并入，补微博直连代理分支）、`FloatPill`（13 处裸类名收口）、`StateBlock`（loading/empty/error 7 处收口）、`StatPill`（含 `PILL_BG` 下沉）；`.lc-nav-btn`/`.lc-nav-pill` 自绘副本删除（−54 行 CSS）
- ✅ **界面细节六项**（2026-09-09，devlog/043）：任务栏图标清晰度（ICO 首项/按尺寸补偿描边 + `build.rs` 补图标依赖，见上表）、顶栏标题字体统一阿里妈妈方圆体、滚动条「粉只属于指针」+ 指针近右缘亮出、二级弹窗关闭钮改 `focus-visible` 焦点环、单图帖封面与正文图按 URL 去重（查看器不再显示两张）、顶栏状态行**定稿：A2 深玫瑰徽章 + 容器只在有事时出现**（空闲=无容器白字+绿点，事件态=#c9406f 徽章+白字，白字对底 4.72:1 达 AA；六轮迭代，见 devlog §7.7）
- ✅ **小尺寸图标专用稿定稿**（2026-09-09，devlog/043 §7.5–7.6）：用户选方案 B 并收细线条 → `docs/design/svg/LOGO-small.svg`（头部轮廓 + 大圆点眼 + 每侧 3 根短胡须，轮廓 stroke 16 / 眼径 26），≤48px 自动接管、≥64px 仍用主稿；24px 下胡须 3.0px、轮廓 1.61px
- ✅ **弹幕取数两段式 + 10 分钟缓存**（2026-09-13，devlog/063）：详情端点只回本地库数据、上游取数走 `/upstream`（此前上游慢会把整个弹窗拖到最坏 93s）
- ✅ **弹幕「没有数据」误报修复**（2026-09-13，devlog/062）：`_LIVE_TIMEOUT` 12s → 30s + 3 次重试（上游会间歇性变慢，实测 1.1s ↔ 15.6s；最近 5 个场次曾 100% 被判"没有弹幕"）

> 更早的 P1–P10 详细条目见本文件上半部分；**Backlog / 远期构想 / 明确不做** 见 `docs/TODO.md` §3–§5。

---

# 以下章节已移至 `docs/TODO.md`（本文件保留占位，避免旧链接落空）

- **Backlog（近期接口/体验，无明确排期）** → `docs/TODO.md` §1（未完成项，已并入四组）
- **Backlog（远期，按价值排序）** → `docs/TODO.md` §3
- **远期构想：让作品被看见（二创发现）** → `docs/TODO.md` §5
- **明确不做（近期）** → `docs/TODO.md` §4

