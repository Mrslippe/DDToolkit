# DDtoolkit 开发路线图（TODO）

> 定位一句话：**个人 VTuber 证据档案库**——定时记录、永不丢失、找得回来。
> 核心闭环（订阅 → 自动抓取 → 浏览 → 归档）已完整；当前主线是补全"档案完整性"，
> 让存下来的数据可检索、可证明、可带走。
>
> **这份文件怎么用（三条）**
> 1. **想到新需求 → 写进 §0**，一句话就行，不必先想清楚怎么做；
> 2. **要干活 → 看 §1**（唯一"待办"清单，分四组：能立刻动手 / 要先拍板 / 用户侧动作 / 已搁置）；
> 3. **想查过去怎么定的 → 看 `docs/ROADMAP-DONE.md`**（已完成条目的原始需求与验收、
>    已落地需求清单、**版本 → devlog 索引**）。
>
> 整理记录：2026-09-13（把历史条目拆到 `docs/ROADMAP-DONE.md`，本文件只留"要干什么"）；
> 同日二次整理：**已落地的需求 R1–R10 与批次索引一并移入该文件**，§0 只剩未落地项。

---

## 0. 📥 待提需求（收集区 —— 新需求写这里）

> **用法**：直接在表里加一行。**只写你想要的**（现象/期望效果），不用写实现方案；
> 优先级拿不准就留空，我会在评估后回填。
>
> **报 bug 请给这四行**（2026-09-15 复盘定，devlog/085）：① 我在做什么（哪个界面 / 按钮 /
> 输入了什么）② 期望什么 ③ **实际什么（截图或原文报错）** ④ 每次都发生吗。
> 有截图 + 最短复现的那次（"加池外 V 报收录失败"）10 分钟就定位到根因；只给一句"好像不对"的那次
> （"微博登录过期"，其实没过期）我按错误前提白查了一轮。
>
> **提功能需求请补一句"怎么算做完"**（验收标准，例：*"我在 B 站搜一个候选池里没有的新 V，
> 能加进来、名字正确、并立刻看到抓取启动"*）。R11 当时若写了这句，"索引来源走哪条收录路径"
> 在方案阶段就会被追问 —— 后来实测到的那个 bug 大概率不会发生。
>
> **状态取值**：`待评估`（刚写下，我还没看）→ `已受理`（已拆成可执行项并移进 §1）
> → `已落地`（**移进 `docs/ROADMAP-DONE.md` 的「需求清单」章节**：原文照抄 + 落地结论 + devlog 指向）
> ｜`不做`（附一句理由，同时落到 §4）。

| # | 日期 | 一句话需求 | 期望效果（可选） | 优先级 | 状态 |
|---|---|---|---|---|---|
| **R45** | 2026-09-24 | **页面工具条**：右栏顶部的视图切换条要「**既能够指示当前处于哪个界面，又不要太突兀去抢占用户的视线**」，并且「**没有需要用到它的时候可以自动隐藏**起来」 | 切换器按需出现、不抢视线；隐藏时**真的把空间还给内容** | 中 | ✅ **已落地**（devlog/174）→ 详见 `docs/ROADMAP-DONE.md`「批次 → devlog 索引」。① **overlay**：从"66px 常驻带子"改成 `position:absolute; height:0` —— 原来的"隐藏"只是"看不见"，66px 还在占位（卡片页可用高 **+66px**）；② **按需出现**：指针进热区停留 **≥140ms** 呼出 / 移出 **+900ms** 收回 / **Tab 聚焦由 `:focus-within` 显形**（`opacity:0 + pointer-events:none` 仍在 Tab 序里，没有这条键盘用户会聚焦到看不见的控件）/ 冷启动与深休眠唤醒各闪现一次；③ **表面**：毛玻璃 → **不透明浮片**（`--pill-bg` + `--pill-shadow`）—— 白 .10 + blur 叠在默认近白纱罩上**贡献精确为 0**（devlog/164 §一 第 3 条已量到，当时只修了柔光那一半）；④ 视图钮 **50→34**（这一行其它控件全 25–30，只有它 50 ⇒ 撑出 59px 的条与 66px 的带子，是"突兀"的尺寸来源）、off 态 **`.4→.7`**（1.89:1 → **3.44:1**，顺手关掉 devlog/164 §五遗留 #2）。护栏：新探针 `--toolbar`（五态）+ 默认三档 `_assert_glow` **整套换口径**（overlay 成立 / **条不与账号切换·分类·搜索的 rect 相交** / 必须有外阴影 / off 对比度 ≥3:1），反向验证 **60 红 → 0**。<br>**R45-A（选中态换语言，devlog/182）**：去描边 → **深粉实底 `--sel-strong #ec407a` + 白图标**。四步被迫：去描边⇒形状靠填充；`--sel-bg` 对**自己这个白条面**只有 **1.10:1**；压深后深图标读不出（1.50:1）⇒ 图标转白；白 ≥3:1 ⇒ 填充亮度 ≤0.30。⚠️ **用户原本要的顶栏粉 `#ffa2b4` 配白图标只有 1.90:1** —— 实测行程中段被瞄准的图标有 **42% 露在白条上**，那一档是白压白会"断成两截"；`#ec407a` 同样暴露但 **3.76:1 仍读得出**。⚠️ **`.float-pill.on` 按用户口径不动**（它影响 4 处，其中 3 处是 12–13px 小字要 4.5:1，**约束方向相反**）。<br>**R45-B（页面标题让开覆盖带，devlog/182）**：list 标题 = **选中账号昵称**、archive = **当前卡片标题**（`DataDeck` 新抛 `onIndexChange`）、profile **本轮不做**、cards 无。新增 `.page-title` + 令牌 `--toolbar-band`；护栏三条（下缘 ≥ 覆盖带 / 不与条相交 / archive 下**导航标题 == 卡片内渲染的标题**）。⚠️ **card 视图垂直居中已否决** —— 用户指出账号一多 hero 会被挤上去，实测药丸每 3 枚一行、9 个账号就撑满，且**每次加账号 hero 都会跳**。<br>⚠️ **四处遗留**（见 §3）：5 个动作钮**没搬进工具条**（1100 档右上组只有 171px，5 枚文字胶囊要 472px）、`glow-bar` 命名债未还、**profile 视图没做标题（它是三个里最严重的：首行卡片通宽，会被盖住中间列）**、**探针两个模式本轮没跑成（esbuild `spawn EPERM`，本沙箱起不了 Vite）⇒ 新判据待复跑** |
| **R25** | 2026-09-16 | **把消息推送到其他平台**（把"某 V 开播 / 发了新动态"这类事件推到外部渠道，让用户不必开着界面也能收到） | 后台常驻时也能第一时间收到通知 | 低 | 📋 **待排期（用户 2026-09-16：再往后放 —— "可能要很后面才会开发"）**。⚠️ **前提约束**：**后台/托盘挂起期间必须保持"尽可能快"的抓取节奏**——所以 R24 的"隐藏后降频"被**明确取消**：降频会直接拖慢推送时效。设计时要顺着这条走（见 `ARCHITECTURE.md` §3.10 第 4 条） |
| **R35** | 2026-09-17 | **置顶动态跟着置顶**：有些 V 用置顶动态放周表 / 舰礼，抓到的置顶帖也要钉在列表顶部，并且**每日轮询要覆盖它**，改了能及时被捕捉 | 周表/舰礼不用翻旧帖找；改了就能看到 | 中 | ✅ **已落地**（devlog/139）→ 详见 `docs/ROADMAP-DONE.md`「需求清单：R35」。迁移 `f005` 落 `posts.is_pinned`；置顶帖每轮刷新（列表页字段免费、详情按 6h 节流）、取消置顶自动撤销、列表排最前 + 「置顶」角标 |
| **R36** | 2026-09-17 | **场次详情别空着**：上游数据没回来时，左列封面下方用一张「信息胶囊卡」填充（不要空着），右侧卡片高度也要固定，防止数据一到弹窗长度跳变 | 打开即完整，不跳动、不出现"加载中…" | 中 | ✅ **已落地**（devlog/140）→ 详见 `docs/ROADMAP-DONE.md`「需求清单：R36」。左列补「本场速览」2×2 四枚胶囊（只放本地值）；右列 A 组指标 / 弹幕信息 / 直播动态三处换成**同尺寸骨架** + 槽预留高度 ⇒ 连采两格高度零变化 |
| **R37** | 2026-09-17 | **档案视图 = 自定义卡片画布**：把"档案卡"这一版做成**以卡片为单位**，用户能编辑卡片的大小 / 位置 / 排布，卡片内容可自定义（纪念日、优质投稿、大事记、时间线…），并**支持扩展** | 每个 V 的档案页由用户自己搭 | 中 | ✅ **R37 六批全部落地**（devlog/141·142·144·145·146·147·148·149 → 详见 `docs/ROADMAP-DONE.md`「需求清单：R37」与「批次 → devlog 索引」）：命名改定（档案卡 → **档案视图**、档案 → **数据视图**）+ 只读卡片画布（12 列网格，`layoutModel.ts` **17 条单测**）+ 卡片注册表（`registerCardKind`，**4 条**）+ 两张内置卡片（纪念日 **14 条** / 优质投稿 **9 条**）。**四个选型已拍板**（自研网格 · 新表 `profile_cards` · 碰撞推开 + 编辑/阅读分离 + 窄窗单列 · 前端注册表）⇒ **P2a 已落地**（devlog/142：迁移 `f006` + 表 + Repo + 两条路由 + purge 同步 + 15 条用例）；**P2b 已落地**（devlog/144：读布局 + 编辑态 + 指针拖拽缩放 + 整版 PUT + 失败回滚，几何用例 17→30，探针 `--board`）；**P3a 已落地**（devlog/145：大事记卡接上 `vtuber_events` —— 也是扩展点的真示例，视图一行没改）；**P4a 已落地**（devlog/146：**卡片视觉规格化** —— 「圆角阴影稍微浮起」的贴纸卡 + 三张卡重排；规格 `docs/design-archive-cards.md`，注册表加 `icon`/`tone` 必填契约，探针加材质/角标/不裁切三组，**反向验证 42 红**）；**P4b 已落地**（devlog/147：**手势动效** —— 长按 350ms 拿起 / 跟手 1:1（视觉位移 ≡ 指针位移）/ 落位收敛 / reduced-motion 保留跟手 + **动效调测页** `?motion=cards`，`motion.ts` **18 条单测**，新探针 `--motion-cards`(±`--reduced`) 与 `--motion-lab`）；**P4c 已落地**（devlog/148：**退避 FLIP + 连续缩放** —— 让位与归位都有动画、补偿量是格距整数倍、缩放手柄连续 px 跟手 + 跨格吸附，`flip.ts` **10 条单测**；顺带修了两个真 bug：`cellsFromPx` 列/行口径不一致导致"拖一点点就跨格"、"拖出去再拖回来模型不回退"导致松手跳一格）；**P3b 已落地**（devlog/149：**增删卡片** —— 编辑态 `+ 添加卡片` 菜单（只列已注册但不在板上的 kind）+ 每张卡的 `×`；落点 `firstFreeSlot`、尺寸取注册表默认；**不弹删除确认**（内容都在库里，随时能加回来）；本批按拍板**只做内置卡增删**，自定义内容留待下一批）；**P4d 已落地**（devlog/151：**拖到边缘自动滚动 / 网格向下拓展** —— 内容坐标位移 `D = P + S`（模型与补偿同一套坐标 ⇒ 卡片视口位置恒等于手指位置）、编辑态 240px 滚动跑道、rAF+定时器双驱动、缩放手柄同样适用；`autoScroll.ts` **10 条单测** + `motion` 滚动项 5 条，探针 `--motion-scroll`）。**R37 全部批次落地**（P1 → P2a → P2b → P3a → P4a → P4b → P4c → P3b → P4d），详见 `docs/ROADMAP-DONE.md`「需求清单：R37」 |
| **R42** | 2026-09-19 | **三张档案卡改造**：① 纪念日卡**开放用户自定义**（名称/日期/emoji）② 优质投稿 → **随机投稿**（大封面、参考图一）③ 大事记 → **时间轴**（参考图二） | 三张内置卡从"只读展示"变成"用户能喂数据、看着更生动" | 中 | ✅ **已落地**（devlog/162）：存储按拍板**复用 `vtuber_events` + 加 `kind`/`emoji`**（迁移 `f007`；⚠️ SQLite 不支持 `ALTER COLUMN` ⇒ 走 `batch_alter_table`）+ 新增 PATCH 端点（`exclude_unset` ⇒ 显式 `null` = 清空 emoji）；三张卡分别重做（增删只在编辑布局态）；护栏 pytest **+4** / vitest **+14**。⚠️ **如实标注**：为这三张卡写的 `--card-content` 探针**没做成**（交互段在虚拟时间 45s 预算里跑不完 ⇒ "卡片都渲染好了但探针没产出"），按规矩**没把半成品留在树里** ⇒ **"渲染面"这一层目前没有机器判据**，下次用更轻的写法（不驱动交互、只断言渲染）补 |
| **R41** | 2026-09-19 | **四条修补**：① 签名改动**左栏及时同步**（用户报"之前修过又出现"）② 直播详情弹窗排版（速览卡去标题去底、四胶囊四色、**两列下端对齐不留白**）③ 图片查看器**滚轮缩放** ④ 修掉 `--reservations` 那条既有红 | 三处"看着不对/用着不对"的地方收干净 | 低 | ✅ **全部落地**（devlog/160）：① 根因是**缺通知通道**（R33 修的是渲染链）⇒ `vtuberList.ts` 事件 + 就地合并（**5 条单测**），探针补上"**当场编辑**"这条真正的缺口；② `.lc-dlg-main` 改 `stretch` + 速览卡去壳 + 四色胶囊（护栏三条）；③ `transform: scale()` + 指针锚点 + `[1,4]`（`nextZoom` **5 条单测**）；④ `clickView('档案')` 是 R37-P1 改名后**前缀匹配错了页** ⇒ 改「数据视图」并拆开两条判据。门禁：十三个探针模式全绿 / vitest 460 / pytest 546 |
| **R40** | 2026-09-19 | **数据视图改牌堆**：抛弃"固定卡片 + 上下滚动"，改成**一次只显示一张卡**，滚轮上下切换（向下 = 当前卡下滑出框 + 背后卡从透明渐显位移缩放；向上 = 当前卡向后渐隐 + 下一张从下方滑入），动画要流畅 | 一次专注一张卡，切换有空间感 | 中 | ✅ **已落地**（devlog/158）：`DataDeck` + `deckWheel.ts`（纯函数 **12 条单测**）+ 探针 `--deck`；拍板：**卡片自适应填充**（`min(631, 可用)`）/ **按 V 记住上次那张** / **右缘竖排圆点** / **滚轮两条通道 + 软锁 150ms + 动画可重定向 + 欠账 ≤2**（用户当场质疑"120ms 静默分界会不会卡手" ⇒ 鼠标离散格每格一张、只有触控板连续流限一次一张）。**复验三条已落地**（devlog/159）：① **滚动成环**（末张向下回首张；两张卡时 `next`/`prev` 撞车 ⇒ 用 `both` 中间位）② 圆点**透明度 50% + 静止自动隐藏**（隐藏时连指针一起关掉）③ **修掉 R36 那条既有红**（骨架词云多 10px 外边距 ⇒ 未到位 319 / 到位 310，去掉后 `[ok]`）。⚠️ 遗留：牌堆只有 2 张卡 ⇒ "快拨连翻多张"在探针里无法演示（改判"锁内反向不吞"），第 3 张卡落地时补一条 |
| **R39** | 2026-09-19 | **四条前端打磨**：① 日期区间选择器加**年份双箭头**（原来只能单月跳，选早年很麻烦）② 设置→关于页**排版整理**（信息/按钮/小字混在一起）③ 顶栏状态胶囊改 **hover 呼出**（离开收起）+ 下拉栏**居中** ④ 右栏顶部：滚动区顶部**渐隐** + 光条**羽化**（去硬边）+ **亮点指示器**跟随当前视图钮（带切换动画） | 四处"看着不整齐/不符合直觉"的地方收干净 | 低 | ① ✅ **已落地**（devlog/152）：头部导航成组 `[上一年][上个月]` / `[下个月][下一年]`，跳年 = `shiftMonths(∓12)`；护栏 = `check_date_range.mjs` +4 条 + 探针新帧 `list-filter-year`（**真点一下**对账"正好跳一年/月份不变/可逆"），反向验证 5 红 → 0。② ✅ **已落地**（devlog/153）：关于页**四段同构**（标题 → 信息/数字 → 动作行 → 注释小字）+ 数值列两端对齐 + 合计前通栏分隔线 + 版本号只出现一次 + 只读项补小节头；护栏 = `--app-settings` 七条排版判据（版本次数/按钮位置/子元素顺序/小字顺序/数值对齐/等宽数字/分隔线），反向验证 7 红 → 0。③ ✅ **已落地**（devlog/154）：hover **进 120ms 弹 / 出 200ms 收**、**点击钉住**（`data-pinned`）、Esc / 点外部 / 条目清空 / ttl 四条收起通道、面板**相对胶囊居中**（原来左缘对齐、实测偏 7px）；规格 `design-status-island.md` 新增 §13.1 口径变更（R38 批 2 起按新口径）+ §10 不变量表补两行并加「实装」列；护栏 = `--status-island` 六条新判据（掠过不弹/悬停必弹/移入保持/离开必收/点击钉住/居中 ≤1.5px），反向验证 3 红 → 0。④ ✅ **已落地**（devlog/155）：**方案 A 顶部渐隐**（滚动体滚下去才挂 mask，开关 = `OverlayScroll` 下发的 `data-scrolled`）+ **光条 2D 径向羽化**（原线性格的上下缘是硬边）+ **亮点指示器**（按激活钮 `offsetLeft/offsetWidth` 定位、220ms 滑动、`pointer-events:none`）；护栏 = 默认探针七条（径向渐变/无圆角描边阴影/亮点居中 ≤1.5px/不吃点击/过渡含 transform/`data-scrolled` 存在/未滚不许有 mask + 新增 `archive-scrolled` 帧判"滚了必须有 mask"），反向验证 3 红/视图 → 0。**复验追加两条已落地**（devlog/156）：② 关于页**去掉合计前发丝线**、按钮挪到该段**最后**、「应用更新」**并入运行信息末尾**（关于页四段 → 三段）；④ 光条改**中心→边缘完整渐变**（横向半径 120% → 72%：原来左右两端仍留约 9% 的白，那就是用户看到的分界线）、光点 **50 → 64px** 且扩散到整圆。**第三轮追加已落地**（devlog/157）：**光条改毛玻璃工具栏**（方案 A）—— 量像素证明盒子边缘亮度落差已是 0.0/0.0/0.8，用户看到的其实是**纹理边界**（半透明白抹掉插画的局部对比）⇒ 换手段而不是继续调数值：`backdrop-filter: blur(6px)` + 极轻白 0.10 + 12px 圆角 + 只允许内描边；**设计指南六条**落 `docs/UI-MAP.md`。**R39 四条全部落地** |
| **R38** | 2026-09-17 | **状态胶囊的灵动岛式形变**：顶栏状态胶囊要"优雅灵动"地**向中间收起 / 展开**，为之后想做的**桌面独立控件**打基础 | 状态变化时是"同一块东西在长大/收拢"，不是闪现一块板 | 中 | 📋 **已受理**：**设计规格已落 `docs/design-status-island.md`**（动效令牌 / 时间轴 / 同心圆角 / 两种宿主材质 / 可断言的不变量 / 5 个批次）—— 动效是"看着好不好"的事，先把规格定下来再实现，评审才有依据。⚠️ **2026-09-19 口径变更**：用户要 hover 展开 ⇒ §13 的「hover 只提亮」被替换（见 R39 ③），批 2 按新口径做 |

> **R35–R37 方案要点（2026-09-17 受理；代码位置都已核实过）**
>
> **R35 置顶动态** —— ✅ **已落地（devlog/139）**：迁移 `f005` 落 `posts.is_pinned / pinned_refreshed_at`；
> `PostRepo.sync_pinned` 在**第一页解析成功后**同步集合（标记 + 撤销），`scheduler._refresh_pinned_post`
> 让置顶帖**每轮刷新**（列表页字段免费刷、详情按 `PINNED_DETAIL_REFRESH_HOURS` 默认 6h 节流，失败不盖戳）；
> 列表排序 `is_pinned desc, published_at desc`；前端 `.post-card-pin` 角标 + 一圈粉描边；
> 护栏：`tests/test_pinned_posts.py` **12 条**（反向验证 7/7 红）+ 第 **17** 个探针 `ui_probe.py --pinned`（反向验证 2/2 红）。
> **三个小口径的定论**（2026-09-17）：① 置顶帖**不单独分页**——它只占第 1 页头部一次，跨页不重复
> （`total` 也不因置顶变化）；② 取消置顶**不进墓碑、不留痕**（静默撤销，帖子回时间线原位）；
> ③ 「数据视图」与导出**本轮不体现置顶**（那是"证据时间线"，保持纯时间序；要动留到 R37 再评估）。
>
> **R36 场次详情占位** —— ✅ **已落地（devlog/140）**。现状回顾：上游（danmakus 的弹幕词云 / 场次指标 /
> 直播动态）没回来时，区块里是 `.lc-dlg-ph` 的「加载中…」文案，而左列封面下方**是空的**（截图红圈处）；
> 数据到达后 DOM 长高 ⇒ **弹窗高度跳变**（实测：窗 533 → 557px、两列区 207 → 276px）。
> **口径**（2026-09-17 用户拍板）：① 胶囊卡**只放四枚本地就有的值、分双行**（时长 / 峰值在线 / 弹幕数 /
> 收益；上游那组不进卡 —— 它们在右列已有行）；② 未到位处一律**同尺寸骨架**，"上游慢/失败"仍出文字；
> ③ 只在「未到位 → 到位」这段保证**零变化**（不做恒定高弹窗）。落地：左列 `.lc-dlg-left` + `.lc-dlg-glance`
> （2×2）+ 右列 `.lc-dlg-metrics`（恒定四行 + 第五行预留）+ `.lc-dlg-slot--danmaku/-evts`（`min-height` 预留）；
> 口径抽到 `components/live/sessionGlance.ts`（**6 条单测**）。
> 护栏：`ui_probe.py --archive` **连采两格**（探针自己把 `/upstream` 压后 2.5s 造未到位态）比
> **滚动内容高** + 窗高 + 两列区 + 右列卡 + 速览卡，**反向验证 3/3 红**（撤速览卡 / 撤骨架标记 / 撤预留高度）。
>
> **R37 档案视图** —— 🔨 **P1 已落地（devlog/141）**，**四个选型 2026-09-17 拍板**：
> ① **自研** CSS Grid（不用 `react-grid-layout`：几何可控、探针可断言、零新依赖）；
> ② 布局存**新表 `profile_cards`**（P2 落，迁移 `f006`；记得同步 `purge`）；
> ③ 交互 = 碰撞**推开** + 编辑/阅读态**分离** + 窄窗**单列降级**；
> ④ 扩展点 = **前端卡片注册表**（`registerCardKind`，照 `registerIdleProvider` 先例）。
> P1 交付：命名改定（档案卡 → **档案视图** / 档案 → **数据视图**）· 只读画布（`layoutModel.ts`，
> **17 → 30 条单测**）· 注册表（**4 → 7 条**）· 两张内置卡（纪念日 **14 → 20 条** / 优质投稿 **12 条**）；
> 护栏 = 默认探针三档的 `_assert_board`（高度=模型 / 不重叠 / 不越界 / 窄窗单列 + 卡片内容非空），
> **反向验证 5/5 红**。
> P2a/P2b 已落地（devlog/142、144：迁移 `f006` + 表 + 两条路由 + 拖拽缩放 + 整版 PUT）；
> P3a 已落地（devlog/145：大事记卡接上 `vtuber_events`）。
>
> **P4a 已落地（2026-09-18，devlog/146）** —— 用户同日口径「圆角阴影稍微浮起的卡片 + 贴纸/小组件式排版」：
> 先出**规格** `docs/design-archive-cards.md`（视觉令牌 / 五槽位卡片语法 / 动效时间轴 / 不变量 /
> 落地批次），**五个决策全部过用户拍板**（12px 圆角 · 轻微过冲 · 长按 350ms 拿起 + 保留按钮 ·
> 批次顺序 P4a→P4b→P4c→P3b · **动效路线 A = FLIP + 跟手偏移**，§9 给了逐环节顺滑度对比）。
> **P4b 已落地（devlog/147）**：motion 令牌 + 手势相位机（`motion.ts` **18 条单测**）+
> 长按拿起 / 跟手 1:1 / 落位收敛 + reduced-motion 口径 + **动效调测页**；探针 `--motion-cards`
> （±`--reduced`）与 `--motion-lab`。三条实测教训写进了规格 §5.1 / §5.5 / §8 与 `DEV-LOOP.md`：
> ① 编辑态不能也要求长按（`--board` 红的）· ② 虚拟时间下 CSS 过渡不推进（动画断言只能判
> "内联/提交值 + 过渡登记"）· ③ 实现里加 rAF 节流会给探针制造竞态（且 reduced 档下浏览器不产帧）。
> **P4c 已落地（devlog/148）**：**退避 FLIP**（让位**与归位**都有动画、补偿量是格距整数倍、
> 拖动卡不参与）+ **缩放手柄连续 px 跟手 + 跨格吸附**；`flip.ts` **10 条单测**。
> 本批又补两条实测：④ 量几何前要 `settleTransforms()`（把在飞的过渡 finish 到终点，
> 否则 rect 量到的是动画起点 —— `--board` 的"零重叠"被误判过一次）·
> ⑤ 顺带修两个真 bug：`cellsFromPx` 列/行口径不一致（拖一点点就跨格）、
> "拖出去再拖回来模型不回退"（松手跳一格）。
> ⛔ **只剩 P3b**（自定义卡片：新增/删除 + `config_json` 载体）。

> **R12–R24 已全部落地，并已按本文件规则移出本表**（原文、期望与落地结论都在
> `docs/ROADMAP-DONE.md` 的「需求清单」章节：R12–R17 / R18 / R19–R24）：
> R12 状态岛/灵动岛（devlog/089、090）· R13 预约进日历（088）· R14 设置界面（091、092）·
> R15 前端三处打磨（087）· R16 筛选钮跟随侧栏（093）· R17 设置窗口两栏改版（094）·
> R18 托盘隐藏（095）· R19 空闲轮播下线（096）· R20 两处实测修复（097）·
> R21 设置与弹窗改版（100–102）· R22 数据目录与占用（103–112）· R23 应用内更新（113–116）·
> R24 托盘资源（117）。**本表当前只剩 R25**（已按用户口径往后放 —— "可能要很后面才会开发"）。
> **R26**（① devlog/126 · ②③④ devlog/128）· **R27**（125）· **R28**（127）· **R29**（129）·
> **R30 静默时段**（130）均已落地 —— **2026-09-16 这轮"请求侧/风控侧"盘点（devlog/124）
> 提出的事项，加上你随后追加的两条（定时时段降频、HTTP/2 是否值得），至此全部收口**。
> **R31 切换手感**（**已在同一轮问答里落地**，devlog/133）也**没进过本表**：它是
> "切换卡顿正常吗"那个问题的后续决定（量完 → 你选"B + 轻微 A" → 当场做完），
> 原文与落地结论直接记在 `docs/ROADMAP-DONE.md` 的「需求清单：R31」。
> **R32 低配体检**（134）同理："低配机器跑得怎样"问完 → 你选 B（README 最低配置 + 固化脚本）
> → 当场落地，原文见「需求清单：R32」。
> **R33 四角白边 + 左栏跟随档案设置**（135）：两条带截图的小需求，同日修完并补了探针
> `--profile-sync`，原文见「需求清单：R33」。
> **R34 圆角交给 Windows**（136）：用户选"先考虑原生方案" ⇒ 圆角/吸附方角全交给 DWM，
> 原文见「需求清单：R34」。**本表当前仍只剩 R25。**

> **编号说明（2026-09-15）**：用户原写 R1–R4，与**已落地的 R1–R11** 重号 → **由我重编为 R12–R15**
> （R1→R12 状态岛 · R2→R13 预约入历 · R3→R14 设置 · R4→R15 三处小改），用户已确认。
> R15 的「中/低/高」按三个子项顺序理解：① 标题粗体=中 · ② 筛选钮文字居中=低 · ③ 徽标「+」不占位=**高**。

> **R1–R11 已全部落地**（R9/R10 见 2026-09-13，R11「添加 V 支持 B 站直查」2026-09-15）→
> 见 `docs/ROADMAP-DONE.md`「需求清单：R11」与「需求清单：R1–R10」；本表只留**还没完全落地**的条目。
>
> **下一条怎么写**（照这个格式加一行；编号接在最后一条之后）：
> `| R26 | 2026-09-dd | 一句话说清现象或想要的效果 | 可选：期望的样子 | 中/低/高 | 待评估 |`

### 0.1 怎么提需求 / 提修改意见（2026-09-15 定，devlog/085）

**三种类型，写法不同**（写在表格里、或直接跟我说都行；一次提 3~5 条也没问题，我会拆批次）：

| 类型 | 请给我 | 例子 |
|---|---|---|
| **bug / 不对劲** | 四行：① 我在做什么（哪个界面·按钮·输入了什么）② 期望什么 ③ **实际什么（截图或原文报错）** ④ 每次都发生吗 | *"添加 V 里我搜一个候选池没有的名字 → 期望能加进来 → 实际弹『候选池中不存在』（截图）→ 每次都这样"* |
| **功能 / 想加的能力** | 想要的效果 + **怎么算做完（验收标准）** + 边界（可选：什么情况**不**该发生） | *"能按 B 站 UID 直接加 V；**算做完** = 我输入 uid 能搜到、点一下加进库、名字是 B 站上的那个"* |
| **修改意见 / 感觉别扭** | 一句话说清"哪里别扭"就够（不用给方案） | *"粉丝趋势那两个档位按钮看着不像按下去了"* |

**四条经验（都来自本项目真实返工）**：

1. **截图/原文报错 > 文字描述**。"加池外 V 报收录失败"那张截图让我 10 分钟定位根因；
   而"微博登录好像过期了"（其实没过期）让我按错误前提白查一轮。
2. **验收标准是最值钱的一句**。R11 当时若写了"搜池里没有的新 V → 能加进来且名字正确"，
   "索引来源走哪条收录路径"在方案阶段就会被追问，你后来实测到的那个 bug 大概率不会发生。
3. **只给约束，不给实现**（除非你有硬要求）。你可以说"必须复用现成接口""不能打上游"，
   但"用哪个函数、怎么改"交给我 —— 我会给方案 + 代价，再动手。
4. **决策类问题请直说归属**：`必须这样` / `可以这样` / `都行，你定`。
   最省事的是像 R11 那样一次拍四个口径（检索是否显式触发、点结果是否直接收录、本地来源范围、翻页上限）
   —— 我不用猜，"你说了算"的条目我会**写明我的决定与理由**再落进本文件的 §1/§4。

**我收到之后的固定动作**（你可以据此检查我有没有偷懒）：
`写进本表 → 我给方案（含代价与验证方式）→ 你确认 → 实现 → 同批次补用例/探针/文档/devlog → 报门禁数字`。
功能类改动我会**先建护栏再动手**；修 bug 我会**先补一条会红的用例**再修（并反向验证它真的会红）。

---

## 1. 🔧 未完成项（当前待办）

> 这是**唯一**"要干活"的清单。每条都写了性质与阻塞，避免"看起来能做其实做不了"。
> 已完成的条目不在本文件堆着 —— 见 `docs/ROADMAP-DONE.md`。

### 1.1 可以立刻动手（无外部阻塞）

| 项 | 性质 | 说明 / 下一步 |
|---|---|---|
| ~~**词云自建扩展点接线**~~ ✅ **早已落地（2026-09-25 核实后关闭，本条是过时条目）** | 生产代码**已经在调**它：`app/routers/vtuber.py:633` 把 **V 名 / 企划 / 设定 / 账号昵称**四路灌进 `build_extra_words()`，再传给 `build_word_cloud(extra_words=…)`；端到端用例 `test_wordcloud_endpoint_passes_vtuber_words_to_tokenizer` 断死 kwargs（6 passed）。⚠️ **本条曾被误判为"没接线"**：只 grep 了 `count_tokens(` 的调用方（在 service 层），而路由器调的是 `build_word_cloud` ⇒ 漏判。**教训：判定"某能力没接线"要顺着调用链看，别只搜一层。** |
| **自建词云首拉耗时** | ⚠️ **移进 §1.2**（这是口径决定，不是代码任务） | 实测 5 万条记录 ~12s 属正常，上游瞬时变慢时单场可达 **120s**（devlog/062 §四）。**刻意不加时间预算**：截断取靠前记录会系统性丢掉下播前的高频词（如「晚安」）—— 要不要牺牲完整度换响应速度，得先定口径 |
| **自建词云也支持取消** | ⚠️ **移进 §1.2**（同上；且当前优先级最低） | 上游取数已可取消（devlog/064）；`/wordcloud` 走同一套（`api.buildLiveSessionWordCloud` 加 `signal` + 调用处 abort）。因它只在用户点按钮时发、120s 是可接受上限，优先级最低 |

| **R38 状态胶囊形变动效** | 前端（规格已就绪，见 `docs/design-status-island.md`） | **五批全部落地 + 收尾（devlog/196）**：**① motion token 化 ✅（devlog/167）** → **② 胶囊宽度形变 + 三条不变量 ✅（devlog/169）** → **③ 面板锚定 + 同心圆角 + 两段式跨级 ✅（devlog/170 —— 前两项已被 R39-C 超前实现 / 无对象，实交面板圆角 14px + 同心圆角判据）** → **④ 打断/重定向 ✅（devlog/171；`.si-count` 的重定向在收尾批补上）** → **⑤ `density="widget"` 宿主 ✅（devlog/172）+ 独立小窗 ✅（devlog/173 起，用户 2026-09-24 已拍板「设置里一个开关 + 与顶栏胶囊并存」；其后 devlog/175–188 九轮真机修复）**。护栏照规格 §10 的不变量写（顶栏高度恒定 / 右簇位移 <1px / 锚点误差 ≤0.5px / 中间态圆角恒为高度半 / 可打断 / reduced-motion 只留淡入）。<br>~~⚠️ §5「点击目标 ≥28px」未做~~ → **✅ 2026-09-25 收尾**：复核发现**那个 28px 没有标准依据**（批 3 因此白挂起一轮），按 [WCAG 2.2 SC 2.5.8](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html) 原文改成 **24 × 24 CSS px + 间距例外**，写成机器判据（采**全部真实目标**的矩形，含面板动作钮这个最小对象；探针为此新增 dev-only 注入通路，否则只量到一个必然通过的胶囊）。实测胶囊 302×25、动作钮 65×25 ⇒ 通过。<br>⚠️ **判据必须能变红**：反向验证三项各自精确报红（目标 20×18 且贴住胶囊 ⇒ 尺寸+间距双破 / `.is-out` 改 140ms ⇒ 复位相不再是瞬时 / 改回 `animation: si-pop-in` ⇒ 重放）。<br>⚠️ **批 1 实跑纠正了规格两处**（devlog/167 §一）：① 规格说"三处零散时长"实际是 **5 处**，且 `rise-in` 不在状态岛里；② 规格说批 1"零行为变化"与 §2 的令牌值**矛盾**（现有 180/300/200 都不在令牌集）—— 用户拍板**按 §2 对齐**，所以批 1 **确实改了视觉**（徽章/文案 → 140ms、chevron → 220ms、面板曲线换标准曲线）。**批 2 起引用 §2 的值时别再假设"现状 = 规格"**。<br>⚠️ **批 2 又纠正一处**（devlog/169 §一）：§5 说"折叠高 24 → 展开高 30"，但 §4 说形变时"高度固定"、§10 要求"中间态高度恒定"——**三处互相矛盾**，用户拍板取 §4/§10（**只做宽度，高度恒定 25px**）。**批 3 引用 §5 时先确认那句是胶囊还是面板**（§5 那段是两者混写的）。 |
| **待排期（可选，不做也不影响）** | 无阻塞，等你挑 | ① **空闲轮播上线**（R19 下线了；等库中真有条目 —— 接 `registerIdleProvider` + 翻 `IDLE_CAROUSEL_ENABLED`，同步改探针断言）；② **深色主题**：R14b 的钩子已就位（填 `:root[data-theme='dark']` + 翻 `DARK_IMPLEMENTED`），但真正做要先把 **243 处硬编码色值**收敛成令牌 + ECharts 双主题 + 逐屏走查 —— 单列一批；③ **能力受限入口并进状态岛面板**（`NoticeActionKind.open-limits` 已预留）；④ 参考图右上的动作行（复制诊断信息 / 打开数据目录）——R17 按用户口径没做，**2026-09-26 起"复制诊断"这一步已经有后端了**（`GET /settings/diagnostics`，批次 16），只差把它接到那一行 |
| **`migrate_data_dir` 的回滚编排补 4 条集成测试**（批次 16 的 ④，2026-09-26 明确没做） | 纯代码任务，无阻塞 | 计划要求的四条失败路径（复制失败 / 校验失败 / 指针写失败 / 探活失败）各断言"**指针未变 + 旧目录内容未动 + 后端仍在旧目录上跑**"。卡点是那句编排现在长在 `#[tauri::command] migrate_data_dir` 里（要 `AppHandle`），**先把它抽成"注入 effects 的纯函数"**（照 `lib.rs::delete_old_dir` 的先例）才好测。今天只有真机验证（见 `docs/DEV-LOOP.md` §一 的真机现场②） |
| **真机"关停"冒烟**（批次 6 的补充，2026-09-26 记） | 纯真机动作，无阻塞 | 关窗 / 托盘退出时看 `logs/app.log` 是否出现「调度运行时已停止（3 个线程已退出…）」且进程在 `scheduler.STOP_JOIN_TIMEOUT`（15s）内退出。⚠️ **自动化覆盖不到 graceful 那一半**：`scripts/dev_check.py` 的后端冒烟走 `proc.terminate()` = Windows 硬杀（不经过 lifespan），目前只有 `tests/test_scheduler_lifecycle.py` 直接驱动 `app.main.lifespan` 那一条（替身掉碰盘碰网的四件事、不起真进程） |

### 1.2 需要先定口径 / 拍板（不是写代码的问题）

| 项 | 卡在哪 | 现状 |
|---|---|---|
| **场次级「直播内容分析」服务** | 要先定分析口径（独立产品级） | 接口字段早已预留（`analysis`），详情弹窗显示"接口已预留"。devlog/035 |
| **原始弹幕明细库 / 全量分析** | 要不要做、以什么粒度落库（这是独立产品级决策） | **通道已实测可用且已被真实使用**：`/api/v3/lives/{liveId}/danmakus` 公开免鉴权 + `offset/limit` 分页（max 100000，含弹幕原文/礼物/上舰/SC）；词云自建（devlog/061）就走这条路，只差"落表 + 前端下钻"。devlog/060 §一.2 |
| **弹幕词云词字下钻**（点某词看该词在场的弹幕） | 依赖上一条的明细存储 | 现在是纯前端破泡交互（点击即消失 + 恢复），没有明细可查 |
| **帖子互动历史曲线** | 要加 post 统计快照表（现在 `stats_json` 原地覆盖，热帖传播过程会丢） | 数据在丢，越早做保留越多；见 §3 |
| **小窗形态梯度的档位与尺寸** | 要定"有几种形态、每档多大" | 见 §1.4 的 W2。**两形态（胶囊 ↔ 面板）已经能用了**（R38 批 5d 补齐 resize 通路，devlog/183），但"灵动岛式多形态"要更多档；档位一旦定了，`widgetExpandGeom` 那套几何要扩成多档 |
| **自建词云首拉耗时**（2026-09-25 从 §1.1 移来） | 要定"**要不要牺牲完整度**换响应" | 实测 5 万条记录 ~12s 属正常，上游瞬时变慢时单场可达 **120s**（devlog/062 §四）。**当前刻意不加时间预算** —— 截断靠前记录会系统性丢掉下播前的高频词（如「晚安」）。三条可选路：分页调优 / 前端进度反馈 / 标注"基于部分弹幕"后截断（**只有第三条要动产品口径**） |
| **自建词云支持取消**（2026-09-25 从 §1.1 移来） | 优先级最低，可不做 | 上游取数已可取消（devlog/064）；`/wordcloud` 走同一套（`api.buildLiveSessionWordCloud` 加 `signal` + 调用处 abort）。只在用户点按钮时发、120s 是可接受上限 |
| **升级/迁移失败的用户可见分类**（2026-09-25 记，devlog/198 明确不做） | 要先定"**备份策略**"口径（备份放哪、保留几份、占多少盘） | 现状：schema 迁移失败时后端起不来，用户只看到「内置后端服务未能在时限内就绪……请关闭应用后重新打开」+「请勿删除数据目录」——**他不知道这是"数据没坏、只是迁移没过、可以找回"**。S3-C 本批只修了"删旧目录"那条不可逆路径，**这一条刻意留出**（属母计划批次 16「升级与迁移安全 + 诊断包」）。判据应当是"用户能自己找回数据"，不是"日志里有异常" |
| **`perf_report.py` 的深休眠段怎么量**（2026-09-26 记，devlog/203） | 要先定"**壳要不要允许外部注入会话 token**" | S1 起 token 由壳每次启动生成、只存内存 ⇒ 壳外的 `perf_report.py` 写不了 `close_action=tray`（401），深休眠那一段量不了。已改成**显式跳过**（不再打假数字）。三条可选路：① 壳支持"env 预置 token"（仅在 dev/测试形态，且要评估对不变量 26 的影响）· ② 脚本改用直接写库（绕过校验，耦合 schema）· ③ 这一段改成真机手动量（记进 §1.3） |

### 1.3 用户侧动作（不需要改代码，但只有你能做）

- **升级/迁移失败的两个真机现场**（批次 16 的验收要求，2026-09-26 记，devlog/207）：
  这两条 `ui_probe` 与 CI 都碰不到（要真库坏掉 / 真迁移中途失败），命令与判据写在
  `docs/DEV-LOOP.md` §一 的「真机现场：升级失败 / 迁移失败」那一小节。期望：
  ① 迁移失败 ⇒ 应用**照常起来** + 横幅说"上次升级没有完成（数据没有丢）" +
  `vtuber.db.failed-<时间戳>` 与 `backups/` 都在位 + `/healthz` 的 `migration.status == 'failed'`；
  ② 迁移中途失败 ⇒ 提示失败原因、数据目录来源没变、旧目录内容与时间戳没变。
  ⚠️ **别拿真档案试**：先把数据目录复制一份，用 `DDTOOLKIT_DATA_DIR` 指到副本上玩。
- **桌面状态控件真机复验**（R38 批 5b；**2026-09-24 首次装机发现阻塞级问题，已修**，devlog/175）：
  首装时「开小窗 → 主窗口点 ✕/最小化没反应、托盘退不掉、小窗只有一块透明方框」。
  根因是 **Tauri ACL 作用域只写了 `"main"`**（小窗一条窗口权限都没有）+ builder 上的外观被延迟应用。
  已修（作用域放到所有窗口 / `build()` 后同步再钉一遍外观 / 关闭改走**不走 ACL** 的自定义命令）。
  **`ui_probe` 验不到真窗口**（它跑在无头 Edge 里，没有 Tauri）⇒ 请按这个顺序复验：
  ① 开开关 → 小窗是不是 **200×40 深色圆角胶囊**（不是方框）；② 拖它 → 能拖走 + 重启后位置还在；
  ③ 主窗口 ✕ / 最小化 → **正常**；④ 关开关 → 小窗消失、再开能回来；
  ⑤ 托盘退出 → 任务管理器里 `ddtoolkit.exe` 与后端**都没了**。出问题看数据目录的 `shell.log`
- **小窗形态梯度 + 两项窗口开关真机复验**（R38 批 5d，devlog/183）——
  **这三条 `ui_probe` 原理上验不到**，必须真机：
  ① **悬停/点胶囊 → 窗口跟着面板长大**（面板在胶囊**下方**；若小窗在屏幕下沿则**翻到上方**、
  胶囊落到窗口底部），收起后**回到原位置**（反复悬停 10 次不该漂走）；
  ② **鼠标穿透**（设置 → 外观 → 小窗鼠标穿透）：开启后点小窗位置**能点到下面的窗口**，
  且**小窗自己点不动了**（预期行为）；关掉后恢复正常；
  ③ **全屏隐藏**：开一个全屏视频/游戏 → 小窗自动消失，退出全屏 → 自动回来
  （注意：它**只 show/hide，不销毁**，所以回来应该是瞬间的）。
  ⚠️ 若面板在小窗里**看不见或高度不对**，看 `shell.log` 里那两行
  `[widget] 展开 → {w,h,x,y,flipUp}` / `[widget] 收起 → …` —— 几何算错会直接体现在这里。
- **P8-B 交互手感实机验证**：平台徽章长按拖动阈值、hover 尾部「+」、档案设置弹窗高度、系统浏览器打开是否顺手（devlog/048）
- **P10-A 视觉手感**：双月历配色与日期格密度、窄窗表现（devlog/050）
- **发版**：**v1.0.1 已发布**（2026-09-15，覆盖 devlog 082–084，[Release](https://github.com/Mrslippe/DDToolkit/releases/tag/v1.0.1)，
  release id 389029336，安装包 56.3MB / 便携 70.3MB）；装机验证（直装版 `_internal` + 首启、便携版解压启动）
  与 PAT 吊销**用户已确认完成**（2026-09-15）。
  **v1.0.2**（覆盖 devlog 085–118）：tag `v1.0.2` = **`36a7ba1`** 已在远端（发布前把 R20–R24 的实测问题都修完
  才定版，期间按用户口径**移动过三次**）、`main` 已对齐、产物按该提交重建并校验
  （setup **57.5MB** / 便携 **71.9MB** / 更新清单签名 420 字符 / FileVersion 1.0.2）；
  **GitHub Release 待建** —— 有 token 时一条命令：
  `python scripts/release.py 1.0.2 --from release`（资产在 `dist-release/`，幂等；
  续跑是否保留 release 由**远端 tag 是否存在**判定，见 devlog/120）。
  发布方式已收敛为一条命令：**`python scripts/release.py <版本>`**（devlog/084，手册 `docs/RELEASE.md`）——
  脚本做版本同步/门禁/打版/产物校验/提交/tag/推送/Release，**只有两件事仍需你亲自做**：
  装一次直装版确认 `binaries\backend\_internal\` 存在且首启能过（v1.0.2 起**顺带验托盘**：✕ 隐藏 → 托盘退出能真退出）、
  便携版解压可启动；以及 token 进过对话就吊销。
- **R9 的展示形态**：等「账号信息历史快照」那条线开工时一起定（曾用名/曾用签名放哪、怎么呈现）

> ✅ 已完成（2026-09-13 复核后从本清单移出）：**P9-A 历史归并脚本已在真实库跑完** ——
> 只读复核 `video_dynamic` 49 条中「与 video 同 bvid = **0**」，`posts.note` 附言 31 条，
> 说明归并已生效（原始 323 条已合并，详见 `docs/ROADMAP-DONE.md` 的 P9-A 小节）。

### 1.4 小窗演进（R38 批 5b 之后；2026-09-24 用户描述的四条需求）

> ⚠️ **2026-09-26 真机复验**：用户报的小窗问题里，**全屏隐藏失效**已修（S3-0 的命令准入把
> `is_fullscreen_app_running` 误填成"只有主窗口"，devlog/208 §六）—— 用户确认"其他问题算是解决了"。
> **仍留着一条：拖动手感**（用户原话：「按住后鼠标需要移动一小段距离后小窗才会跟着移动」）。
> 位置与原因都已定位：`StatusWidgetWindow.tsx:377-399` 的 `onPointerMove` 里有一个**刻意的
> `DRAG_THRESHOLD_PX` 阈值** —— 同一个胶囊既要"点一下展开"又要"拖走"，所以先等位移超过阈值才调
> `startDragging()`。⇒ 这不是权限/IPC 问题，是**交互口径**问题：重构时三选一（① 阈值调小或改按
> 速度判定；② `pointerdown` 就 startDragging、点击用"未移动"判定；③ 给一个明确的拖动手柄区）。

> **用户原话（2026-09-24）**：「这个小窗会作为类似 iPhone 灵动岛的消息聚合中心，可能是定时抓取任务、
> 当前正在播放的音乐、空闲时的小提示等等」「需要和灵动岛一样能够有尽可能丝滑的动画和多种形态」
> 「主题要可以更改」「位置可以自定义，但最好给出可吸附的几个标准位置，例如屏幕顶部居中」。

**核实过的现状（2026-09-24，逐条查过代码，不是印象）**：

| 事实 | 位置 | 后果 |
|---|---|---|
| 隐藏到托盘时**轮询全部停摆** | `TopBar.tsx` L177 / L186 / L395 都读 `isShellHidden()` 提前 return | 主窗口一收进托盘，**小窗就没数据可显示** |
| 隐藏满 10 分钟**深休眠 `destroy()` 掉 WebView** | `deep_sleep_impl` + `deep_sleep_after()` = 600s | **发出数据的那个 renderer 根本不存在了** |
| `usePrefs` **只在 `AppSettingsDialog` 里被调用**；主题靠 `applyTheme(documentElement)` 写 `data-theme` | `usePrefs.ts` / `theme.ts` | **小窗完全不知道主题**（两个 renderer 不共享 JS 上下文）|
| widget 宿主的颜色是**写死**的 `rgba(20,20,26,.94)` | `layout.css` `[data-density='widget']` | 需求"主题可改"一来，这些硬编码就得变成令牌 |
| ~~窗口尺寸在 Rust 侧**写死** `W=200.0 H=40.0`~~ | ✅ **R38 批 5d 已补**（devlog/183）：`resize_widget_window` + `utils/widgetWindow.ts` 的几何纯函数 | 展开/收起已经能用了；W2 要把它从"两档"扩成"声明式多档" |

**三层改动，按依赖排序**（详细论证见 devlog/180 之后的对话）：

| # | 层 | 内容 | 依赖 | 规模 |
|---|---|---|---|---|
| **W1** | **主题广播** | 主窗口 `applyTheme` → `emit('shell:theme')` → 小窗 `listen` → 同款 `applyTheme`。顺手把 widget 宿主那几个**写死的颜色变成令牌** | 无 | 小（半天）|
| **W2** | **窗口管理器** | 抽声明式 `WindowSpec`（label / url / size / material / lifecycle / anchor），统一负责**单例保证**（现在第六轮手写的 `CREATING_WIDGET` 应是它的职责）· **位置钳制 + 多显示器** · **`resize()` 通路**（给 W2 之后的形态用）· **标准吸附位**（顶部居中 / 右下 / 左上…）| W1 | 中（1–2 天）|
| **W3** | **数据源搬进 Rust** | 新建常驻 `WidgetService`：持有通知列表 + 优先级/ttl/分组状态机（现在 `notificationHub.ts` 那套要**搬到 Rust 或让 Rust 成为真相源**）。主窗口/小窗都退化成**纯订阅者** —— 谁在谁显示、都不在也照常采集 | W2 | **大（3–5 天）** |

⚠️ **W3 才是"消息聚合中心"的地基**：不做它，主窗口一关小窗就是块废玻璃（上表前两行）。
W1/W2 可以在**现在的架构上**做完，但它们只是让 W3 少踩坑。

**可参考的成熟方案**（用户问过"有没有成熟框架"）：

| 参考 | 学什么 |
|---|---|
| **`tauri-plugin-window-state`** | 位置/尺寸持久化 + **多显示器钳制**。⚠️ 现在 `utils/widgetWindow.ts` 那一整个手写模块（localStorage + 解析 + 钳制 + 16 条单测）**基本就是它的手写版**，换掉能删一个模块 |
| **Electron `BrowserWindow` + `windowStateKeeper`** | "单例窗口 + 位置钳制 + 多显示器兜底"的完整做法（最值得照抄）|
| **VS Code 多窗口模型** | 主窗口当 hub、辅助窗**纯渲染**；以及"辅助窗不跑主进程逻辑"的边界纪律 |
| **Raycast / uTools 浮动面板** | 无边框 + 置顶 + 拖动 + **焦点策略**（什么时候抢焦点、什么时候不抢）|

### 1.5 已搁置（有计划恢复，但现在不做）

**P3 JSONL 归档包导出** ⏸（2026-09-05 用户定：P4 数据源先行）

> 搁置理由：导出格式依赖表结构稳定；P4 会新增表/列，等数据源落地后再做格式定稿。
> **格式设计仍有效**（schema_version + vtuber/account/posts 三段、raw_json 保真层、
> P2P 构想的 Phase 0 地基），恢复时按原计划执行。
> **阻塞已解除**：P4+ 的表结构变动已收敛（迁移 head = `f007`），用户点头即可重启。

- 按 V 或全库导出：`schema_version` + vtuber/account/posts 三段，posts 保留 `raw_json` 保真层
- 独立价值：备份、迁移、跨设备；同时是未来 P2P 构想的 Phase 0 格式地基
- CLI 脚本放 `scripts/export_archive.py`，暂不做前端入口

**后端内存与包体优化** ⏸（2026-09-16 用户定：**只记账不删**，先把实测与候选记下来）

> 实测基线（devlog/121；复测方法见 `docs/ARCHITECTURE.md` §3.12）：打包版后端空闲 **128.7MB**
> （任务管理器口径），其中**业务代码只占 ~8MB**，其余是解释器 + 框架地板；打包比 dev 多 ~19MB。
> `_internal/`（100.6MB）里还有 numpy 25.9MB + PIL 12.7MB **在盘不在内存**（`app/` 无人 import）、
> pywin32 1.1MB 是**历史构建残留**（依赖图里根本没有）。三个候选都量过收益，当前都不做：

- **A. 词云词典空闲卸载**：**−55MB**（唯一有实感的项）—— 只在此前真开过一次词云时才有感，
  代价是再次打开词云首屏多等 0.7s；要改 `JiebaTokenizer` 生命周期 + 补用例
- **B. 打包瘦身**（排除 numpy/PIL）：**−38.6MB 磁盘**、内存 0 变化 —— 必须在**打包版**上
  端到端验扫码登录（冻结版才暴露的坑，见 devlog/036 的打平事故）
- **C. Alembic 懒加载**（`alembic_version` 已是最新就不 import）：几 MB，收益最小且迁移路径变复杂
- **不做 D：删掉自建词云与 jieba** —— 上游 `extra.wordCloud` 2026-09-16 实测已恢复（6/6 场次
  各 40 词条），但它在 09-13 断供过一次（devlog/060）才有自建；且 jieba 已懒加载、空闲不占内存，
  留着当断供应急

**数据目录指针的两处收口** ⏸（2026-09-16 用户定：先记下，等 1.0.3 做 —— 现在动要重打桌面壳产物）

> 起因：用户装完构建产物发现**安装版与 dev 用的是同一个库**。根因不是环境变量（三处都是空），
> 而是**指针文件是用户级固定路径 `%APPDATA%\DDToolkit\data-dir.txt`、不带构建标识**
> （`frontend/src-tauri/src/datadir.rs` §`pointer_path`）⇒ 两个构建**共用同一份迁移记录**：
> 在 dev 里迁到 `E:\test\DDToolkit-data`，安装版读到同一条记录就跟了过去。用户删掉那个目录后，
> 两侧各自回退自己的默认目录（`com.ddtoolkit.app` / `com.ddtoolkit.app-dev`），数据都在、没丢。
> 对**普通用户**无影响（只装一份安装版，不会有第二个程序改它的迁移记录）。

- **A. 指针按构建分家**：dev 用 `data-dir-dev.txt`（release 保持 `data-dir.txt`），
  dev 的迁移不再连带改安装版；改动小（`pointer_path()` 一处 + `cargo test` + 文档）
- **B. 加「清除迁移记录（回到默认目录）」入口**：命令清单里现在只有
  `migrate_data_dir` / `delete_old_data_dir`，**没有 clear** ⇒ 目标目录被删/移动（外接盘没插、
  格式化、误删）之后，「迁移记录不可用，已回退默认目录」那行红字会**一直挂着**，
  用户只能自己去 `%APPDATA%` 手删文件。建议放在「设置 → 关于」存储占用面板那行红字旁边
- 附带记一笔：**卸载时 NSIS 不会删这个指针**（它不是安装清单里的文件）⇒ 卸载后重装会沿用旧记录；
  通常正是用户想要的（数据还在），但属于"卸载没清干净"

---

## 2. ✅ 能力现状（已上线，只在判断新需求可行性时查）

> 2026-09-13 更新（原为 2026-09-10 二次审计）。**这一节不需要动作。**
> 最右列写的是"这条还差什么"，差的部分都在 §1。

| 能力 | 状态 |
|------|------|
| 关键词搜索（title/summary/正文 三路 LIKE） | ✅ 已上线（v0.5.2，FTS5 暂缓） |
| 发布时间范围筛选 | ✅ 已上线（**v0.9.9 换成双月历区间选择器**，P10-A，devlog/050） |
| list 筛选行收纳（状态 / 归档三态 / 时间 → 单钮弹窗） | ✅ 已上线（v0.9.9，`.post-filter-pop` 三分区 + `DateRangePicker`，devlog/050） |
| 类型筛选 / 服务端分页 / 「已归档 N」chip | ✅ 已上线 |
| 候选池收录 / 解订阅连带清帖 / 批量任务 | ✅ 已上线（v0.5.0；解订阅外键回滚已修，devlog/040） |
| 抓取任务优先级（手动 > 定时档） | ✅ 已上线（devlog/040） |
| 抓取调度合并为综合档（24h 数据驱动 + **按平台名单** + 外部批次排队） | ✅ 已上线（devlog/042；预算自适应 054；名单/并发与周期下限 070→078：**全部账号按平台分组，名单间并行 / 名单内串行**，一轮 ≈40~44s、周期 60~70s） |
| 收录 / 添加账号后立即抓取 | ✅ 已上线（devlog/041/044：账号信息可见 6.0s → 0.9s） |
| 外部任务进度胶囊 + 完成后卡片自动刷新 | ✅ 已上线（devlog/044） |
| 顶栏展示策略（自动节拍静默） | ✅ 已上线（devlog/051；状态行视觉定稿 devlog/043） |
| 微博 / B 站增量漏帖（置顶帖打乱流序） | ✅ 已修（devlog/045） |
| 微博扫码登录 / B 站扫码登录 | ✅ 已上线（v0.5.0） |
| 账号统计快照历史（粉丝趋势） | ✅ 采集 + 可视化（**v0.9.x 重写为 ECharts canvas**，recharts 已退役） |
| 档案设置弹窗（背景 / 头像 / 签名 / 已订阅账号，全实时生效） | ✅ 已上线（devlog/066–080）：签名走**来源+覆盖**模型（A3，devlog/074）——平台签名只读、卡片签名 = 覆盖 → 来源账号 → 主账号 → 空；**字段锁定已退役**，旧值由 `vtuber_field_history` 记账（**仅在平台侧覆盖前**，手改不入账）。下拉栏为「内容体内绝对定位浮层」（devlog/075）。**账号信息历史**（曾用值 + 抓取快照）在账号行的历史钮里（R9，devlog/080） |
| 删除检测（墓碑） | ✅ 已上线（v0.5.1：两击判定 + 已删筛选/角标/时间线） |
| 外部固定化数据源 | ✅ 已上线（zeroroku 粉丝历史/礼物日 + danmakus 索引/直播场次/弹幕） |
| 直播日历 + 场次内容管道（danmakus+self 合并、9 类类型 v2、校正 override） | ✅ 已上线（v0.9.0–v0.9.x M1–M4；对外 id 定权见 devlog/052） |
| 场次详情弹窗（信息 / 分类校正 / 弹幕词云 / 直播动态 / 分析预留） | ✅ 已上线；**上游取数已拆成独立请求 + 10 分钟缓存**（devlog/063）；分析服务未接入 → §1.2 |
| 弹幕词云（上游 top40 + 本地自建兜底 + 五态文案） | ✅ 已上线（devlog/061/062）；**原始弹幕明细库暂缓** → §1.2 |
| 滚动条标准（不占宽 + 自动隐藏 + OverlayScroll 统一） | ✅ 定案并落地（UI-MAP F 节） |
| 桌面端图标清晰度（任务栏/资源管理器） | ✅ 已修（devlog/043） |
| 验证基建（pytest / vitest / eslint / ui_probe / 位级签名） | ✅ 已上线（devlog/056/059 + 062–066；基线见 §6.2） |
| 数据导出 / 可移植格式 | ❌ 无 → §1.5（P3） |

---

## 3. 📦 Backlog（远期，按价值排序）

| 方向 | 说明 | 前置 |
|------|------|------|
| 帖子互动历史曲线 | 同 §1.2：`stats_json` 原地覆盖，热帖传播过程会丢；要保留须加 post 统计快照 | 无（越早越不丢数据） |
| 弹幕分析（全量） | 场次级词云已接入；原始弹幕明细库与全量分析（实质是独立产品）暂缓 | 明细存储（§1.2） |
| 涨粉趋势可视化 | ✅ 已上线（v0.6.0 起，v0.9.x 重写 ECharts）；多观察者数据合并后会更稠密 | — |
| P2P 分布式交换 | Phase 1 文件交换（依赖 P3 格式 + 合并语义）→ Phase 2 目录服务 → Phase 3 DHT；需签名 provenance 与隐私 opt-in（贡献即暴露关注列表）；Phase 2+ 可为二创精选提供涌现质量信号 | P3（§1.4） |
| 关注流二创精选（让作品被看见） | 匹配失败而非曝光不足；二创自带收件人地址（关于哪个 V，受众即该 V 的粉）。详见 §5 | stats 历史（P0 已积累） |
| ~~**账号多时账号行横向溢出被裁**（R45-E 发现）~~ ✅ **已修（2026-09-25，R49 批 4）** | 用户拍板：**换行**（不是横向滚动）。`.account-switch` 加 `flex-wrap: wrap` + `row-gap: 6px` —— 旧顾虑"行高变高会让筛选弹窗算错"**已不成立**（R45-E 把弹窗高度改成实测 + ResizeObserver，能自动跟上）。<br>**判据补上了那个盲区**：原先 `overflowing` 那条**主动放行"被祖先裁掉"的**（`clippedByAncestor`，为折叠组那类有意为之留的口子），于是这个真 bug **全部判据全绿**、只能靠用户截图发现。新增 `clippedOverflow`：**账号行的子元素不许越过本行矩形**。<br>⚠️ 没有去放宽 `overflowing`（那会把折叠组那类有意的裁掉全部报红）—— **把判据写具体，而不是把判据放宽**。<br>**反向验证**：改回 `nowrap` ⇒ **7 处红**，精确点名"5 枚药丸越过本行矩形（行高 30px）"+ 坐标 | 无 |
| ~~**`glow-bar` / `glow-spot` / `--glow-spot` 命名债**（R45 遗留）~~ ✅ **已还（2026-09-25）** | 改成了 `.view-switch` / `.view-switch-thumb` / `--view-thumb-size`，**并把探针那个测量桶 `glow` → `layout`**（它装的其实是 heroTop/deckPadTop/panelRect/toolbarGap，跟"光"更没关系）；`glowRef` → `switchRef`。覆盖 `PostsPage.tsx` + `posts.css` + `tokens.css` + `probe.ts` + `toolbarZone.ts` + `ui_probe.py` + 5 份文档。<br>**护栏（纯改名的证明）**：改名前后各跑一次探针三档，输出**逐行完全一致**（`问题=0`），另有 `tsc` 0。<br>⚠️ devlog 里的旧名是历史记录，**别改回去** | 无 |
| **工具条"向下滚动立即隐藏"**（R45 遗留） | 不是正确性所需（指针离开 900ms 后自然会收），但它能让"开始读内容就让位"更干脆。需要给 `OverlayScroll` 加**方向**信号（现在只有 `data-scrolled` 布尔），那是共享组件、影响四个视图 ⇒ 单独一批 | `OverlayScroll` 加 `data-scroll-dir` |
| **工具条"向下滚动立即隐藏"**（R45 遗留） | 不是正确性所需（指针离开 900ms 后自然会收），但它能让"开始读内容就让位"更干脆。需要给 `OverlayScroll` 加**方向**信号（现在只有 `data-scrolled` 布尔），那是共享组件、影响四个视图 ⇒ 单独一批 | `OverlayScroll` 加 `data-scroll-dir` |
| ~~**profile 视图没有页面标题**（R45-B 遗留）~~ **不做（2026-09-25 用户决定）** | 用户口径：「**空着，之后档案视图要全部重构**」—— 补一个马上要重做的占位物没有意义。✅ **遮挡问题 R45-E 已经修掉**：让开量收归 `.view-body::before` 一处后，四个视图首块内容在同一条线（实测 103 = 52+51），profile 首行**不再被工具条盖住**。⇒ **本条关闭**；档案视图重构时再一起定它顶上放什么 | 无（等重构） |
| ~~**`.float-pill.on` 白字对比 2.54:1**（R45-A 遗留）~~ ✅ **已修（2026-09-25，R49 批 3）** | ⚠️ **真实范围比本条原写的大得多**：不是 4 处，而是 **10 个选择器**共用 `--c-primary-deep` 当底 + 白字（`.filter-chip.on` / `.acc-switch-btn.on` / `.type-chip.active` / `.drp-preset.on` / `.lc-dlg-tab.on` / `.board-btn.on` / `.vd-sign-opt.on` / `.lc-month-pop-btn.on` / `.fan-preset.on` / 能力窗「去登录」），全部 2.54:1（**小字 4.5、图标 3，两边都不达标**）。<br>**修法**：令牌 `#fb77a1` → **`#c9406f`**（白字 4.72:1），一处改好全部；逐个改 CSS 会留下"同一个激活态两种粉"。<br>**护栏**：`ui_probe.py` `_assert_layout` ③b（扫那族里**有文字的**逐个算，<18.66px 按 4.5:1）；**反向验证**：令牌调回旧值 ⇒ 三档全红、读数 2.54:1、一次点名多个元件。<br>⚠️ 原条把 3 处 `.filter-chip` 误当成 `.float-pill.on`（它们不是同一个类）—— 这也是"范围被记小了"的一部分 | 无 |
| **探针两个模式待复跑**（R45-A/B） | ✅ **已复跑（2026-09-24）** —— 用户开了完全权限，`esbuild spawn EPERM` 消失（那确实是沙箱窄权限导致的）。首跑 **34 处红**，逐条查根因**没有一条是产品代码错**：`calc()` 不在 `:root` 求值（1 处）/ 探针没杀 `.view-btn` 的过渡（尺子读在半路，2 处）/ **坐标系混用**（视口 vs 面板内，18 处）/ 判据写成 `tag == "list"` 而 list 有 6 个扩展帧（12 处）/ 以及**一个真 bug**：`_seed_accounts` 漏了 `live_status` ⇒ `VTuberOut` 校验失败 ⇒ 后端 500 ⇒ 前端 `Failed to fetch` ⇒ 所有视图级判据一起报"缺失"。详见 devlog/182 §4 |
| **R45 的视觉评审产物** | ✅ **已补**：`--shot-toolbar` 截卡片页/列表页。**判据全绿之后，截图仍发现两个问题**：① 卡片页头像顶部被工具条切掉（探针**从来没量过 hero 几何** ⇒ 没判据）② `+2` 溢出钮白底矮方块、像错位。两条都已修（`.hero` padding 23→38 + 新判据 `heroTop ≥ --toolbar-band`；`.pill-more` 改用 `--sel-bg` + 同一条阴影）。**元教训：判据拦"结构错"、拦不住"看着不对"，而 R45 三批改的全是"看着对不对"的东西** |

---

## 4. 🚫 明确不做（近期）

- **IconRail 占位入口**（日历/邮件视图）：核心价值兑现前不加新面板
  —— 2026-09-08 已删除四枚占位（用户/日历/刷新/设置）与右栏光条邮件占位钮，只保留已接线的「帖子浏览」与四个视图钮
- **多用户 / 云同步**：坚持本地优先定位
- **截图式"证据留存"（页面快照/PDF 存档）**：与现有"结构化字段 + 原始 `raw_json`"路线重复，
  且体积与时序噪声大；若要"证明力"，优先补 §1.2 的原始弹幕明细这类**结构化原始数据**

---

## 5. 🌱 远期构想：让作品被看见（二创发现）

动机：一件优秀作品没被最可能喜欢它的人看见是残忍的。病灶是匹配失败，
不是曝光不足——不需要社区平台，需要的是路由能力。

核心洞察：二创天然自带收件人地址——作品关于哪个 V，受众就是喜欢那个 V 的
人。订阅关系、faction 字段、联合投稿多 V 存储就是现成的路由表。

定位约束：做成 DDtoolkit 内的功能，不做平台。无服务器、无账号、无投票货币。

分阶段形态：

1. **编辑精选周刊**——手工挑选，静态推送，零基建验证需求是否真实存在
2. **统计遗珠信号**——小创作者异常互动率（硬币/收藏对播放比例失衡）自动浮现，
   依赖粉丝基数与帖子统计历史的积累（→ §3 帖子互动历史曲线是前置）
3. **归档涌现信号**——接入 P2P 归档网络后，「多少节点愿意为它花磁盘空间」
   即最诚实的质量票（与 §3 的 P2P 条目合流）

明确不采用：虚拟钱币投票 + 推送权重正反馈（赢家通吃反噬长尾发现；
中心服务器复刻热情退去问题；国内 UGC 合规成本单人不可承担）。

---

## 6. 📊 基线（当前门禁；历史索引在 `docs/ROADMAP-DONE.md`）

> 本节只回答"现在的门禁跑出来是多少"。**已完成条目、已落地需求清单、版本 → devlog 索引
> 都在 `docs/ROADMAP-DONE.md`**，本文件不再重复（2026-09-13 二次整理）。

### 6.1 版本 / 批次 → devlog

> 索引已移入 **`docs/ROADMAP-DONE.md` → 「批次 → devlog 索引」**（2026-09-13 整理：
> 本文件只留"要干什么"与当前基线，历史索引与已完成条目同处一份文件更好查）。

### 6.2 当前门禁基线（2026-09-26 实测 / 复核）

> 只放**只能人跑**的实测值：一行一值 + 日期。**能派生的量指向真源，别抄** ——
> 迁移 head / 表数 / 路由装饰器 / 下一篇 devlog 编号 / 静态用例条数 → `python scripts/gen_doc_numbers.py --list`；
> `.py` 文件数由 `scripts/dev_check.py` 自己打印；词云 sha256 由 `node scripts/check_wordcloud_layout.mjs` 自算；
> 探针各模式 → `python scripts/ui_probe.py --help`。**变更史不在这里**（那是 devlog 的体裁：基线怎么涨的看各批 devlog）。

| 门禁 | 命令 | 当前基线（括号里 = 该值实测日） |
|---|---|---|
| 后端 | `python -m pytest -q`（**解释器走 `.venv`**，见 `ARCHITECTURE.md` §6 第 24 条） | **710–711 passed / 0 failed**（总数 711：其中 1 条**打真上游**的用例在不可达时按设计 skip ⇒ `passed` 那一格会差 1；2026-09-26 实测 ≈50–100s） |
| 桌面壳 | `cargo test`（工作目录 `frontend/src-tauri`） | **56 passed**（2026-09-26 实测；含 `delete_old_dir` 的真实 junction 用例、S1 的 token 生成用例与 S3 的准入表/白名单用例） |
| 前端单测 | `npm --prefix frontend run test` | **592–594 passed**（2026-09-26 实测；文件数不抄，跑一次就有） |
| 前端类型 / lint | `npx tsc --noEmit`（**必须在 `frontend/` 里跑**）/ `npm --prefix frontend run lint` | 0 错 / 0 错（2026-09-26 复核） |
| 文档漂移 | `python scripts/doc_check.py` | **0 FAIL**（2026-09-26 复核；另有 1 条历史 devlog 索引欠账 WARN，WARN 看脚本逐条输出） |
| 上游冒烟 | `python scripts/smoke_upstream.py [--cold]` | 真上游 **5 ok** / 冷进程 **3 ok**，0 FAIL（2026-09-23 复核） |
| 未登录能力矩阵 | `python scripts/capability_matrix.py --write` | 两态逐接口实测（结论 = `docs/ARCHITECTURE.md` §3.9；**`--include-content` 触发 IP 级 412，别顺手跑**） |
| 布局探针 | `python scripts/ui_probe.py --seed-accounts 8` | 三档 1100/1280/1440 × 10 视图全过（2026-09-26 复核）。⚠️ `8` 是常规参数：开发库只有 2 个账号，不种就是空转的门禁 |
| 档位门禁 | `python scripts/gate.py` | A 档 **8 步**全过 / **98–126s**（2026-09-26 实测，含 `cargo test`、三档探针与**全仓语法扫描**；~250s 那版是 pytest 提速前 —— 见 devlog/200）；C 档 ≈46s |
| CI | GitHub Actions（`.github/workflows/`，真源在那里） | 两条腿：Linux（后端 ×2 个 Python + 前端）与 Windows（Rust + 冻结后端冒烟）。**2026-09-25 已首次跑绿**（`e3499f1`）——首跑到闭环共四次红，根因见 devlog/200（其中一条是**英文 Windows 用户首启即崩**的真 bug）。此后每个推送两条腿都跑：S1（`dd1613e`）与 S1b（`eafdd73`）均全绿（2026-09-26） |
| 冻结产物体积 | `python scripts/build_backend.py` 的输出 | **71.7 MB**（2026-09-25；切 `uv.lock` 前记录 118.8MB）。出包后以 `release.py --only verify` 为准 |
| 一把梭 | `python scripts/dev_check.py` | syntax / pytest / frontend logic / docs drift / dev backend 五项全 ok（2026-09-23 全量实跑） |

> ⚠️ 探针签名（`--hero-expect` / `--calendar-expect`）**含实时数据**，只适合"改动前后短窗口对比" ——
> 见 `docs/DEV-LOOP.md` §二·五，此处不复述。
