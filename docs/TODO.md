# DDtoolkit 开发路线图（TODO）

> 定位一句话：**个人 VTuber 证据档案库**——定时记录、永不丢失、找得回来。
> 核心闭环（订阅 → 自动抓取 → 浏览 → 归档）已完整；当前主线是补全"档案完整性"，
> 让存下来的数据可检索、可证明、可带走。
>
> **这份文件怎么用（三条）**
> 1. **想到新需求 → 写进 §0**，一句话就行，不必先想清楚怎么做；
> 2. **要干活 → 看 §1**（唯一"待办"清单，分四组：能立刻动手 / 要先拍板 / 用户侧动作 / 已搁置）；
> 3. **想查过去怎么定的 → 看 `docs/ROADMAP-DONE.md`**（已完成条目的原始需求与验收），
>    版本 → devlog 索引见 §6。
>
> 整理记录：2026-09-13（把历史条目拆到 `docs/ROADMAP-DONE.md`，本文件只留"要干什么"）。

---

## 0. 📥 待提需求（收集区 —— 新需求写这里）

> **用法**：直接在表里加一行。**只写你想要的**（现象/期望效果），不用写实现方案；
> 优先级拿不准就留空，我会在评估后回填。
>
> **状态取值**：`待评估`（刚写下，我还没看）→ `已受理`（已拆成可执行项并移进 §1）
> → `已落地`（在 §1 划掉并指向 devlog）｜`不做`（附一句理由，同时落到 §4）。

| # | 日期 | 一句话需求 | 期望效果（可选） | 优先级 | 状态 |
|---|---|---|---|---|---|
| R1 | 2026-09-13 | card视图的档案设置弹窗还是存在bug，头像设置的第一个选中后实际展示的是无头像时的默认图，并且签名锁定的逻辑好像反过来了 | 修复 | 低 | ✅ **已落地**（devlog/066）：头像改走 `ProxyImage` 三态链（图床无 Referer 直连实测 403 ⇒ 此前回落默认图）；头像与锁定**点击即写入**（库里 `locked_fields`/`vtubers.avatar` 实测全空 = 从未落库）+ 写入后回灌父级（否则重开按旧值播种，看着就像"反了"） |
| R2 | 2026-09-13 | 直播日历好像在定时动态抓取任务完成后会闪动，排查原因 | 修复 | 中 | ✅ **已落地**（devlog/066）：根因 = `loading` 一来就把 42 格整块换成加载态，而每轮 `fetch-idle`（自动动态流 ~80s）都会触发 reload → 网格消失重建（趋势卡同款，还白扔 ECharts 初始化）。改为**只在无数据时**显示加载态 + 有旧数据时刷新失败给红字小注。**可选第二步**（`fetch-idle` 带 kind、只在 external/account 后刷新）并入 R6 批 |
| R3 | 2026-09-13 | 直播日历和粉丝趋势的右上角标注出数据来源 | 例如“数据来自danmakus” | 低 | ✅ **已落地**（devlog/066）：两卡本就有 `.card-src-note` 槽位，改为**按实际数据来源**出文案（日历看场次 `source`、趋势看点的来源），tooltip 写清口径 |
| R4 | 2026-09-13 | 在一些自抓取的数据存在的情况下优先使用自抓取数据 | 例如粉丝趋势中的粉丝数记录，优先使用自己记录的快照因为第三方可能不完全 | 中 | ✅ **已落地**（devlog/066，按你选的 B）：合并口径抽成 `utils/fanTrend.mergeTrendDays`（**同日 self 优先、第三方只补空洞、逐日生效**）+ 8 条断言；图表拆两条线：self 实线 / 第三方回填虚线。⚠️ 改前行为**正好相反**（后写覆盖让第三方盖掉当天 self） |
| R5 | 2026-09-13 | list视图中搜索框右侧的筛选按钮文字不居中 | 修复 | 低 | ✅ **已落地**（devlog/066）：`.pfilter-btn` 为 caret 留的不对称内距（caret 其实是绝对定位、不占流）→ 改对称 `0 16px` |
| R6 | 2026-09-13 | 综合档中动态抓取分账号并行，并调整抓取频率 | 即先按平台分类取抓取任务名单，然后并行根据名单进行抓取，一轮抓取时间如果小于一分钟则休息至一分钟，如果大于则休息时间保证不触及上限 | 高 | ✅ **已落地**（devlog/070）：①`per_platform=DYNAMICS_CONCURRENCY`（默认 3，平台内并发，默认 1 = 旧行为，账号流不受影响）；②新增 `DYNAMICS_MIN_CYCLE_SECONDS=60`，到期改为「轮**开始** + 60s」；③平台内请求间隔上移为**平台级起跑闸门**（并发下才成立，风控面不变）。真机两轮：旧 30.2s/轮·周期 47~75s → 新 25.7s/轮·**周期稳定 60s**，请求数同为 7/轮、0 风控。调参口见 devlog/070 §五 |
| R7 | 2026-09-13 | card视图中的直播徽标可以点击直接拉起浏览器进入直播间 | hover有动画 | 低 | ✅ **已落地**（devlog/066）：`live_url` 优先、否则 B 站 `room_id` 拼；**未开播也可点**（你定）；hover 抬起+描边，`reduced-motion` 下只留描边 |
| R8 | 2026-09-13 | 粉丝趋势卡片中右侧的档位按钮根据设计令牌重构 |  | 低 | ✅ **已落地**（devlog/066）：描边改 `var(--c-border)`（原写死值与之同值）、容器补 `--sel-bg`、选中改 `--c-primary-deep`（本仓“激活=粉底白字”口径）；几何不动 |
| R9 | 2026-09-13 | 曾用名/曾用签名应归入「账号信息历史快照」，**先不展示**；且要的是"V **在平台上**曾经用过的"值，不是本地手改入库的字符串 | 档案设置窗口里那两处展示撤掉 | 中 | ⏳ **已受理（撤出已落地）**：devlog/075 删掉 `.vd-former`/`.vd-acc-former` 两处展示与 `getFormerValues` 调用；`PUT /account` **不再记账**（手改 ≠ 平台侧历史），抓取覆盖前照常记账、端点保留。展示入口待"账号信息历史快照"落地后重新设计（数据一直在记，`account_stat_snapshots` 目前**不含**昵称/签名） |

> **示例**（写完可删）：
> `| R0 | 2026-09-13 | 弹幕词云点某个词能看这个词在场的弹幕 | 想核对"某句话为什么火" | 中 | 待评估 |`

---

## 1. 🔧 未完成项（当前待办）

> 这是**唯一**"要干活"的清单。每条都写了性质与阻塞，避免"看起来能做其实做不了"。
> 已完成的条目不在本文件堆着 —— 见 `docs/ROADMAP-DONE.md`。

### 1.1 可以立刻动手（无外部阻塞）

| 项 | 性质 | 说明 / 下一步 |
|---|---|---|
| **词云自建扩展点接线**（可选增强） | 体验 | `count_tokens(extra_words=…)` / `JiebaTokenizer.add_words()` 已就绪但**没有自动灌词**：按 V 名/企划名自动灌自定义词典，避免"明前奶绿"被切碎（接线处留了 `ExtraWordsHook`）。devlog/061 §四 |
| **自建词云首拉耗时**（可选优化） | 体验，可接受 | 实测 5 万条记录 ~12s 属正常，上游瞬时变慢时单场可达 **120s**（devlog/062 §四）。**刻意不加时间预算**：截断取靠前记录会系统性丢掉下播前的高频词（如「晚安」）。可选改法：分页大小调优 / 前端进度反馈 / 明确标注"基于部分弹幕"后再截断 |
| **自建词云也支持取消**（照搬现成管道） | 体验小修 | 上游取数已可取消（devlog/064）；`/wordcloud` 走同一套（`api.buildLiveSessionWordCloud` 加 `signal` + 调用处 abort）。因它只在用户点按钮时发、120s 是可接受上限，优先级最低 |
| **`PostsPage` 场景切换机抽 hook**（行为级重构） | 技术债，无功能影响 | 视图已拆完（1134 → **859**，devlog/065），剩下的主体是"预取门控 + 原子提交 + `EXIT_MS` 退场"（文件内 166–325 行）。**护栏尝试失败**（devlog/071）：探针的 `--virtual-time-budget` 下切 V 会停在 `scene-exit` 不提交（路由与侧栏都切了，6s 虚拟时间没落地；同一套定时器在"切视图"路径是好的），分不清是探针环境还是真 bug ⇒ 先给探针加**真实时间模式**再动，顺带复核那是不是真 bug |
| **R2 可选第二步：`fetch-idle` 带 kind** | 体验优化 | 视觉闪动已修（devlog/066），这条只差省请求：让日历/趋势只在 `external`/`account` 类任务后刷新（动态流每轮不再触发无意义重取）。改法：TopBar 两处 `dispatchEvent` 换成 `CustomEvent` 带 `kind`，PostsPage 拆出 `dataTick` 传给两张卡 |

### 1.2 需要先定口径 / 拍板（不是写代码的问题）

| 项 | 卡在哪 | 现状 |
|---|---|---|
| **场次级「直播内容分析」服务** | 要先定分析口径（独立产品级） | 接口字段早已预留（`analysis`），详情弹窗显示"接口已预留"。devlog/035 |
| **原始弹幕明细库 / 全量分析** | 要不要做、以什么粒度落库（这是独立产品级决策） | **通道已实测可用且已被真实使用**：`/api/v3/lives/{liveId}/danmakus` 公开免鉴权 + `offset/limit` 分页（max 100000，含弹幕原文/礼物/上舰/SC）；词云自建（devlog/061）就走这条路，只差"落表 + 前端下钻"。devlog/060 §一.2 |
| **弹幕词云词字下钻**（点某词看该词在场的弹幕） | 依赖上一条的明细存储 | 现在是纯前端破泡交互（点击即消失 + 恢复），没有明细可查 |
| **帖子互动历史曲线** | 要加 post 统计快照表（现在 `stats_json` 原地覆盖，热帖传播过程会丢） | 数据在丢，越早做保留越多；见 §3 |

### 1.3 用户侧动作（不需要改代码，但只有你能做）

- **P8-B 交互手感实机验证**：平台徽章长按拖动阈值、hover 尾部「+」、档案设置弹窗高度、系统浏览器打开是否顺手（devlog/048）
- **P10-A 视觉手感**：双月历配色与日期格密度、窄窗表现（devlog/050）
- **P9-A 历史归并脚本在真实库执行**：`python scripts/merge_video_dynamics.py`（**先 `--dry-run`**；实测仍可归并 323 条）—— devlog/047
- **发版**：v0.9.9 已发布；**devlog/060–063 这几批尚未发版**（要不要编 v0.10 或直接补一个 v0.9.10，你定，见 `docs/RELEASE.md`）

### 1.4 已搁置（有计划恢复，但现在不做）

**P3 JSONL 归档包导出** ⏸（2026-09-05 用户定：P4 数据源先行）

> 搁置理由：导出格式依赖表结构稳定；P4 会新增表/列，等数据源落地后再做格式定稿。
> **格式设计仍有效**（schema_version + vtuber/account/posts 三段、raw_json 保真层、
> P2P 构想的 Phase 0 地基），恢复时按原计划执行。
> **阻塞已解除**：P4+ 的表结构变动已收敛（迁移 head = `f004`），用户点头即可重启。

- 按 V 或全库导出：`schema_version` + vtuber/account/posts 三段，posts 保留 `raw_json` 保真层
- 独立价值：备份、迁移、跨设备；同时是未来 P2P 构想的 Phase 0 格式地基
- CLI 脚本放 `scripts/export_archive.py`，暂不做前端入口

---

## 2. ✅ 能力现状（已上线，只在判断新需求可行性时查）

> 2026-09-13 更新（原为 2026-09-10 二次审计）。**这一节不需要动作。**
> 最右列写的是"这条还差什么"，差的部分都在 §1。

| 能力 | 状态 |
|------|------|
| 关键词搜索（title/summary/正文 三路 LIKE） | ✅ 已上线（v0.5.2，FTS5 暂缓） |
| 发布时间范围筛选 | ✅ 已上线（**v0.9.9 换成双月历区间选择器**，P10-A，devlog/050） |
| list 筛选行收纳（状态 / 归档三态 / 时间 → 单钮弹窗） | ✅ 已上线（v0.9.9，`.post-filter-pop` 三分区 + `DateRangePicker`，devlog/050） |
| 类型筛选 / 服务端分页 / 「已归档 N」chip | ✅ 已上线 |
| 候选池收录 / 解订阅连带清帖 / 批量任务 | ✅ 已上线（v0.5.0；解订阅外键回滚已修，devlog/040） |
| 抓取任务优先级（手动 > 定时档） | ✅ 已上线（devlog/040） |
| 抓取调度合并为综合档（24h 数据驱动 + 双粒度并发 + 外部批次排队） | ✅ 已上线（devlog/042；预算自适应 devlog/054） |
| 收录 / 添加账号后立即抓取 | ✅ 已上线（devlog/041/044：账号信息可见 6.0s → 0.9s） |
| 外部任务进度胶囊 + 完成后卡片自动刷新 | ✅ 已上线（devlog/044） |
| 顶栏展示策略（自动节拍静默） | ✅ 已上线（devlog/051；状态行视觉定稿 devlog/043） |
| 微博 / B 站增量漏帖（置顶帖打乱流序） | ✅ 已修（devlog/045） |
| 微博扫码登录 / B 站扫码登录 | ✅ 已上线（v0.5.0） |
| 账号统计快照历史（粉丝趋势） | ✅ 采集 + 可视化（**v0.9.x 重写为 ECharts canvas**，recharts 已退役） |
| 档案设置弹窗（背景 / 头像 / 签名 / 已订阅账号，全实时生效） | ✅ 已上线（devlog/066–075）：签名走**来源+覆盖**模型（A3，devlog/074）——平台签名只读、卡片签名 = 覆盖 → 来源账号 → 主账号 → 空；**字段锁定已退役**，旧值改由 `vtuber_field_history` 记账（**仅在平台侧覆盖前**，手改不入账；展示见 R9）。下拉栏为「内容体内绝对定位浮层」（devlog/075，替换 portal+fixed） |
| 删除检测（墓碑） | ✅ 已上线（v0.5.1：两击判定 + 已删筛选/角标/时间线） |
| 外部固定化数据源 | ✅ 已上线（zeroroku 粉丝历史/礼物日 + danmakus 索引/直播场次/弹幕） |
| 直播日历 + 场次内容管道（danmakus+self 合并、9 类类型 v2、校正 override） | ✅ 已上线（v0.9.0–v0.9.x M1–M4；对外 id 定权见 devlog/052） |
| 场次详情弹窗（信息 / 分类校正 / 弹幕词云 / 直播动态 / 分析预留） | ✅ 已上线；**上游取数已拆成独立请求 + 10 分钟缓存**（devlog/063）；分析服务未接入 → §1.2 |
| 弹幕词云（上游 top40 + 本地自建兜底 + 五态文案） | ✅ 已上线（devlog/061/062）；**原始弹幕明细库暂缓** → §1.2 |
| 滚动条标准（不占宽 + 自动隐藏 + OverlayScroll 统一） | ✅ 定案并落地（UI-MAP F 节） |
| 桌面端图标清晰度（任务栏/资源管理器） | ✅ 已修（devlog/043） |
| 验证基建（pytest / vitest / eslint / ui_probe / 位级签名） | ✅ 已上线（devlog/056/059 + 062–066；基线见 §6.2） |
| 数据导出 / 可移植格式 | ❌ 无 → §1.4（P3） |

---

## 3. 📦 Backlog（远期，按价值排序）

| 方向 | 说明 | 前置 |
|------|------|------|
| 帖子互动历史曲线 | 同 §1.2：`stats_json` 原地覆盖，热帖传播过程会丢；要保留须加 post 统计快照 | 无（越早越不丢数据） |
| 弹幕分析（全量） | 场次级词云已接入；原始弹幕明细库与全量分析（实质是独立产品）暂缓 | 明细存储（§1.2） |
| 涨粉趋势可视化 | ✅ 已上线（v0.6.0 起，v0.9.x 重写 ECharts）；多观察者数据合并后会更稠密 | — |
| P2P 分布式交换 | Phase 1 文件交换（依赖 P3 格式 + 合并语义）→ Phase 2 目录服务 → Phase 3 DHT；需签名 provenance 与隐私 opt-in（贡献即暴露关注列表）；Phase 2+ 可为二创精选提供涌现质量信号 | P3（§1.4） |
| 关注流二创精选（让作品被看见） | 匹配失败而非曝光不足；二创自带收件人地址（关于哪个 V，受众即该 V 的粉）。详见 §5 | stats 历史（P0 已积累） |

---

## 4. 🚫 明确不做（近期）

- **IconRail 占位入口**（日历/邮件视图）：核心价值兑现前不加新面板
  —— 2026-09-08 已删除四枚占位（用户/日历/刷新/设置）与右栏光条邮件占位钮，只保留已接线的「帖子浏览」与四个视图钮
- **多用户 / 云同步**：坚持本地优先定位
- **截图式"证据留存"（页面快照/PDF 存档）**：与现有"结构化字段 + 原始 `raw_json`"路线重复，
  且体积与时序噪声大；若要"证明力"，优先补 §1.2 的原始弹幕明细这类**结构化原始数据**

---

## 5. 🌱 远期构想：让作品被看见（二创发现）

动机：一件优秀作品没被最可能喜欢它的人看见是残忍的。病灶是匹配失败，
不是曝光不足——不需要社区平台，需要的是路由能力。

核心洞察：二创天然自带收件人地址——作品关于哪个 V，受众就是喜欢那个 V 的
人。订阅关系、faction 字段、联合投稿多 V 存储就是现成的路由表。

定位约束：做成 DDtoolkit 内的功能，不做平台。无服务器、无账号、无投票货币。

分阶段形态：

1. **编辑精选周刊**——手工挑选，静态推送，零基建验证需求是否真实存在
2. **统计遗珠信号**——小创作者异常互动率（硬币/收藏对播放比例失衡）自动浮现，
   依赖粉丝基数与帖子统计历史的积累（→ §3 帖子互动历史曲线是前置）
3. **归档涌现信号**——接入 P2P 归档网络后，「多少节点愿意为它花磁盘空间」
   即最诚实的质量票（与 §3 的 P2P 条目合流）

明确不采用：虚拟钱币投票 + 推送权重正反馈（赢家通吃反噬长尾发现；
中心服务器复刻热情退去问题；国内 UGC 合规成本单人不可承担）。

---

## 6. 🗄 已完成（索引）

> 详细条目（原始需求 / 验收 / 实现注意）在 **`docs/ROADMAP-DONE.md`**，本文件不重复。

### 6.1 版本 / 批次 → devlog

| 版本 · 批次 | devlog |
|---|---|
| v0.5.0 平台化 + 统计快照 + 品牌整理 | 021 |
| v0.5.1 删除检测（墓碑） | 022 |
| v0.5.2 全文搜索深度升级（P2） | 023 |
| v0.6.0 P4 数据源分离 / P5 档案视图 / P6 界面优化 | 024–026 |
| 应用启动链 / 时效分层调度 / 二级界面重绘 / 档案卡精简 | 027–030 |
| v0.6.1 视图账号状态隔离 | 031 |
| v0.7.0 档案视图细化（P7 + 追加两项） | 032 |
| v0.8.0 直播日历重设计 | 033 |
| v0.9.0 月历按设计稿重构 | 034 |
| v0.9.1 场次内容管道 M1–M4 + 弹幕词云首版 + 滚动条定案 + 前端审计 | 035 |
| v0.9.1 修复：首启卡幕 / 布局滚动条 / LOGO 与视图定序 | 036–038 |
| v0.9.2 修复：列宽乱跳 / 外键回滚 / 加账号立即抓取 | 039–041 |
| v0.9.3 调度重构（综合档）+ 界面细节六项 + 小尺寸图标稿 | 042–043 |
| v0.9.4 收录提速（账号信息 ∥ 首屏 + 头像延后） | 044 |
| 增量漏帖修复（置顶帖打乱流序） | 045 |
| v0.9.5 P8-A 前端快赢 + P8-C 顶栏进度 | 046 |
| v0.9.6 P9-A 数据正确性（场次去重 / 视频合并 / 平台化类型） | 047 |
| v0.9.7 P8-B card 改造 + 档案设置窗口 | 048 |
| v0.9.8 P9-B 启动外部补抓 + 动态流自适应节奏 | 049 |
| v0.9.9 P10-A 筛选行收纳 + 双月历区间 | 050 |
| 顶栏展示策略 + 弹窗草稿保护 + UI-MAP 右栏重整 | 051 |
| 场次对外 id 定权（修"近期场次详情空白"） | 052 |
| 静态审计风险项修复（预算越界 / 旧库桥接 / 未定义令牌） | 053 |
| 静默失效三修（调度线程兜底 / 危险文案 / 探针假失败）+ 预算自适应 | 054 |
| 收尾四处缺口 + 词云抽出 | 055 |
| 前端 P2 分层收敛（PostsPage 纯逻辑 / useLiveSessions / LiveSessionDialog / 词云件） | 056–059 |
| 词云断供调查 → **更正为误判** | 060–061 |
| 弹幕「抓不下来」= 12s 超时误报（30s + 3 次重试） | 062 |
| 弹幕取数拆出详情端点（两段式加载 + 10 分钟缓存） | 063 |
| 上游取数可取消（切场次/关弹窗即 abort） | 064 |
| P2 分层收敛收尾：`PostsPage` 拆出三个视图件（1134 → 859） | 065 |
| 需求清单 R1–R5/R7/R8 落地（头像链路 / 防闪 / 来源标注 / self 优先 / 按钮居中 / 徽标可点 / 令牌化） | 066 |
| R1/R4 补充：档案设置全实时生效、头像规则、趋势来源入 hover | 067 |
| 「基本资料」整节删除（用户确认读法 B） | 067 §四 |
| 签名平台下拉栏 + §1.1 三项收尾（灌词接线 / 进度反馈 / 可取消） | 069 |
| 需求 R6：动态流分账号并发 + 60s 周期下限 | 070 |
| §1.1-d 场景机护栏尝试失败（为何仍未做 + 下次顺序） | 071 |
| 签名下拉栏改版（内嵌 chevron + 参与布局面板 + 渐隐横滚）+ `--settings` 探针 | 072 |
| 签名下拉栏浮层化（portal + fixed）+ 再点收起 + 面板等宽 | 073 |
| 签名来源与覆盖（**A3**）+ 字段锁定退役 + 曾用名/曾用签名（迁移 f004）+ 新表补进 purge | 074 |
| 签名下拉栏"**点不动**"根因修复（radix `body{pointer-events:none}`）+ 面板挂回内容体 + 曾用值撤出展示 | 075 |
| 日志排查：动态流每轮 ERROR = 起跑闸门持有 loop-bound `asyncio.Lock`（R6 回归）+ 顺清同形态隐患 | 076 |
| 日志按天轮转（`app.log.YYYY-MM-DD` ×7）+ 配置搬到 `core/logging_setup.py` 并补护栏 | 077 |

### 6.2 当前门禁基线（2026-09-13 实测 / 复核）

| 门禁 | 命令 | 基线 |
|---|---|---|
| 后端 | `python -m pytest -q` | **309 passed** |
| 前端类型 | `npx tsc --noEmit`（`npm run build` 也会跑） | **0 错** |
| 前端 lint | `npm --prefix frontend run lint` | **0 错**（`--max-warnings 0`） |
| 前端单测 | `npm --prefix frontend run test` | **119 passed** |
| 词云布局 | `node scripts/check_wordcloud_layout.mjs` | sha256 `19ecc7e6…`（本轮实跑一致） |
| 布局探针 | `python scripts/ui_probe.py --hero-expect c1154858… --vtuber 15`<br>`python scripts/ui_probe.py --archive --calendar-expect fb75217e… --vtuber 15`<br>`python scripts/ui_probe.py --settings --vtuber 15`（两个带签名账号）/ `--vtuber 14`（单账号长签名） | 三条都本轮实跑一致（8 段契约 0 问题 / 三档宽度 0 问题 / 档案设置几何与**可点性** **21 项**，含 `panelHit`/`rowHit` 命中测试、`panelPlacedByRect`、`pickKeepsDialog`；V14 因只有一行会打印 `[跳过] 候选行切换断言`） |
| 一把梭 | `python scripts/dev_check.py` | 测试 + 后端冒烟（详见 `docs/DEV-LOOP.md`） |

> ⚠️ 探针的 `--hero-expect` / `--calendar-expect` 签名**含实时数据**，只适合"改动前后短窗口对比"，
> 不适合当跨天基线；数据一变签名就漂（文档里的哈希值只是当时的记录值）。
