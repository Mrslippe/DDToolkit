# DDtoolkit 开发路线图（TODO）

> 定位一句话：**个人 VTuber 证据档案库**——定时记录、永不丢失、找得回来。
> 核心闭环（订阅 → 自动抓取 → 浏览 → 归档）已完整；当前主线是补全"档案完整性"，
> 让存下来的数据可检索、可证明、可带走。
>
> **这份文件怎么用（三条）**
> 1. **想到新需求 → 写进 §0**，一句话就行，不必先想清楚怎么做；
> 2. **要干活 → 看 §1**（唯一"待办"清单，分四组：能立刻动手 / 要先拍板 / 用户侧动作 / 已搁置）；
> 3. **想查过去怎么定的 → 看 `docs/ROADMAP-DONE.md`**（已完成条目的原始需求与验收、
>    已落地需求清单、**版本 → devlog 索引**）。
>
> 整理记录：2026-09-13（把历史条目拆到 `docs/ROADMAP-DONE.md`，本文件只留"要干什么"）；
> 同日二次整理：**已落地的需求 R1–R10 与批次索引一并移入该文件**，§0 只剩未落地项。

---

## 0. 📥 待提需求（收集区 —— 新需求写这里）

> **用法**：直接在表里加一行。**只写你想要的**（现象/期望效果），不用写实现方案；
> 优先级拿不准就留空，我会在评估后回填。
>
> **报 bug 请给这四行**（2026-09-15 复盘定，devlog/085）：① 我在做什么（哪个界面 / 按钮 /
> 输入了什么）② 期望什么 ③ **实际什么（截图或原文报错）** ④ 每次都发生吗。
> 有截图 + 最短复现的那次（"加池外 V 报收录失败"）10 分钟就定位到根因；只给一句"好像不对"的那次
> （"微博登录过期"，其实没过期）我按错误前提白查了一轮。
>
> **提功能需求请补一句"怎么算做完"**（验收标准，例：*"我在 B 站搜一个候选池里没有的新 V，
> 能加进来、名字正确、并立刻看到抓取启动"*）。R11 当时若写了这句，"索引来源走哪条收录路径"
> 在方案阶段就会被追问 —— 后来实测到的那个 bug 大概率不会发生。
>
> **状态取值**：`待评估`（刚写下，我还没看）→ `已受理`（已拆成可执行项并移进 §1）
> → `已落地`（**移进 `docs/ROADMAP-DONE.md` 的「需求清单」章节**：原文照抄 + 落地结论 + devlog 指向）
> ｜`不做`（附一句理由，同时落到 §4）。

| # | 日期 | 一句话需求 | 期望效果（可选） | 优先级 | 状态 |
|---|---|---|---|---|---|
| **R25** | 2026-09-16 | **把消息推送到其他平台**（把"某 V 开播 / 发了新动态"这类事件推到外部渠道，让用户不必开着界面也能收到） | 后台常驻时也能第一时间收到通知 | 低 | 📋 **待排期（用户 2026-09-16：再往后放 —— "可能要很后面才会开发"）**。⚠️ **前提约束**：**后台/托盘挂起期间必须保持"尽可能快"的抓取节奏**——所以 R24 的"隐藏后降频"被**明确取消**：降频会直接拖慢推送时效。设计时要顺着这条走（见 `ARCHITECTURE.md` §3.10 第 4 条） |
| **R35** | 2026-09-17 | **置顶动态跟着置顶**：有些 V 用置顶动态放周表 / 舰礼，抓到的置顶帖也要钉在列表顶部，并且**每日轮询要覆盖它**，改了能及时被捕捉 | 周表/舰礼不用翻旧帖找；改了就能看到 | 中 | ✅ **已落地**（devlog/139）→ 详见 `docs/ROADMAP-DONE.md`「需求清单：R35」。迁移 `f005` 落 `posts.is_pinned`；置顶帖每轮刷新（列表页字段免费、详情按 6h 节流）、取消置顶自动撤销、列表排最前 + 「置顶」角标 |
| **R36** | 2026-09-17 | **场次详情别空着**：上游数据没回来时，左列封面下方用一张「信息胶囊卡」填充（不要空着），右侧卡片高度也要固定，防止数据一到弹窗长度跳变 | 打开即完整，不跳动、不出现"加载中…" | 中 | ✅ **已落地**（devlog/140）→ 详见 `docs/ROADMAP-DONE.md`「需求清单：R36」。左列补「本场速览」2×2 四枚胶囊（只放本地值）；右列 A 组指标 / 弹幕信息 / 直播动态三处换成**同尺寸骨架** + 槽预留高度 ⇒ 连采两格高度零变化 |
| **R37** | 2026-09-17 | **档案视图 = 自定义卡片画布**：把"档案卡"这一版做成**以卡片为单位**，用户能编辑卡片的大小 / 位置 / 排布，卡片内容可自定义（纪念日、优质投稿、大事记、时间线…），并**支持扩展** | 每个 V 的档案页由用户自己搭 | 中 | 🔨 **P1 已落地**（devlog/141）：命名改定（档案卡 → **档案视图**、档案 → **数据视图**）+ 只读卡片画布（12 列网格，`layoutModel.ts` **17 条单测**）+ 卡片注册表（`registerCardKind`，**4 条**）+ 两张内置卡片（纪念日 **14 条** / 优质投稿 **9 条**）。**四个选型已拍板**（自研网格 · 新表 `profile_cards` · 碰撞推开 + 编辑/阅读分离 + 窄窗单列 · 前端注册表）⇒ **P2a 已落地**（devlog/142：迁移 `f006` + 表 + Repo + 两条路由 + purge 同步 + 15 条用例）；**P2b 已落地**（devlog/144：读布局 + 编辑态 + 指针拖拽缩放 + 整版 PUT + 失败回滚，几何用例 17→30，探针 `--board`）；**P3a 已落地**（devlog/145：大事记卡接上 `vtuber_events` —— 也是扩展点的真示例，视图一行没改）；**P3b**（自定义卡片：新增/删除 + `config_json` 载体）待做 |
| **R38** | 2026-09-17 | **状态胶囊的灵动岛式形变**：顶栏状态胶囊要"优雅灵动"地**向中间收起 / 展开**，为之后想做的**桌面独立控件**打基础 | 状态变化时是"同一块东西在长大/收拢"，不是闪现一块板 | 中 | 📋 **已受理**：**设计规格已落 `docs/design-status-island.md`**（动效令牌 / 时间轴 / 同心圆角 / 两种宿主材质 / 可断言的不变量 / 5 个批次）—— 动效是"看着好不好"的事，先把规格定下来再实现，评审才有依据 |

> **R35–R37 方案要点（2026-09-17 受理；代码位置都已核实过）**
>
> **R35 置顶动态** —— ✅ **已落地（devlog/139）**：迁移 `f005` 落 `posts.is_pinned / pinned_refreshed_at`；
> `PostRepo.sync_pinned` 在**第一页解析成功后**同步集合（标记 + 撤销），`scheduler._refresh_pinned_post`
> 让置顶帖**每轮刷新**（列表页字段免费刷、详情按 `PINNED_DETAIL_REFRESH_HOURS` 默认 6h 节流，失败不盖戳）；
> 列表排序 `is_pinned desc, published_at desc`；前端 `.post-card-pin` 角标 + 一圈粉描边；
> 护栏：`tests/test_pinned_posts.py` **12 条**（反向验证 7/7 红）+ 第 **17** 个探针 `ui_probe.py --pinned`（反向验证 2/2 红）。
> **三个小口径的定论**（2026-09-17）：① 置顶帖**不单独分页**——它只占第 1 页头部一次，跨页不重复
> （`total` 也不因置顶变化）；② 取消置顶**不进墓碑、不留痕**（静默撤销，帖子回时间线原位）；
> ③ 「数据视图」与导出**本轮不体现置顶**（那是"证据时间线"，保持纯时间序；要动留到 R37 再评估）。
>
> **R36 场次详情占位** —— ✅ **已落地（devlog/140）**。现状回顾：上游（danmakus 的弹幕词云 / 场次指标 /
> 直播动态）没回来时，区块里是 `.lc-dlg-ph` 的「加载中…」文案，而左列封面下方**是空的**（截图红圈处）；
> 数据到达后 DOM 长高 ⇒ **弹窗高度跳变**（实测：窗 533 → 557px、两列区 207 → 276px）。
> **口径**（2026-09-17 用户拍板）：① 胶囊卡**只放四枚本地就有的值、分双行**（时长 / 峰值在线 / 弹幕数 /
> 收益；上游那组不进卡 —— 它们在右列已有行）；② 未到位处一律**同尺寸骨架**，"上游慢/失败"仍出文字；
> ③ 只在「未到位 → 到位」这段保证**零变化**（不做恒定高弹窗）。落地：左列 `.lc-dlg-left` + `.lc-dlg-glance`
> （2×2）+ 右列 `.lc-dlg-metrics`（恒定四行 + 第五行预留）+ `.lc-dlg-slot--danmaku/-evts`（`min-height` 预留）；
> 口径抽到 `components/live/sessionGlance.ts`（**6 条单测**）。
> 护栏：`ui_probe.py --archive` **连采两格**（探针自己把 `/upstream` 压后 2.5s 造未到位态）比
> **滚动内容高** + 窗高 + 两列区 + 右列卡 + 速览卡，**反向验证 3/3 红**（撤速览卡 / 撤骨架标记 / 撤预留高度）。
>
> **R37 档案视图** —— 🔨 **P1 已落地（devlog/141）**，**四个选型 2026-09-17 拍板**：
> ① **自研** CSS Grid（不用 `react-grid-layout`：几何可控、探针可断言、零新依赖）；
> ② 布局存**新表 `profile_cards`**（P2 落，迁移 `f006`；记得同步 `purge`）；
> ③ 交互 = 碰撞**推开** + 编辑/阅读态**分离** + 窄窗**单列降级**；
> ④ 扩展点 = **前端卡片注册表**（`registerCardKind`，照 `registerIdleProvider` 先例）。
> P1 交付：命名改定（档案卡 → **档案视图** / 档案 → **数据视图**）· 只读画布（`layoutModel.ts`，
> **17 条单测**）· 注册表（**4 条**）· 两张内置卡（纪念日 **14 条** / 优质投稿 **9 条**）；
> 护栏 = 默认探针三档的 `_assert_board`（高度=模型 / 不重叠 / 不越界 / 窄窗单列 + 卡片内容非空），
> **反向验证 5/5 红**。⛔ P2（拖拽缩放 + 落库）· ⛔ P3（自定义卡片 + 扩展点接线）待做。


> **R12–R24 已全部落地，并已按本文件规则移出本表**（原文、期望与落地结论都在
> `docs/ROADMAP-DONE.md` 的「需求清单」章节：R12–R17 / R18 / R19–R24）：
> R12 状态岛/灵动岛（devlog/089、090）· R13 预约进日历（088）· R14 设置界面（091、092）·
> R15 前端三处打磨（087）· R16 筛选钮跟随侧栏（093）· R17 设置窗口两栏改版（094）·
> R18 托盘隐藏（095）· R19 空闲轮播下线（096）· R20 两处实测修复（097）·
> R21 设置与弹窗改版（100–102）· R22 数据目录与占用（103–112）· R23 应用内更新（113–116）·
> R24 托盘资源（117）。**本表当前只剩 R25**（已按用户口径往后放 —— "可能要很后面才会开发"）。
> **R26**（① devlog/126 · ②③④ devlog/128）· **R27**（125）· **R28**（127）· **R29**（129）·
> **R30 静默时段**（130）均已落地 —— **2026-09-16 这轮"请求侧/风控侧"盘点（devlog/124）
> 提出的事项，加上你随后追加的两条（定时时段降频、HTTP/2 是否值得），至此全部收口**。
> **R31 切换手感**（**已在同一轮问答里落地**，devlog/133）也**没进过本表**：它是
> "切换卡顿正常吗"那个问题的后续决定（量完 → 你选"B + 轻微 A" → 当场做完），
> 原文与落地结论直接记在 `docs/ROADMAP-DONE.md` 的「需求清单：R31」。
> **R32 低配体检**（134）同理："低配机器跑得怎样"问完 → 你选 B（README 最低配置 + 固化脚本）
> → 当场落地，原文见「需求清单：R32」。
> **R33 四角白边 + 左栏跟随档案设置**（135）：两条带截图的小需求，同日修完并补了探针
> `--profile-sync`，原文见「需求清单：R33」。
> **R34 圆角交给 Windows**（136）：用户选"先考虑原生方案" ⇒ 圆角/吸附方角全交给 DWM，
> 原文见「需求清单：R34」。**本表当前仍只剩 R25。**

> **编号说明（2026-09-15）**：用户原写 R1–R4，与**已落地的 R1–R11** 重号 → **由我重编为 R12–R15**
> （R1→R12 状态岛 · R2→R13 预约入历 · R3→R14 设置 · R4→R15 三处小改），用户已确认。
> R15 的「中/低/高」按三个子项顺序理解：① 标题粗体=中 · ② 筛选钮文字居中=低 · ③ 徽标「+」不占位=**高**。


> **R1–R11 已全部落地**（R9/R10 见 2026-09-13，R11「添加 V 支持 B 站直查」2026-09-15）→
> 见 `docs/ROADMAP-DONE.md`「需求清单：R11」与「需求清单：R1–R10」；本表只留**还没完全落地**的条目。
>
> **下一条怎么写**（照这个格式加一行；编号接在最后一条之后）：
> `| R26 | 2026-09-dd | 一句话说清现象或想要的效果 | 可选：期望的样子 | 中/低/高 | 待评估 |`

### 0.1 怎么提需求 / 提修改意见（2026-09-15 定，devlog/085）

**三种类型，写法不同**（写在表格里、或直接跟我说都行；一次提 3~5 条也没问题，我会拆批次）：

| 类型 | 请给我 | 例子 |
|---|---|---|
| **bug / 不对劲** | 四行：① 我在做什么（哪个界面·按钮·输入了什么）② 期望什么 ③ **实际什么（截图或原文报错）** ④ 每次都发生吗 | *"添加 V 里我搜一个候选池没有的名字 → 期望能加进来 → 实际弹『候选池中不存在』（截图）→ 每次都这样"* |
| **功能 / 想加的能力** | 想要的效果 + **怎么算做完（验收标准）** + 边界（可选：什么情况**不**该发生） | *"能按 B 站 UID 直接加 V；**算做完** = 我输入 uid 能搜到、点一下加进库、名字是 B 站上的那个"* |
| **修改意见 / 感觉别扭** | 一句话说清"哪里别扭"就够（不用给方案） | *"粉丝趋势那两个档位按钮看着不像按下去了"* |

**四条经验（都来自本项目真实返工）**：

1. **截图/原文报错 > 文字描述**。"加池外 V 报收录失败"那张截图让我 10 分钟定位根因；
   而"微博登录好像过期了"（其实没过期）让我按错误前提白查一轮。
2. **验收标准是最值钱的一句**。R11 当时若写了"搜池里没有的新 V → 能加进来且名字正确"，
   "索引来源走哪条收录路径"在方案阶段就会被追问，你后来实测到的那个 bug 大概率不会发生。
3. **只给约束，不给实现**（除非你有硬要求）。你可以说"必须复用现成接口""不能打上游"，
   但"用哪个函数、怎么改"交给我 —— 我会给方案 + 代价，再动手。
4. **决策类问题请直说归属**：`必须这样` / `可以这样` / `都行，你定`。
   最省事的是像 R11 那样一次拍四个口径（检索是否显式触发、点结果是否直接收录、本地来源范围、翻页上限）
   —— 我不用猜，"你说了算"的条目我会**写明我的决定与理由**再落进本文件的 §1/§4。

**我收到之后的固定动作**（你可以据此检查我有没有偷懒）：
`写进本表 → 我给方案（含代价与验证方式）→ 你确认 → 实现 → 同批次补用例/探针/文档/devlog → 报门禁数字`。
功能类改动我会**先建护栏再动手**；修 bug 我会**先补一条会红的用例**再修（并反向验证它真的会红）。

---

## 1. 🔧 未完成项（当前待办）

> 这是**唯一**"要干活"的清单。每条都写了性质与阻塞，避免"看起来能做其实做不了"。
> 已完成的条目不在本文件堆着 —— 见 `docs/ROADMAP-DONE.md`。

### 1.1 可以立刻动手（无外部阻塞）

| 项 | 性质 | 说明 / 下一步 |
|---|---|---|
| **词云自建扩展点接线**（可选增强） | 体验 | `count_tokens(extra_words=…)` / `JiebaTokenizer.add_words()` 已就绪但**没有自动灌词**：按 V 名/企划名自动灌自定义词典，避免"明前奶绿"被切碎（接线处留了 `ExtraWordsHook`）。devlog/061 §四 |
| **自建词云首拉耗时**（可选优化） | 体验，可接受 | 实测 5 万条记录 ~12s 属正常，上游瞬时变慢时单场可达 **120s**（devlog/062 §四）。**刻意不加时间预算**：截断取靠前记录会系统性丢掉下播前的高频词（如「晚安」）。可选改法：分页大小调优 / 前端进度反馈 / 明确标注"基于部分弹幕"后再截断 |
| **自建词云也支持取消**（照搬现成管道） | 体验小修 | 上游取数已可取消（devlog/064）；`/wordcloud` 走同一套（`api.buildLiveSessionWordCloud` 加 `signal` + 调用处 abort）。因它只在用户点按钮时发、120s 是可接受上限，优先级最低 |
| ~~`PostsPage` 场景切换机抽 hook~~ | ✅ **已落地**（devlog/080） | 护栏先成（`ui_probe --scene`，顺带**反转了 devlog/071 的"卡死"结论**：那是探针读了旧 DOM 节点）；机器抽到 `hooks/useSceneTransition.ts`，只搬不改；hero/日历位级签名保持一致 → 详见 `docs/ROADMAP-DONE.md` |
| ~~R2 可选第二步：`fetch-idle` 带 kind~~ | ✅ **已落地**（devlog/080） | `utils/fetchIdle.ts` + `TopBar` 两种派发带 kind + 趋势图独立 `trendTick`；**日历三类都要**（动态流的直播卡片会落场次）→ 详见 `docs/ROADMAP-DONE.md` |
| ~~未登录也能加 V（B 站检索的匿名签名）~~ | ✅ **已落地**（2026-09-15，devlog/086 的 P0–P3） | 实测：匿名 `nav` 也下发 `wbi_img` ⇒ 检索/账号信息/粉丝数/直播状态未登录可用；**内容抓取与微博须登录**（匿名被平台 `412 request was banned`，IP 级）。落地：`wbi.allow_anonymous`（只给检索路径）· `services/capabilities.py` 能力表（三态）+ `GET /capabilities` · **内容抓取闸门**（未登录不发请求、5 端点 403、`login_required`）· 前端未登录提示（顶栏入口 + 说明窗 + 各浮窗标注，**只标不藏**）· 两态实测矩阵 + 策略/实测双向契约用例 → 详见 `docs/ROADMAP-DONE.md`「未登录可用范围（guest mode）」 |
| **R15 前端三处小改** | ✅ **已落地**（2026-09-15，devlog/087） | ① 顶栏标题 `400 → 700`（**只改字重**，字距沿用原设计 5px；探针量出文本宽 114.3/150）。② 筛选钮居中 —— **先量后改，结论与直觉相反**：文字本身左右各 21.5px（差 0），真正偏的是"文字+caret"这一组（caret 绝对定位钉在最右角 ⇒ 组中心右偏 9.25px）；改为 `.pfilter-btn .pill-caret{position:static}` + 内距 12px ⇒ **组差 0**、宽度仍 68px、工具行几何不变（侧栏那枚 caret 不受影响）。③ `.pill-add` 空闲时 `height:0 + margin-top:-10px`（抵消列 gap）⇒ **净占位 0、不可命中**，徽标→分割线 **57px → 10px**；hover 展开回 37px 且可点。护栏：新探针 `ui_probe --polish`（三条全量出来 + **反向验证过**）；hero 签名 `c1154858…` **未变**、主探针三档 `问题=0` |
| **R13 直播预约进日历** | ✅ **已落地**（2026-09-15，devlog/088） | **后端零改动**（方案里原打算加聚合端点，核实发现 `GET /vtuber/{id}/future-reservations` 已存在、且日历本就是单 V 的 ⇒ 聚合是多余的）。前端：`api.futureReservations()` + `hooks/useReservations`（V 级取数，独立于 `useLiveSessions` 的 effect 顺序契约；失败不清空）+ `utils/reservationDays.ts`（按天分组 / 徽章优先级 **场次类型 > 预约 > 休息·待定** / 展示行，9 条单测）+ 日历格 `.lc-resv`（**不动九类色系**，左缘粉竖条；计数槽放**预约人数**；有场次时压成一行小字）+ hover 浮层预约块（只有预约的日子也能 hover）。护栏：`ui_probe --reservations`（**探针自己种一条明天的预约**进数据副本，断整条链路，**反向验证过**）+ 路由契约用例。日历签名 `48519bae… → 50b78ec0…`：**数据漂移**（当晚 V15 新落库两场），已排除本批改动并重取基线 |
| **R12 状态胶囊 → 通知/灵动岛控件**（拆 a/b） | ✅ **已落地**（R12a devlog/089 · R12b devlog/090） | **R12a**：`utils/notificationHub.ts`（条目模型 + 优先级 `alert>progress>report>message` + 过期/常驻 + 命名规则，**12 条单测**；「自动节拍不占顶栏」从散落判断变成**具名规则 + 反向用例**）· `components/StatusIsland.tsx`（四态；面板 portal+fixed）· **六类信息源全接**：任务进度 / 第三方同步 / **完成报告（改常驻条目 + 「查看详情」，不再自动弹窗）** / 登录失效（+「去登录」）/ **风控冷却**（后端 `fetch-status` 新增 `rate_limit` 字段，9 处冷却点统一走 `_cooldown_for_rate_limit` 记账）/ 瞬时消息（ttl 4s）· 键盘 Enter/Esc。<br>**R12b**：期望②**动效** —— 面板入场 `si-panel-in` 220ms（位移 6px + 缩放 .985，`transform-origin: top`）、chevron 翻转 `transition .2s`、计数徽章 `si-pop-in` 180ms；`prefers-reduced-motion` **保留淡入、去掉位移**（`si-panel-in-fade` 180ms，实测 `--force-prefers-reduced-motion` 走通）。期望③**空闲轮播** —— `utils/idleQuotes.ts`（**15 条单测**：池子第 0 格 = 状态文案、取模选格、去重、来源抛错不炸顶栏、语录忌词/忌竖线、档位区间）+ `registerIdleProvider()` 扩展点；时钟是**组件自己的定时器**（只在空闲时开 —— 挂抓取轮询上会随轮询间隔漂移甚至停住）；空闲文案仍**无容器**只有绿点。护栏：`--status-island` 扩到**连采三格**（断言词出自 `data-idle-pool`、就是 index 那一格、索引真的在走、池内无进度词）+ 入场动画按 `matchMedia` 判分支（量 `getComputedStyle`/`getAnimations()`，CSS 里写了不算数；chevron 展开必翻转、reduce 下只去过渡不减指示）→ **反向验证过**（停表 + 塞进度词 → 三条断言同时红；还原后绿）→ 详见 `docs/ROADMAP-DONE.md` |
| **R14 设置界面**（拆 a/b） | ✅ **已落地**（R14a devlog/091 · R14b devlog/092） |**R14a**：IconRail 底端齿轮 → 独立设置弹窗（`.aps-*`；分区标题带**生效时机**，只读区**逐条写理由**）· 后端 `app/core/runtime_settings.py` 覆盖层（**19 个可热更键** + 10 条只读说明）+ `GET/PUT /settings` + `POST /settings/reset`（白名单/类型/闭区间/**跨字段上限≥下限**；越界 400 带中文原因）· 读取路径在 `Settings.__getattribute__` 拦截（优先级 **实例属性 > 覆盖层 > 类属性**，从而保住 `monkeypatch.setattr(settings, …)` 这条既有调试通路）· **零迁移**：落库复用 `app_meta`（前缀 `settings.`）。<br>**R14b**：「浅色 / 跟随系统」偏好（`prefs.theme`；`GET/PUT /settings/prefs` 枚举白名单）+ `utils/theme.ts`（解析 / 写 `html[data-theme]` / 真·跟随系统 / 该说的说明，10 单测）+ `:root[data-theme='dark']` **空块**标记深色未实现 + **跨语言契约**（TS 的 `DARK_IMPLEMENTED` ↔ 后端下发的说明）。<br>**两处按核实改方案**：① 原计划"逐个迁移 10–15 个调用点"—— 实测 25 处调用点全在 `scheduler.py` 且都是每轮读取，没有模块级快照，于是改成拦截层；② 原计划新建表 —— 与 `app_meta` 同构，收益是零。<br>**验收**：后端 29 用例（含"真实调用点读到新值"、落库失败整体放弃、脏偏好不覆盖用户数据）+ 前端 21 用例 · 探针 `--app-settings`（保存后**回问后端**对账；深色分支用 `--force-dark-mode` 验过）· **反向验证过**（断开覆盖层接线 → 6 红；放宽范围/清空跨字段 → 3 红；白名单加 `dark` → 1 红）。<br>**留待后续**：深色主题单列一批（243 处硬编码色值收敛为令牌 + ECharts 双主题 + 逐屏走查；钩子已就位，只需填 `:root[data-theme='dark']` + 翻 `DARK_IMPLEMENTED`） |
| **R16 list 筛选钮跟随侧栏筛选钮** | ✅ **已落地**（2026-09-15，devlog/093） | 用户给两张截图（左栏「默认」= 参照，list「筛选」= 待改）。**先量后改**：新探针 `--filter-pill` 把两枚浮片逐项量化，量出真正的差异 —— 尺寸 89×25 vs 68×30 · 字 13 vs 12.5 · 内距 8 vs 12 · caret **absolute（右上 3px，不占流）** vs static · **文字中心偏移 0 vs −5.3px**。改法：`.pfilter-btn` 换成侧栏同一套**配方**（字 13 / 内距 8 / `line-height:1` / caret 绝对定位右上角），**宽高照旧 68×30**。用户当场补了一句口径：「宽高还是原本的宽高，保持同一行中元素的和谐」⇒ 第一版按"逐项一致"改成 89×25 被纠正为**配方跟随、尺寸各行其是**（回到 68×30，与同行搜索框齐平）。<br>**探针逼出一个真问题**：合成的超长文案（「筛选 · 12 项」）下 caret 距文字只剩 **0.6px**（caret 落脚区 9.63px > 内距 8px）⇒ 文案外套 `.pf-label`（**左右各 11px 对称留白**）：文字仍是几何中心（三态偏移全 0），长文案让浮片自己长（68 → 84.5 → 106.6）、caret 与文字恒留 9.4px。<br>**R15② 被取代**：那版把 caret 留在流内让"文字+箭头"居中，文字仍偏 5.3px；`--polish` 断言随之收紧为 **文字偏移 ≤1px + caret 必须 absolute**。契约分两桶：**STYLE 逐项必须相等**（字号/行高/内距/颜色/斜切/caret 定位·inset·尺寸·距右缘/文字居中），**SIZE 各行其是**（另加"高必须 30✅与同行齐平""文字两侧留白必须对称"两条硬约束）。视觉上另用 `--shot` 截图裁两枚并排与整行比对过 |

| **R18 关闭窗口 → 隐藏到系统托盘** | ✅ **已落地**（2026-09-15，devlog/095） | 用户口径：首次点 ✕ 问一次并记住选择；P1（隐藏+停表）与 P2（深休眠）一起做。<br>**Rust**：`tray-icon` feature + 托盘（左键唤回 / 菜单退出）· 拦 `CloseRequested` → `prevent_close` + `hide` + `set_skip_taskbar` · `ExitRequested` 非主动退出时 `prevent_exit`（**深休眠销毁 WebView 也会走到这里，这条是必须的**）· 退出路径（**R20 起不依赖前端**：托盘「退出」先问后端 `fetch-status.manual_running`，没任务直接 `exit(0)`，有任务才唤回窗口确认）→ `quit_app`（置 `QUITTING` 再 `exit`）· 深休眠 10 分钟销毁 WebView，唤回重建窗口 + `?restored=1` · single-instance 回调改成唤回（原来只 `set_focus`，隐藏时等于点了没反应）。<br>**前端**：`utils/shellLifecycle`（同步可见性源 + dev 钩子）+ `hooks/useShellHidden`（停表：顶栏两条轮询 / 状态岛轮播；恢复：**立刻补一轮**）+ `utils/shellState`（关闭语义三态 + 深休眠现场持久化，路径/视图**都过校验**）+ `CloseActionDialog`（首次询问，两个选项各自写清后果）+ 设置「外观」页可改。<br>**护栏**：新探针 `--tray-suspend`（可见基线 → 隐藏停表 → 唤回补一轮）· vitest +17 · `cargo build` 通过 · 人工验收清单在 devlog/095 与 DEV-LOOP。<br>⚠️ **探针抓到两个真 bug**：① 停表判据读了 React 状态（有一帧延迟）→ 改读同步源；② 只在"排程"时判不够 —— 定时器可能是**还可见时**排下的 10s 后那一轮，隐藏后照样触发（实测漏网时刻 22063 / 隐藏发生在 14349）⇒ **"排程"与"触发"两处都要判** |

| **R37-P3b 档案视图：自定义卡片（新增 / 删除 + config_json）** | 前端，无阻塞（接口已够用） | **大事记卡已在 P3a 落地**（devlog/145），扩展点已被真跑通一次。本批做「用户自己加卡片」：`+ 添加卡片`（列出已注册、当前不在板上的 kind）+ 编辑态删除（×）+ `config_json` 载体（「自定义文字 / 外链」卡：编辑态可改文字，存进 `config_json`）。护栏：配置解析单测 + 探针 `--board` 扩一格（加一张卡 → 落库 → 刷新后还在；删掉 → 库里也没了） |
| **R38 状态胶囊形变动效** | 前端（规格已就绪，见 `docs/design-status-island.md`） | 按规格的 5 批走：① motion token 化（零行为变化）→ ② 胶囊折叠/展开形变 → ③ 面板锚定 + 同心圆角 → ④ 打断/重定向 → ⑤ `density="widget"` + 独立小窗（**桌面控件本体**）。护栏照规格 §10 的不变量写（顶栏高度恒定 / 右簇位移 <1px / 锚点误差 ≤0.5px / 中间态圆角恒为高度半 / 可打断 / reduced-motion 只留淡入） |
| **待排期（可选，不做也不影响）** | 无阻塞，等你挑 | ① **空闲轮播上线**（R19 下线了；等库中真有条目 —— 接 `registerIdleProvider` + 翻 `IDLE_CAROUSEL_ENABLED`，同步改探针断言）；② **深色主题**：R14b 的钩子已就位（填 `:root[data-theme='dark']` + 翻 `DARK_IMPLEMENTED`），但真正做要先把 **243 处硬编码色值**收敛成令牌 + ECharts 双主题 + 逐屏走查 —— 单列一批；③ **能力受限入口并进状态岛面板**（`NoticeActionKind.open-limits` 已预留）；④ 参考图右上的动作行（复制诊断信息 / 打开数据目录）——R17 按用户口径没做 |

| **R19 空闲语录轮播下线** | ✅ **已落地**（2026-09-15，devlog/096） | 用户口径：「顶栏状态栏空置的时候轮播的语录集暂时下线，等之后库中真有了条目再上线」。做法：`IDLE_CAROUSEL_ENABLED = false`（R12b 那 7 句内置语录是**占位文案**，与库中内容无关）+ 关掉时**连 6s 定时器都不开**；**下线不是删掉** —— 池子照建、取模选格逻辑单测用 `enabled: true` 覆盖着测（逻辑不会烂）、`registerIdleProvider` 扩展点留着。<br>**护栏整体反向**（这条比改动本身重要）：探针 `--status-island` 从"必须轮播/索引前进/文案不重复"改成"**恒为状态文案 / 索引恒 0 / `data-idle-carousel === 'off'`**"，而池长、第 0 格是状态文案、池内无进度词那几条**照旧断言**（将来接真实条目时同样成立）。`data-idle-carousel` 由常量驱动 ⇒ **改常量不改断言（或反之）就红**，省得悄悄开了/关了没人知道。上线步骤写在常量注释里（翻常量 + 恢复那组断言 + 接 provider） |

### 1.2 需要先定口径 / 拍板（不是写代码的问题）

| 项 | 卡在哪 | 现状 |
|---|---|---|
| **场次级「直播内容分析」服务** | 要先定分析口径（独立产品级） | 接口字段早已预留（`analysis`），详情弹窗显示"接口已预留"。devlog/035 |
| **原始弹幕明细库 / 全量分析** | 要不要做、以什么粒度落库（这是独立产品级决策） | **通道已实测可用且已被真实使用**：`/api/v3/lives/{liveId}/danmakus` 公开免鉴权 + `offset/limit` 分页（max 100000，含弹幕原文/礼物/上舰/SC）；词云自建（devlog/061）就走这条路，只差"落表 + 前端下钻"。devlog/060 §一.2 |
| **弹幕词云词字下钻**（点某词看该词在场的弹幕） | 依赖上一条的明细存储 | 现在是纯前端破泡交互（点击即消失 + 恢复），没有明细可查 |
| **帖子互动历史曲线** | 要加 post 统计快照表（现在 `stats_json` 原地覆盖，热帖传播过程会丢） | 数据在丢，越早做保留越多；见 §3 |

### 1.3 用户侧动作（不需要改代码，但只有你能做）

- **P8-B 交互手感实机验证**：平台徽章长按拖动阈值、hover 尾部「+」、档案设置弹窗高度、系统浏览器打开是否顺手（devlog/048）
- **P10-A 视觉手感**：双月历配色与日期格密度、窄窗表现（devlog/050）
- **发版**：**v1.0.1 已发布**（2026-09-15，覆盖 devlog 082–084，[Release](https://github.com/Mrslippe/DDToolkit/releases/tag/v1.0.1)，
  release id 389029336，安装包 56.3MB / 便携 70.3MB）；装机验证（直装版 `_internal` + 首启、便携版解压启动）
  与 PAT 吊销**用户已确认完成**（2026-09-15）。
  **v1.0.2**（覆盖 devlog 085–118）：tag `v1.0.2` = **`36a7ba1`** 已在远端（发布前把 R20–R24 的实测问题都修完
  才定版，期间按用户口径**移动过三次**）、`main` 已对齐、产物按该提交重建并校验
  （setup **57.5MB** / 便携 **71.9MB** / 更新清单签名 420 字符 / FileVersion 1.0.2）；
  **GitHub Release 待建** —— 有 token 时一条命令：
  `python scripts/release.py 1.0.2 --from release`（资产在 `dist-release/`，幂等；
  续跑是否保留 release 由**远端 tag 是否存在**判定，见 devlog/120）。
  发布方式已收敛为一条命令：**`python scripts/release.py <版本>`**（devlog/084，手册 `docs/RELEASE.md`）——
  脚本做版本同步/门禁/打版/产物校验/提交/tag/推送/Release，**只有两件事仍需你亲自做**：
  装一次直装版确认 `binaries\backend\_internal\` 存在且首启能过（v1.0.2 起**顺带验托盘**：✕ 隐藏 → 托盘退出能真退出）、
  便携版解压可启动；以及 token 进过对话就吊销。
- **R9 的展示形态**：等「账号信息历史快照」那条线开工时一起定（曾用名/曾用签名放哪、怎么呈现）

> ✅ 已完成（2026-09-13 复核后从本清单移出）：**P9-A 历史归并脚本已在真实库跑完** ——
> 只读复核 `video_dynamic` 49 条中「与 video 同 bvid = **0**」，`posts.note` 附言 31 条，
> 说明归并已生效（原始 323 条已合并，详见 `docs/ROADMAP-DONE.md` 的 P9-A 小节）。

### 1.4 已搁置（有计划恢复，但现在不做）

**P3 JSONL 归档包导出** ⏸（2026-09-05 用户定：P4 数据源先行）

> 搁置理由：导出格式依赖表结构稳定；P4 会新增表/列，等数据源落地后再做格式定稿。
> **格式设计仍有效**（schema_version + vtuber/account/posts 三段、raw_json 保真层、
> P2P 构想的 Phase 0 地基），恢复时按原计划执行。
> **阻塞已解除**：P4+ 的表结构变动已收敛（迁移 head = `f004`），用户点头即可重启。

- 按 V 或全库导出：`schema_version` + vtuber/account/posts 三段，posts 保留 `raw_json` 保真层
- 独立价值：备份、迁移、跨设备；同时是未来 P2P 构想的 Phase 0 格式地基
- CLI 脚本放 `scripts/export_archive.py`，暂不做前端入口

**后端内存与包体优化** ⏸（2026-09-16 用户定：**只记账不删**，先把实测与候选记下来）

> 实测基线（devlog/121；复测方法见 `docs/ARCHITECTURE.md` §3.12）：打包版后端空闲 **128.7MB**
> （任务管理器口径），其中**业务代码只占 ~8MB**，其余是解释器 + 框架地板；打包比 dev 多 ~19MB。
> `_internal/`（100.6MB）里还有 numpy 25.9MB + PIL 12.7MB **在盘不在内存**（`app/` 无人 import）、
> pywin32 1.1MB 是**历史构建残留**（依赖图里根本没有）。三个候选都量过收益，当前都不做：

- **A. 词云词典空闲卸载**：**−55MB**（唯一有实感的项）—— 只在此前真开过一次词云时才有感，
  代价是再次打开词云首屏多等 0.7s；要改 `JiebaTokenizer` 生命周期 + 补用例
- **B. 打包瘦身**（排除 numpy/PIL）：**−38.6MB 磁盘**、内存 0 变化 —— 必须在**打包版**上
  端到端验扫码登录（冻结版才暴露的坑，见 devlog/036 的打平事故）
- **C. Alembic 懒加载**（`alembic_version` 已是最新就不 import）：几 MB，收益最小且迁移路径变复杂
- **不做 D：删掉自建词云与 jieba** —— 上游 `extra.wordCloud` 2026-09-16 实测已恢复（6/6 场次
  各 40 词条），但它在 09-13 断供过一次（devlog/060）才有自建；且 jieba 已懒加载、空闲不占内存，
  留着当断供应急

**数据目录指针的两处收口** ⏸（2026-09-16 用户定：先记下，等 1.0.3 做 —— 现在动要重打桌面壳产物）

> 起因：用户装完构建产物发现**安装版与 dev 用的是同一个库**。根因不是环境变量（三处都是空），
> 而是**指针文件是用户级固定路径 `%APPDATA%\DDToolkit\data-dir.txt`、不带构建标识**
> （`frontend/src-tauri/src/datadir.rs` §`pointer_path`）⇒ 两个构建**共用同一份迁移记录**：
> 在 dev 里迁到 `E:\test\DDToolkit-data`，安装版读到同一条记录就跟了过去。用户删掉那个目录后，
> 两侧各自回退自己的默认目录（`com.ddtoolkit.app` / `com.ddtoolkit.app-dev`），数据都在、没丢。
> 对**普通用户**无影响（只装一份安装版，不会有第二个程序改它的迁移记录）。

- **A. 指针按构建分家**：dev 用 `data-dir-dev.txt`（release 保持 `data-dir.txt`），
  dev 的迁移不再连带改安装版；改动小（`pointer_path()` 一处 + `cargo test` + 文档）
- **B. 加「清除迁移记录（回到默认目录）」入口**：命令清单里现在只有
  `migrate_data_dir` / `delete_old_data_dir`，**没有 clear** ⇒ 目标目录被删/移动（外接盘没插、
  格式化、误删）之后，「迁移记录不可用，已回退默认目录」那行红字会**一直挂着**，
  用户只能自己去 `%APPDATA%` 手删文件。建议放在「设置 → 关于」存储占用面板那行红字旁边
- 附带记一笔：**卸载时 NSIS 不会删这个指针**（它不是安装清单里的文件）⇒ 卸载后重装会沿用旧记录；
  通常正是用户想要的（数据还在），但属于"卸载没清干净"

---

## 2. ✅ 能力现状（已上线，只在判断新需求可行性时查）

> 2026-09-13 更新（原为 2026-09-10 二次审计）。**这一节不需要动作。**
> 最右列写的是"这条还差什么"，差的部分都在 §1。

| 能力 | 状态 |
|------|------|
| 关键词搜索（title/summary/正文 三路 LIKE） | ✅ 已上线（v0.5.2，FTS5 暂缓） |
| 发布时间范围筛选 | ✅ 已上线（**v0.9.9 换成双月历区间选择器**，P10-A，devlog/050） |
| list 筛选行收纳（状态 / 归档三态 / 时间 → 单钮弹窗） | ✅ 已上线（v0.9.9，`.post-filter-pop` 三分区 + `DateRangePicker`，devlog/050） |
| 类型筛选 / 服务端分页 / 「已归档 N」chip | ✅ 已上线 |
| 候选池收录 / 解订阅连带清帖 / 批量任务 | ✅ 已上线（v0.5.0；解订阅外键回滚已修，devlog/040） |
| 抓取任务优先级（手动 > 定时档） | ✅ 已上线（devlog/040） |
| 抓取调度合并为综合档（24h 数据驱动 + **按平台名单** + 外部批次排队） | ✅ 已上线（devlog/042；预算自适应 054；名单/并发与周期下限 070→078：**全部账号按平台分组，名单间并行 / 名单内串行**，一轮 ≈40~44s、周期 60~70s） |
| 收录 / 添加账号后立即抓取 | ✅ 已上线（devlog/041/044：账号信息可见 6.0s → 0.9s） |
| 外部任务进度胶囊 + 完成后卡片自动刷新 | ✅ 已上线（devlog/044） |
| 顶栏展示策略（自动节拍静默） | ✅ 已上线（devlog/051；状态行视觉定稿 devlog/043） |
| 微博 / B 站增量漏帖（置顶帖打乱流序） | ✅ 已修（devlog/045） |
| 微博扫码登录 / B 站扫码登录 | ✅ 已上线（v0.5.0） |
| 账号统计快照历史（粉丝趋势） | ✅ 采集 + 可视化（**v0.9.x 重写为 ECharts canvas**，recharts 已退役） |
| 档案设置弹窗（背景 / 头像 / 签名 / 已订阅账号，全实时生效） | ✅ 已上线（devlog/066–080）：签名走**来源+覆盖**模型（A3，devlog/074）——平台签名只读、卡片签名 = 覆盖 → 来源账号 → 主账号 → 空；**字段锁定已退役**，旧值由 `vtuber_field_history` 记账（**仅在平台侧覆盖前**，手改不入账）。下拉栏为「内容体内绝对定位浮层」（devlog/075）。**账号信息历史**（曾用值 + 抓取快照）在账号行的历史钮里（R9，devlog/080） |
| 删除检测（墓碑） | ✅ 已上线（v0.5.1：两击判定 + 已删筛选/角标/时间线） |
| 外部固定化数据源 | ✅ 已上线（zeroroku 粉丝历史/礼物日 + danmakus 索引/直播场次/弹幕） |
| 直播日历 + 场次内容管道（danmakus+self 合并、9 类类型 v2、校正 override） | ✅ 已上线（v0.9.0–v0.9.x M1–M4；对外 id 定权见 devlog/052） |
| 场次详情弹窗（信息 / 分类校正 / 弹幕词云 / 直播动态 / 分析预留） | ✅ 已上线；**上游取数已拆成独立请求 + 10 分钟缓存**（devlog/063）；分析服务未接入 → §1.2 |
| 弹幕词云（上游 top40 + 本地自建兜底 + 五态文案） | ✅ 已上线（devlog/061/062）；**原始弹幕明细库暂缓** → §1.2 |
| 滚动条标准（不占宽 + 自动隐藏 + OverlayScroll 统一） | ✅ 定案并落地（UI-MAP F 节） |
| 桌面端图标清晰度（任务栏/资源管理器） | ✅ 已修（devlog/043） |
| 验证基建（pytest / vitest / eslint / ui_probe / 位级签名） | ✅ 已上线（devlog/056/059 + 062–066；基线见 §6.2） |
| 数据导出 / 可移植格式 | ❌ 无 → §1.4（P3） |

---

## 3. 📦 Backlog（远期，按价值排序）

| 方向 | 说明 | 前置 |
|------|------|------|
| 帖子互动历史曲线 | 同 §1.2：`stats_json` 原地覆盖，热帖传播过程会丢；要保留须加 post 统计快照 | 无（越早越不丢数据） |
| 弹幕分析（全量） | 场次级词云已接入；原始弹幕明细库与全量分析（实质是独立产品）暂缓 | 明细存储（§1.2） |
| 涨粉趋势可视化 | ✅ 已上线（v0.6.0 起，v0.9.x 重写 ECharts）；多观察者数据合并后会更稠密 | — |
| P2P 分布式交换 | Phase 1 文件交换（依赖 P3 格式 + 合并语义）→ Phase 2 目录服务 → Phase 3 DHT；需签名 provenance 与隐私 opt-in（贡献即暴露关注列表）；Phase 2+ 可为二创精选提供涌现质量信号 | P3（§1.4） |
| 关注流二创精选（让作品被看见） | 匹配失败而非曝光不足；二创自带收件人地址（关于哪个 V，受众即该 V 的粉）。详见 §5 | stats 历史（P0 已积累） |

---

## 4. 🚫 明确不做（近期）

- **IconRail 占位入口**（日历/邮件视图）：核心价值兑现前不加新面板
  —— 2026-09-08 已删除四枚占位（用户/日历/刷新/设置）与右栏光条邮件占位钮，只保留已接线的「帖子浏览」与四个视图钮
- **多用户 / 云同步**：坚持本地优先定位
- **截图式"证据留存"（页面快照/PDF 存档）**：与现有"结构化字段 + 原始 `raw_json`"路线重复，
  且体积与时序噪声大；若要"证明力"，优先补 §1.2 的原始弹幕明细这类**结构化原始数据**

---

## 5. 🌱 远期构想：让作品被看见（二创发现）

动机：一件优秀作品没被最可能喜欢它的人看见是残忍的。病灶是匹配失败，
不是曝光不足——不需要社区平台，需要的是路由能力。

核心洞察：二创天然自带收件人地址——作品关于哪个 V，受众就是喜欢那个 V 的
人。订阅关系、faction 字段、联合投稿多 V 存储就是现成的路由表。

定位约束：做成 DDtoolkit 内的功能，不做平台。无服务器、无账号、无投票货币。

分阶段形态：

1. **编辑精选周刊**——手工挑选，静态推送，零基建验证需求是否真实存在
2. **统计遗珠信号**——小创作者异常互动率（硬币/收藏对播放比例失衡）自动浮现，
   依赖粉丝基数与帖子统计历史的积累（→ §3 帖子互动历史曲线是前置）
3. **归档涌现信号**——接入 P2P 归档网络后，「多少节点愿意为它花磁盘空间」
   即最诚实的质量票（与 §3 的 P2P 条目合流）

明确不采用：虚拟钱币投票 + 推送权重正反馈（赢家通吃反噬长尾发现；
中心服务器复刻热情退去问题；国内 UGC 合规成本单人不可承担）。

---

## 6. 📊 基线（当前门禁；历史索引在 `docs/ROADMAP-DONE.md`）

> 本节只回答"现在的门禁跑出来是多少"。**已完成条目、已落地需求清单、版本 → devlog 索引
> 都在 `docs/ROADMAP-DONE.md`**，本文件不再重复（2026-09-13 二次整理）。

### 6.1 版本 / 批次 → devlog

> 索引已移入 **`docs/ROADMAP-DONE.md` → 「批次 → devlog 索引」**（2026-09-13 整理：
> 本文件只留"要干什么"与当前基线，历史索引与已完成条目同处一份文件更好查）。

### 6.2 当前门禁基线（2026-09-17 实测 / 复核，R35 批次）

| 门禁 | 命令 | 基线 |
|---|---|---|
| 后端 | `python -m pytest -q` | **546 passed**（2026-09-17 实测；上次记录 503 —— 期间 **R32 体检脚本 5**、**R35 置顶动态 12**、**R37-P2a 卡片布局 15**、**动态标题/正文口径 11**，共 +43。分项：R11 检索 16 + API 5；发布脚本 30；管线复盘 15；未登录能力 24；R13 路由契约 +1；R12a 风控字段 +1；R14a 运行时设置 +24；R14b 偏好 +5；**R18 关闭语义偏好 +1**；**R20 文案扫描（禁 `**` 与反引号）+1 / prefs 白名单 +1**；**R21 导航只有两大类 / 关键项白名单 / 成对不拆散 +3**；**R22 数据目录体检与库维护 7 / 图片缓存上限 5 / 存储接口与缓存全清 6**；**R27 风控冷却与状态契约 12（`tests/test_rate_limit.py`，含"写库→清内存→读回"与"按平台隔离"）**；**R26① 设备指纹 6（`tests/test_auth.py`：名字/冲突优先级/领号与落盘/失败静默）+ R26②③ UA 卫生 5（`tests/test_user_agent.py`：单一来源扫描 / 版本自洽 / 无 `Connection` / 六个消费方同源 / 机器人 UA 跟版本）**；**R28 动态流节流 9（`tests/test_dynamics_backoff.py`：档位/计数/预算真上限/两条接线）**；**R30 静默时段 9（`tests/test_quiet_hours.py`：时段边界与跨午夜 / `START==END` 不生效 / 与空闲档取更保守 / **T0 不受影响的源码级护栏**）**；**R32 `tests/test_perf_report.py` 5（进程树解析纯函数，含"端口取最后一条 boot 行"）**；**R35 `tests/test_pinned_posts.py` 12（集合同步标记/撤销 / 排序与跨页不重复 / 节流窗口与失败不盖戳 / B 站与微博两条循环接线；含文档门禁 `test_doc_check.py` 2 条）**；含 1 条真实网络冒烟，离线环境会 skip） |
| 桌面壳 | `cargo test`（工作目录 `frontend/src-tauri`） | **32 passed**（2026-09-17 实测；上次记录 28 —— 期间 R33 头像/登录态相关、R34 DWM 圆角判据 `dwm_corners_ok` 各补若干。托盘退出判据仍在：`manual_running` 字段识别 / 字段缺失或异常一律当"没在跑"） |
| 未登录能力矩阵 | `python scripts/capability_matrix.py [--include-content] --write` | 两态逐接口实测，fixture 落 `tests/fixtures/capability_matrix.json`；结论：匿名可用 = 检索 / 粉丝数 / 直播状态 / 第三方 / 本地，**内容接口 412 需登录**（devlog/086） |
| 文档漂移 | `python scripts/doc_check.py`（或 `dev_check.py --docs`） | **0 FAIL**（1 条历史警告：41 篇早期 devlog 按批次未逐篇进索引） |
| 上游冒烟 | `python scripts/smoke_upstream.py [--cold]`（或 `dev_check.py --upstream`） | 真上游 **5 ok / 0 FAIL**；冷进程 **3 ok / 0 FAIL**（未登录三态） |
| 前端类型 | `npx tsc --noEmit`（`npm run build` 也会跑） | **0 错** |
| 前端 lint | `npm --prefix frontend run lint` | **0 错**（`--max-warnings 0`） |
| 前端单测 | `npm --prefix frontend run test` | **361 passed**（31 个文件；2026-09-17 实测，较上次记录 266 +70 —— **R31 场景机 `sceneStep` +11**、**R33 头像取值 `avatarSource` +6**、**R36 速览胶囊 `sessionGlance` +6**、**R37-P1 档案视图 47**（网格几何 `layoutModel` 17 · 卡片注册表 `cardRegistry` 4 · 纪念日 `anniversary` 14 · 优质投稿 `topPosts` 9 · 其余既有）。分项：`addVtuberSearch` 7 · `capabilities` 6 · `dialogFoot` 3 · `format` 21 · `reservationDays` 9 · `notificationHub` 12 · `idleQuotes` 18 · `settingsDraft` 22 · `settingsNav` 16 · `theme` 13 · `shellState` 11 · `shellLifecycle` 6 · `trayStatus` 5 · **`sceneStep` 11** · **`avatarSource` 6** · **`sessionGlance` 6** · 其余既有。R35 的置顶渲染、R36 的卡片渲染与 R37-P1 的**画布几何接线**都没进这一层：本仓 vitest 跑 node 环境（无 jsdom），组件渲染由探针守（`--pinned` / `--archive` 的 R36 段 / 默认三档的 `_assert_board`） |
| 词云布局 | `node scripts/check_wordcloud_layout.mjs` | sha256 `19ecc7e673b95c8a1fa7c8c219ada78e9b581a15764aa57b33a7de473fc63dac`（本轮实跑一致 ✓） |
| 布局探针 | `python scripts/ui_probe.py --hero-expect c11548580e73d910ca667047b8120075a4ab121fa3fa098ff6654326ed183666 --vtuber 15`<br>`python scripts/ui_probe.py --archive --vtuber 15`（`--archive-print` 出签名；**R36 起**还连采两格断言弹窗高度零变化 + 速览卡几何）<br>（默认三档还含 **R37-P1 的卡片画布**断言：卡片高度 = 模型算的像素高 / 不重叠 / 不越界 / **窄窗单列** + 卡片内容非空）<br>`python scripts/ui_probe.py --settings --vtuber 15`（两个带签名账号）/ `--vtuber 14`（单账号长签名）<br>`python scripts/ui_probe.py --scene --vtuber 15`<br>`python scripts/ui_probe.py --add-v --vtuber 15`（R11）<br>`python scripts/ui_probe.py --capabilities`（未登录现场）<br>`python scripts/ui_probe.py --polish`（R15 三处打磨）<br>`python scripts/ui_probe.py --reservations`（R13：**探针自己种预约**进数据副本）<br>`python scripts/ui_probe.py --status-island`（R12a/R12b 顶栏状态岛）<br>`python scripts/ui_probe.py --app-settings`（R14a/R14b/R17 应用设置：**会写盘**，跑在数据副本上；`--shot` 出视觉存档）<br>`python scripts/ui_probe.py --filter-pill`（R16 两枚筛选浮片逐项对账 + 三态文字居中/caret 间距）<br>`python scripts/ui_probe.py --tray-suspend`（R18 托盘隐藏停表：可见基线 → 隐藏停表 → 唤回补一轮）<br>`python scripts/ui_probe.py --close-ask`（R20 首次点 ✕ 的询问流程：ask 弹框 → 记住 → 隐藏 → 再点不再问）<br>`python scripts/ui_probe.py --switch-perf --vtuber 15`（**R31/性能基线**：视图与 V 切换的「点击 → 目标可见」分布 + 连点 + 长任务；**测量模式、不是不变量门禁**，只在"没切成"时判失败）<br>`python scripts/ui_probe.py --profile-sync --vtuber 15`（R33：左栏跟随档案设置 + 对照组）<br>`python scripts/ui_probe.py --pinned`（**R35**：置顶帖排在帖子列表第 1 张 + 角标/描边 + 对照帖不挂角标） | 十七条实跑通过（hero 三档签名一致 `c1154858…` / settings 29 项 / scene 提交 250ms / add-v 来源分流 / capabilities 未登录提示 / **polish：标题 700 文本宽 114.3/150、筛选钮文字左右各 31.5px·中心偏移 0·caret absolute、徽标「+」空闲高度 0·不可命中·徽标→分割线 10px、hover 37px 可命中** / **filter-pill：list 那枚与侧栏那枚配方一致（STYLE 逐项相等 / SIZE 各行其是），三态文字居中且 caret 不压字** / **reservations：预约格徽章「预约」+ 计数槽 `128 人预约` + 标题、hover 浮层条目** / **status-island（R19 后）：空闲无容器 + 空闲文案恒为「数据服务运行中」·`data-idle-carousel='off'`·池 8 格（扩展点还在）→ 消息点亮 → 面板可命中不挤动右栏 → Esc 收起 → 入场动画挂上 → ttl 过期自清** / **app-settings（R17 两栏 / R21 分组、折叠、排版与控件）：导航 4 项与后端分组一致（外观 / 抓取设置 / 数据源 / 关于）→ 页内小组 5 个（顺序 = 后端声明序）→ 可见字段**恰好**等于后端非高级集 → 「高级」**默认收起**（收起时 DOM 里 0 行，不是渲染后隐藏）→ 展开后恰好等于后端高级集 8 项 → 换页自动收回 → 两栏几何/命中 → 分页只渲染当前页 → 圆点标对页且切页不丢草稿 → 越界被拦 → 保存回问后端 10→7 → 关于页只读带理由且无可写控件 → 恢复默认回 10 → 主题三卡（深色只标不藏）→ Esc 关闭** ｜ **R21 批 2 量化：排版字段名 14px/600 · 说明 12px · 行内距 9px · 小组标题 13px；步进条高 30 / 内框 0px / 点「+」得 8、点「-」回 7 / 上界「+」置灰而「-」仍可用；开关滑块 32×18 · 圆点 14 · 外壳 border·padding 0 且底色透明 · 有 `--pill-shadow` 投影 · 文案只有「开/关」** ｜ **R21 批 3：二级弹窗页脚 = `.float-pill` + 主操作带 `.on` + 可命中（`--app-settings` 2 钮 / `--capabilities` 去登录 / `--close-ask` 取消 / `--settings` 账号历史各断言自己那个弹窗）** / **tray-suspend：可见 1 次 → 隐藏 0 次（轮播也停）→ 唤回立刻补 1 次** / **close-ask：ask 弹框（两选项+记住）→ 选托盘写偏好并隐藏 → 再点不再问** / **profile-sync：左栏签名/头像与卡片同源、对照组未被污染（R33）** / **pinned：种子帖（2020 时间戳）排第 1 张 + `.post-card-pin` + `is-pinned`、首页只出现一次、对照帖无角标（R35）**）。记录值：`--archive` 日历签名 **`50b78ec0…`**（2026-09-15 20:5x 实测；17:00 那次 `48519bae…` 的差异来自当晚 V15 新落库两场直播 —— **数据漂移、非代码**） |
| 一把梭 | `python scripts/dev_check.py` | **语法扫描**（**109** 个 .py，约 0.2s；补它的原因见 devlog/131：`scripts/` 不被任何其它门禁编译）+ 测试 + 前端静态 + 后端冒烟（详见 `docs/DEV-LOOP.md`） |

> ⚠️ 探针的 `--hero-expect` / `--calendar-expect` 签名**含实时数据**，只适合"改动前后短窗口对比"，
> 不适合当跨天基线；数据一变签名就漂（文档里的哈希值只是当时的记录值）。
> `--calendar-expect` 尤其注意：**日历格「待定/休息」按当天前后翻转**，跨天比对必然失败。
