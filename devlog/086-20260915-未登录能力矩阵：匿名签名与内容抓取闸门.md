# 086-20260915-未登录能力矩阵（P0+P1）：匿名签名、能力表、内容抓取闸门

> 用户：「我希望用户在没有登陆的情况下也可以尽可能使用所有功能，并明确告知这些功能存在限制」，
> 随后定案：「先把 P0 和 P1 执行了，文档最后再来做」。
>
> 本批只做 **P0（把测量固化成护栏）** 与 **P1（后端）**；P2（前端提示）与 P3（文档口径）按用户要求留后。
> 设计原则四条：能力**三态**（不是布尔）· **绝不静默失败** · 未登录**不撞内容接口** · 本地优先叙事。

---

## 一、先量：未登录到底能用什么（这是本批的事实基础）

冷进程（空数据目录 + 显式清空凭据）+ 匿名 WBI 签名，**一个进程只发一条请求**逐项量
（混在一个进程里量是错的：前一条的失败会波及后面，第一版就量错过）。矩阵结果：

| 功能 | 未登录 | 已登录 | 证据（2026-09-15） |
|---|---|---|---|
| 名称模糊搜 `wbi/search/type` | ✅ | ✅ | 匿名 `code=0`，20 条 |
| 账号信息 `space/wbi/acc/info` | ⚠️ 间歇 | ✅ | 一次 `code=0`，另一次 `-352 风控校验失败` |
| 粉丝数 `relation/stat` | ✅ | ✅ | 匿名 `code=0` |
| 直播状态（live 批量） | ✅ | ✅ | 匿名 `code=0` |
| **投稿列表** `arc/search` | ❌ | ✅ | 匿名 `-352` → 重复后 **HTTP 412 `request was banned`** |
| **动态流** `polymer/.../feed/space` | ❌ | ✅ | 匿名**直接** `HTTP 412 request was banned` |
| 第三方 danmakus（场次/弹幕/词云） | ✅ | ✅ | 公开接口（既有 `--upstream` 冒烟） |
| 微博 `ajax/statuses/mymblog` | ❌ | ✅（微博登录） | 匿名 `302 passport.weibo.com/visitor` |
| 本地归档（浏览/搜索/筛选/归档/日历/趋势） | ✅ | ✅ | 纯本地，零上游 |

两条关键细节：**`-412` 是 IP 级、会持续**（换新进程、隔 40 秒再打仍 412）；
**它不连累登录态**（同一时刻登录态 `arc/search` 返回 `200 / code=0 / 5 条`，已实测）。

**结论**：目标要分两半 —— "未登录用全部功能"做不到（内容抓取是平台策略，不是我们的实现），
但"本地全功能 + 轻量在线信息 + 检索"这一大块能拿到，而且**不该让用户去撞 412**。

---

## 二、P0：把测量变成常驻护栏

### 2.1 `scripts/capability_matrix.py`（新）

两态（`anon` / `login`）× 逐接口测量：

```powershell
python scripts/capability_matrix.py                      # 两态 × 轻量接口，打印矩阵
python scripts/capability_matrix.py --include-content    # 额外量投稿/动态（有代价，别勤跑）
python scripts/capability_matrix.py --write              # 写 tests/fixtures/capability_matrix.json
```

纪律写在脚本头里：**一个进程只发一条请求**（`--probe <name>` 子进程模式）、
冷态**显式清空凭据**、默认不量内容接口（会触发 IP 级 412）、不重试、条间 `--gap`。

### 2.2 `tests/fixtures/capability_matrix.json`（新）

真机测量快照（含 `captured_at`、每条 HTTP/code/message/条数/耗时）。它是**测量**，
`capabilities.py` 是**策略**，两者由用例双向约束（见 §三.3）。

---

## 三、P1：后端

### 3.1 `wbi`：匿名签名（使能件）

```python
await wbi.get_wbi_keys(allow_anonymous=True)   # nav 回 -101 但带 data.wbi_img 时照常取键
await wbi.sign_params(params, allow_anonymous=True)
wbi.wbi_status()          # {"cached": bool, "anonymous": bool|None} —— 能力矩阵/诊断用
```

匿名取到键时记一行日志；`allow_anonymous=False`（默认）**行为逐字不变** ——
抓取路径靠它继续"未登录就快速明确地失败"。启动预热改为匿名允许（未登录也能搜）。

### 3.2 `app/services/capabilities.py`（新）：能力表 + 闸门

- `FEATURES`：每条 = `id / label / platform / anon_state / anon_note / login_note / evidence`，
  三态 `full` / `degraded`（能用但完整性或稳定性打折）/ `requires_login`；
- `snapshot(bili_logged_in, weibo_logged_in)` → `GET /capabilities` 的返回：
  `features` + `limited`（前端只读这个就能渲染提示）+ `wbi` 状态 + `measured_at`；
- `content_fetch_allowed()` → `(bool, 原因)`：**内容抓取闸门**（未登录返回 False + 一句人话）；
- `weibo_available()`：沿用 devlog/079 的"微博未登录整条名单跳过"口径。

### 3.3 策略 vs 实测的**双向契约**（`tests/test_capabilities.py`）

- **R1**：实测匿名可用 ⇒ 策略**不得** `requires_login`（不许白白限制用户）；
- **R2**：全部探测被**硬拒**（HTTP 412 封禁，非 -352 软风控）⇒ 策略**必须** `requires_login`。

这样平台哪天放宽/收紧，用例会先红，逼我们重测后再改承诺，而不是让承诺悄悄失真。

### 3.4 闸门落地（未登录**不发请求**，而不是"试了失败"）

| 位置 | 行为 |
|---|---|
| `scheduler.async_fetch_posts` / `async_fetch_first_screen` | 未登录**入口即返回** `PostFetchResult(stop_reason="login_required")` |
| `scheduler._lane_skip_reason("bilibili")` | 动态流名单整条跳过（与微博名单同一套模式） |
| 5 个内容端点（`fetch-posts` / `fetch-all-posts` / `update-posts` / `batch/*`） | **403** + 原因（不再打了才知道） |
| 账号信息类端点（`/vtuber/fetch`、`/{id}/fetch`、建账号） | **不挡**（实测匿名可用） |
| `bili_search.search_users` / `exact_user` | 放行匿名签名；`exact_user` 把 tenacity 的 `RetryError` 归类为 `upstream_degraded`（"平台拒绝了本次查询"），**不再冒充签名故障** |

---

## 四、验证（本批实跑）

| 项 | 结果 |
|---|---|
| pytest 全量 | **400 passed**（+24：capabilities 9 / wbi 匿名 6 / 内容闸门 5 / 端点 3 / 检索接线 1） |
| 反向验证 | 拆掉 `async_fetch_posts` 的闸门 → `test_..._without_touching_network` 立刻红（"未登录不该发起内容抓取请求"），恢复后绿 |
| 冷进程冒烟 | `smoke_upstream.py --cold` **4 ok / 1 skip / 0 FAIL**：匿名**能搜到 20 条**、内容端点 **403 拒绝**、`/capabilities` 如实列出受限项、匿名 uid 直查被上游拒绝时**报 503 而不是 404** |
| 暖态冒烟 | `smoke_upstream.py` **5 ok / 0 FAIL**（登录态行为不受影响 —— 这是本批的硬约束） |
| 能力矩阵 | `capability_matrix.py --include-content --write` 两态实跑，fixture 落地 |

---

## 五、P2 前端：**只标不藏**

| 位置 | 改法 |
|---|---|
| `utils/capabilities.ts` | 纯逻辑（`limitOf` / `isLoginRequired` / `limitsSummary` / `loginActionLabel` / `availableSummary`）+ 6 条单测（"判定错了界面不报错，只是说错话"） |
| `hooks/useCapabilities.ts` | 模块级缓存（TTL 60s，多组件共用一个请求）+ `ddtoolkit:capabilities-refresh` 事件；读不到就退化成不提示，绝不把主界面搞挂 |
| `components/CapabilityLimits.tsx` | 顶栏入口（**没有限制就不渲染**）+ 说明窗：**先列"现在能做什么"再列受限项**，每条带后端 `note`，底部「去登录」复用原 LoginDialog |
| `BatchFetchDialog` | 内容类两项**禁用 + 标「需要登录」+ 说明原因**；账号信息与归档**保持可用**（实测匿名可用） |
| `AddVtuberDialog` | 一行受限说明（"能搜能收录，投稿与动态要登录后抓"），**搜索与收录照常** |
| TopBar | 登录浮窗关闭即 `refreshCapabilities()` —— 刚扫码成功的用户不该还看到"未登录受限" |

口径：**受限功能照常可见**，只标注不隐藏；说明窗**不只列不能做的**（那等于劝退）。

### P2 验收：`ui_probe --capabilities`（新，现场 = 数据目录副本删 `.env`）

```
顶栏入口=True 文案='未登录 · 2 项受限'
说明窗=True 能用项=6 受限项=2 ['fetch_posts','weibo_content'] 去登录=True '登录 B 站 / 微博'
添加 V：浮窗=True 受限提示=True 仍能搜出=37 行（可点 37）
批量浮窗=True 全量帖子禁用=True(needs_login=1) 更新未归档禁用=True 归档可用=True 账号信息可用=True
```

它同时盯**两条相反**的错法：该说的没说（入口/说明窗/去登录缺失）、**过度限制**
（功能被藏起来、或归档/账号信息被一起禁掉）。
**反向验证过**：临时让 `limitsSummary` 返回空串（入口不渲染）→ 探针立刻红并指名
"顶栏没有「未登录 · N 项受限」入口"，恢复后绿。

---

## 六、P3 文档口径

- **GLOSSARY**：§8 新增第 14 条（未登录 ≠ 不可用，但内容抓取必须登录；闸门与实测矩阵）；
  术语补「能力矩阵 / capabilities」「内容抓取闸门」「未登录提示 / 能力角标」。
- **ARCHITECTURE**：§3.5 风控表补一行"匿名调用被硬拒"，新增 **§3.9 未登录能力矩阵与内容抓取闸门**
  （矩阵表 + 三条实现纪律）。
- **UI-MAP**：新增 **§A1-b 未登录能力入口**（DOM 契约：`.topbar-limits` / `.cap-limits-dialog` /
  `.cap-limits-ok` / `.cap-limits-item[data-limit-id]` / `.cap-login-cta` / `.cap-need-login` /
  `.cap-inline-hint`）。
- **DEV-LOOP**：新增 §二·八（`capability_matrix.py` 三条纪律 + `ui_probe --capabilities`）。
- **backend-repositories-and-routers**：路由数 52→**53**（操作 54→55）、vtuber.py 48→**49**；
  新增 `/capabilities` 行；5 个内容端点标注"未登录 403"；`/vtuber/bili/search` 与 `/adopt`
  改为"未登录也能用/首屏内容跳过"的新口径。
- **TODO**：§1.1 该条改 ✅ 已落地（含四块落地内容）；§6.2 基线更新（pytest 400、vitest 144、
  新增能力矩阵与 `--capabilities` 探针两行）。
- **ROADMAP-DONE**：新增「未登录可用范围（guest mode）」小节（矩阵 + 落地四块 + 验收数字）。

> ⚠️ **`docs/releases/v1.0.1.md` 不改**：那份说明写"添加 V 的 B 站直查需要 B 站登录态"，
> 对 **v1.0.1 那个构建是准确的**（匿名签名是本批才落地的）。改它等于改写已发布的历史 ——
> 新口径记在本 devlog、TODO 与下一版发布说明里。

---

## 七、遗留（有意不做 / 留给用户）

- **P2 的顶栏入口是新控件**，而用户已提新需求"顶栏胶囊 → 统一通知 / 类灵动岛多功能控件"：
  届时这个入口与说明窗**直接吸收过去**（组件自包含，DOM 契约在 UI-MAP §A1-b，不依赖顶栏结构）。
- **匿名 `acc/info` 的间歇性风控**没法从我们这侧消除（平台行为），所以能力表标 `degraded`
  并在文案里说清"可能失败、会如实报错"；不做自动重试（会加重风控）。
- **不做**：伪造/共享 cookie、游客 cookie 池、把匿名失败伪装成"没数据"。
