# 093-20260915-R16 list 筛选钮跟随侧栏筛选钮（两张截图的量化对齐）

> 用户需求（2026-09-15，附两张截图）：「list 视图中的筛选按钮的样式跟随左栏工具栏中的
> 筛选按钮」。截图上：左栏那枚（`.list-filter-btn`，文案「默认」）是**参照**，
> list 视图那枚（`.pfilter-btn`，文案「筛选」）是要改的。
>
> 这条需求是**纯视觉**的，所以这一批的重点不是"改 CSS"，而是**把"像不像"变成可判定的东西**。

---

## 一、先量现状：两张截图能说明"像不像"，说明不了"差在哪"

新建探针 `ui_probe --filter-pill`：把两枚浮片的样式逐项量出来对账。
第一次运行（改之前）的输出就是这张差异表：

| 字段 | 侧栏（参照） | list 视图（改前） |
|---|---|---|
| 尺寸 w×h | 89×**25** | 68×**30** |
| 字号 / 行高 | 13px / 13px | 12.5px / 18.75px |
| 内距 | `0 8px` | `0 12px` |
| caret 定位 | **absolute**（右上 3px，不占流） | static（在流内，gap 4px） |
| caret 距文字 / 距右缘 | 21.9 / 3 | 4 / 16.2 |
| caret 垂直偏移 | −6.2（偏上） | 0（居中） |
| **文字中心偏移** | **0** | **−5.3** |
| 文字两侧留白 | [31.5, 31.5] | [16.2, 26.8] |

**关键发现**：参照那枚的文字偏移是 **0**（真的是几何中心），而 list 那枚偏 **−5.3px** ——
这正是用户从 R5 到 R15 反复说"文字没居中"的**根因**：caret 只要留在流内，就必然挤占文字的位置。
R15 当时把 caret 留在流里、让"文字+箭头"这一组居中，文字本身仍偏 5.3px；侧栏那枚的做法更彻底 ——
**caret 出流（绝对定位到右上角）+ 浮片比文字宽出一段**，于是文字就是中心，caret 落进右侧留白里。

（顺带纠正 R5 的一次错误解法：那时给 list 那枚加了**非对称内距** `0 16px 0 10px`
"给 caret 让位"，而非对称内距本身就会把文字推偏 —— 正确解法是对称内距 + 足够宽度。）

## 二、改法：逐项对齐

`PostFilterPop.tsx` 去掉 `size="md"`（高度回到 `--pill-h-sm` = 25），`posts.css` 里
`.pfilter-btn` 改成与 `.list-filter-btn` 同一套配方：

```css
.pfilter-btn { min-width: 89px; padding: 0 8px; font-size: 13px; line-height: 1; }
.pfilter-btn .pf-label { padding: 0 11px; white-space: nowrap; }   /* 见下 */
```

并删掉 R15 加的 `.pfilter-btn .pill-caret { position: static }` 覆盖 —— 共用 `.pill-caret`
的绝对定位（`top/right: 3px`）。改完探针逐项一致（唯一差异是 `minWidth`：侧栏是**定宽** 89
的工具行格子，list 那枚用 `min-width` 好让「筛选 · N」能长出去；`w` 在等长文案下相等）。

## 三、超长文案那一测，逼出了 `.pf-label`

改完第一版就绿了，但探针里那条"造一个超长文案"的边界测**红了**，而且红得有道理：

```
超长文案：文案='筛选 · 12 项' 宽=89 文字中心偏移=0 文字两侧=[10.2, 10.2] caret 距文字=0.6
```

caret 占右缘 `3px` 起、宽 `6.63px`；文字两侧各剩 10.2px 时，caret 几乎贴到字上（0.6px）。
根因：**内距只有 8px，比 caret 的落脚区还窄** —— 短文案靠 `min-width` 富余量遮住了这个问题，
长文案才暴露。参照那枚之所以没事，只是因为它的文案短（「默认」）且宽度是写死的 89。

修法（不是放水改判据，而是**从构造上消掉**）：文案外套一层 `.pf-label`，给**左右各 11px 对称留白**：

- 对称 ⇒ 文字仍在浮片几何中心（改完三态实测偏移都是 0）；
- 这层留白同时把 caret 的落脚区让出来 ⇒ 长文案下 caret 与文字恒留 **~9.4px**（实测）；
- 短文案仍由 `min-width: 89px` 兜住 ⇒ **渲染结果与参照那枚逐项相同**（探针对账仍全绿）。

一个真实文案够不到的边界，就这样变成了构造上的保证。

## 四、护栏（这一批的产物）

| 层 | 内容 |
|---|---|
| 新探针 `--filter-pill` | ① 两枚浮片**逐项对账**（尺寸/字号/行高/内距/圆角/颜色/斜切/caret 定位·inset·尺寸·垂直偏移/文字中心偏移/文字两侧留白），唯一豁免 `minWidth` 并写明理由；② 三态（静态「筛选」/ 真实最长「筛选 · N」/ **合成超长文案**）：文字偏移 \|≤1\|、caret 距文字 ≥6px、文字两侧留白 ≥7.5px、caret 必须 `absolute` + `3px/3px`；③ 超长文案必须把浮片**撑宽**（证明 `min-width` 是下限不是定宽）；④ 断言探针**自己复位**了筛选（不留痕） |
| `--polish` 断言更新 | R15② 的三条被 R16 取代：文字中心偏移从"允许 ≤6px"收紧到 **≤1px**；caret 从"必须 static"改为**必须 absolute**（留在流内就会把文字挤偏）。实跑：`文字左/右=31.5/31.5 差=0 文字中心偏移=0 ｜ caret position=absolute`（改前是 `−5.3` / `static`） |
| 视觉验证 | `--shot` 存档截图 → 裁两枚浮片并排比对（2× DPR，3× 放大）：形状/斜切/字号/**文字居中**/caret 落角**一致**；主探针三档 `问题=0`（宽度 68→89 没有撑出滚动条或出窗元素） |

## 五、门禁

tsc 0 · eslint 0 · vitest 201 passed（未改前端逻辑，只改样式与 DOM 包一层）·
pytest 431 passed · doc_check 0 FAIL · **十二条探针全绿**（新增 `--filter-pill`）·
hero 签名与词云 sha 未变。

## 六、留待用户看一眼

- list 那枚现在与侧栏那枚**一样宽（89px，原 68）**，工具行因此右移 21px：窄窗下
  "类型 chips" 与 "搜索+筛选" 两行的换行时机比原来早一点点（P10-A 那套换行兜底照旧，
  主探针三档无出窗/无滚动条）。若希望行内更紧凑，可把 `min-width` 收小（代价：长文案时
  浮片会先长再补留白，宽度变化更频繁）。
- 它的高度从 30 降到 25（跟随参照），于是与**同一行**的搜索框（30px）不再等高。
  这是"跟随侧栏那枚"的必然结果（侧栏那行是 25+25 等高的）；如果更在意行内等高，
  说一句就把这一行的搜索框也压到 25px。
