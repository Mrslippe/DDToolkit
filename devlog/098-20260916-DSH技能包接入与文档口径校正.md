# 098-20260916-DSH 技能包接入与文档口径校正

> 起因（2026-09-16）：本机改用 DSH（DeepSeek Harness）作为编码 agent。原先放在 `.opencode/skills` 的
> 10 个设计类技能**不会被 DSH 加载**（DSH 只扫 `.dsh/skills` 等根）。借这次迁移，把项目专属的工程知识
> 也做成技能包，并顺带把长期无人复核的**数字类口径**做了一轮校正。

## 一、接入 DSH 项目技能（12 个，本地不入库）

**扫描根**（`dsh-skill-filesystem`）：`<项目>/.dsh/skills`、`<项目>/.agents/skills`、`~/.dsh/skills`、
`~/.agents/skills` + 配置自定义目录；**不含** `.opencode/skills` —— 所以原技能在 DSH 下等于不存在。

| 动作 | 说明 |
|---|---|
| 迁移 | `.opencode/skills` 的 10 个技能**复制**到 `.dsh/skills`（复制而非移动，opencode CLI 侧不受影响） |
| 新增 | 2 个项目专属技能，从 `docs/` 提炼，只做"路由 + 清单 + 指向源头"，不复制整篇文档 |
| 热发现 | 技能目录有 watcher，新增后**无需重启**即进入会话技能目录（实测 10 → 12 当场生效） |
| 格式 | frontmatter 必需 `name` + `description`（缺任一被**静默忽略**，只留一行 warn）；`description` 写"什么时候用"，模型靠它匹配 |
| 归档 | `.dsh/` 与 `.opencode/` 同类（本机 agent 工具链），已加入 `.gitignore` |

两个新技能：

| 技能 | 覆盖 | references |
|---|---|---|
| `ddtoolkit-conventions` | 后端抓取 / 数据层 / 迁移 / 路由：分层纪律、**15 条不变量速查**、改动→验收命令矩阵、10 条高危清单 | `invariants.md`（15 条完整表述 + 与 `GLOSSARY` §8 那组 14 条的差异对照）· `verification.md`（逐字命令全表）· `change-recipes.md`（扩展点与分步配方） |
| `ddtoolkit-docs-devlog` | devlog 命名与编号、活文档映射、TODO↔ROADMAP-DONE 双向维护、`doc_check` 门禁、发版 6 锚点 | `devlog-template.md` · `doc-map.md`（含已知漂移清单）· `release-checklist.md` |

> 两个技能各自加了一行路由指向对方，避免"代码侧不变量"与"文档纪律"职责重叠。

## 二、文档校正（8 处 + 探针清单补收录，全部先用真源复核再改）

原则：**能指向权威源的就不要再写一遍数字**。路由数这类漂移的根因就是同一个数字散落在多处，
各改各的、越改越乱。

| # | 位置 | 旧 | 新（真源） |
|---|---|---|---|
| 1 | `docs/README.md` §1（ARCHITECTURE 描述） | 10 条不变量 | **15 条**（`ARCHITECTURE.md` §6 实际条数） |
| 2 | `docs/README.md` §2 · 根 `README.md` 目录树 · `ARCHITECTURE.md` §1 图 | "54 个 HTTP 操作" / "52 个路由 / 54 个操作" | 改为**指向 `backend-repositories-and-routers.md` §3** 的路由计数三口径，不再复述数字 |
| 3 | `RELEASE.md` §2 标题 · `docs/README.md` §3 · `GLOSSARY.md` §6/§7 | 版本号"5 处" | **6 处**（真源 `release.py::VERSION_FILES` 6 项；同节正文与 `--check-version` 本来就是"六处"） |
| 4 | `GLOSSARY.md` §7 `VERSION` 默认值 | `1.0.0` | **`1.0.2`**（`app/core/config.py` 实读） |
| 5 | `GLOSSARY.md` §5（UI 探针） | 7 组不变量 | **八组**（`DEV-LOOP.md` §二·五 的 8 行表） |
| 6 | `DEV-LOOP.md` §二 `--full` | `= --frozen --portable` | **`= --frozen --portable --docs --upstream`**（`dev_check.py` 的 help 文本与代码一致） |
| 7 | `DEV-LOOP.md` §二 pytest | "266 个用例" | 改为**只指向** `docs/TODO.md` §6.2（真值 **434**；§6.2 当时自己还写着 432，099 批次一并修正） |
| 8 | `RELEASE.md` 头部「最近发布」+ §5 输出示例的 release id | v1.0.0（id `388158299`） | **v1.0.1**（2026-09-15，id **`389029336`**，真源 `TODO.md` §1.3）。示例里那个 id 实为 **v1.0.0** 的（`devlog/082`：notes 取自 `v1.0.0.md`），已按 v1.0.1 更正，示例块内部恢复自洽 |

另：`DEV-LOOP.md` §二·五 的探针命令清单**补收录 `--close-ask`**（R20；`TODO.md` §6.2 已收录、此处遗漏）。

同时把两处**实测数字**留在了技能的 `references/doc-map.md`「已知文档漂移」一节，供后续 agent 参考，
避免下一个人再把旧数字抄回来。

## 三、未处理（留档）

- `TODO.md` §0 让 R19/R20「详见 `ROADMAP-DONE.md`」，但该文只有索引行、没有对应需求清单章节
  —— 用户 2026-09-16 明确**暂缓**，留待后续批次补章节。

## 四、门禁

`python scripts/doc_check.py` —— **[ok] 0 FAIL（1 条历史警告：41 篇早期 devlog 按批次未逐篇进索引）**。
新增一篇 devlog 后已回填 `docs/ROADMAP-DONE.md`「批次 → devlog 索引」的裸编号行，否则
**最近 5 篇**那条会 FAIL。

本批只动文档与 `.gitignore`（**无代码改动**），代码侧门禁沿用 R20 批次实测：
`pytest` **434 passed** / `tsc` 0 / `eslint --max-warnings 0` 0 / `vitest` **241 passed** /
`cargo test` **2 passed** / 探针 **14 条**实跑通过。

> ⚠️ **本批自己踩了一次"抄旧数"**：§二 表格第 7 行把 `DEV-LOOP.md` 的 pytest 数从 `266` 改成
> `TODO.md` §6.2 里的 **432** —— 而 §6.2 自身在 R20 加了 2 条用例后没更新（真值 **434**）。
> 修法是**双层**：① `TODO.md` §6.2 改成 434（它是唯一真源）；② `DEV-LOOP.md` **不再复述数字**、
> 只写"基线数字只在 §6.2 维护"。这正是 §一 那条原则（能指向权威源就别再写一遍数字）的自我应用。

> ⚠️ **教训**：`doc_check.py` **只查索引类同步**（devlog 索引 / 六处版本号 / 发布说明导航），
> **不查数字类口径**（表数 / 路由数 / 用例数 / 不变量条数）。这类漂移没有任何自动门禁兜底，
> 只能靠"改到就顺手复核"。本轮 8 处**没有一处**能靠门禁发现，最久的一处（`VERSION = 1.0.0`）已落后两个版本。
