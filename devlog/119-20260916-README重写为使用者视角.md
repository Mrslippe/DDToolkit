# 119-20260916-README 重写为使用者视角

> 用户 2026-09-16："先把 README 给润色成普通人能够看懂的版本，类似你随便在 GitHub 上找个项目参考，
> 反正排版更美观，使用者更好理解，标注出最主要的功能就行"。
> 配图问过一次 → 用户选**不要图、纯文字排版**，所以本批没有任何截图资产。

---

## 一、结构：从"开发者速查"改成"使用者先看懂"

旧 README 是**给维护者看的**：目录结构 / 快速开始（`tauri:dev`）/ 数据目录约定 / 打包发布 排在最前，
"这软件能干什么"要靠读者自己从"定时抓取 B 站与微博的动态、账号统计"一句话里推导。

| 新结构 | 给谁看 | 关键变化 |
|---|---|---|
| 一句话定位 + 徽章 | 所有人 | 从"个人向 VTuber 帖子 / 账号证据归档工具"改成"给你推的 VTuber 建一个不会消失的档案库"，并点明**数据在本地、不经过服务器** |
| **主要功能**（11 行表） | 使用者 | 旧版没有这一节。"删了也留证"排第 2（**立项理由**应该在最前面），并把托盘常驻 / 应用内更新 / 账号信息留痕这些 R22–R23 才有的能力补上 |
| **下载与安装** | 使用者 | 旧版把"打包发布"（维护者命令）当成了下载说明。新版给**安装版 / 便携版对照表** + 系统要求 + 为什么几十 MB（Python 运行时打进去了）+ SmartScreen 提示 |
| **第一次打开** | 使用者 | 新增：启动幕 → 扫码登录 → 添加 V → 等首轮抓完 → 收进托盘，五步 |
| **界面上都有什么** | 使用者 | 新增：左栏 / 顶栏 / 图标栏 / 三个视图 / 设置四类，用"在哪能找到什么"回答 |
| **数据放在哪** | 使用者 | 旧版是"数据目录约定（重要）"，只给了一张运行方式→路径的表；新版补**迁移到其他盘**、缓存上限、实测体积（160MB）与低空间提示 |
| **常见问题**（7 条，默认折叠） | 使用者 | 新增：C 盘会不会爆 / 关窗还在抓吗 / 检查更新为什么失败 / "已删除"什么意思 / 不登录差在哪 / 端口与防火墙 / 日志在哪 |
| **数据从哪来** | 使用者 | 保留四个来源表，删掉端点清单（那是深度文档的内容） |
| **从源码运行**（折叠） | 开发者 | 旧的"快速开始 / 目录结构 / 打包发布"整块收进 `<details>`，顶部不再被命令占据 |
| **文档索引** | 开发者 | 从列表改成表格，补 `FRONTEND-ARCH.md`，行数/用例数等会漂的数字改成指向权威源 |

## 二、写的过程中逐条回真源核对，改对 6 处（都是"看起来对、其实相反"）

| # | 我第一版怎么写的 | 真源 | 事实 |
|---|---|---|---|
| 1 | "已删帖子默认不进普通列表" | `PostsPage.tsx`：`is_deleted: deletedOnly \|\| undefined`；`vtuber_repo.py`：`None = 全部` | **默认照常出现在列表里**（带墓碑标记），「筛选 → 状态」可以只看已删 |
| 2 | "托盘图标唤回或退出" | `lib.rs::build_tray` + `show_menu_on_left_click(false)` | **左键唤回**、**右键菜单**（显示主界面 / 后台运行中 / 退出） |
| 3 | "安装版需要管理员权限"（想当然） | `tauri.conf.json`：`nsis.installMode = currentUser` | **用户级安装，不需要管理员** |
| 4 | "便携版的数据跟着解压目录走" | `datadir.rs`：`portable = true` 仅在**设了 `DDTOOLKIT_DATA_DIR`** 时成立 | 便携版默认同样写 `%APPDATA%\com.ddtoolkit.app`（105/106 已有先例，这次没再踩） |
| 5 | 差点把"深色主题"写进功能表 | `UI-MAP` §A2-b：`DARK_IMPLEMENTED = false` | 深色**尚未实现**（外观页那张卡片是禁用 + "尚未实现"），README 不提 |
| 6 | `python scripts/dev_check.py` = "静态检查 + 文档门禁" | `dev_check.py` 的 argparse | 它是**开发态后端链路自检**，文档门禁要**额外带 `--docs`** |

另外两处按"丑话说在前面"处理：**「档案卡」视图是占位页**（改版中）照实写一句，避免用户点了以为坏了；
**检查更新当前必然失败**（GitHub 上还没有 Release）写进 FAQ 并说明错误信息会区分网络与远端。

## 三、文档口径：补齐 devlog/098 漏掉的两处旧数字

098 定的口径是"**能指向权威源的就不要再写一遍数字**"，当时改了 `docs/README.md` §2、
根 README 目录树与 `ARCHITECTURE.md` §1 图，但**漏了两个文件**，这次一并扫掉：

- `docs/backend-repositories-and-routers.md` 头部摘要：`54 个 HTTP 操作` → **指向本文 §3 的三种数法**；
- `docs/ARCHITECTURE.md` §0 阅读路径里同一处旧数字 → 同上。

新版 README 自己也遵守这条：目录树与文档索引里不再写 `58 / 69`，只指向 `§3`。

## 四、门禁

- `python scripts/doc_check.py` → **0 FAIL**（1 WARN：devlog 索引的历史缺口，长期存在）；
- 发版锚点完好：README 里 `version-<x.y.z>-` 只出现**一次**（那枚徽章）。
  `release.py::set_version` 是 `count=1` 的定点替换 —— 多一处就**静默漏改**，所以这条要当护栏看；
- 本批无代码改动，未跑 pytest / vitest / cargo。

## 五、下一步

README 只等 GitHub Release 建好即可对外（下载链指向 `releases/latest`）；
建 Release 仍是用户侧动作：`$env:GITHUB_TOKEN` 后 `python scripts/release.py 1.0.2 --from release`。
