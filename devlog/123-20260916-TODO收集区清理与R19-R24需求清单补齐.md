# 123-20260916-TODO 收集区清理与 R19–R24 需求清单补齐

> 用户 2026-09-16："更新 todo，完成了的从待提需求中移除"。
> 这不是简单地删几行 —— **§0 里 R18–R24 的原文只存在于那张表里**（`ROADMAP-DONE.md` 当时
> 只有索引行、没有需求清单章节，正是 devlog/098 §三 留档的那条缺口）。
> 所以本批是「**先迁后删**」：先把原文与落地结论搬进 `ROADMAP-DONE.md`，再清掉 §0 的已落地行。

---

## 一、先补缺口：`需求清单：R19–R24`

`ROADMAP-DONE.md` 原有「需求清单：R11」「R12–R17」「R18」「R1–R10」四个章节，
**R19–R24 六条一条章节都没有**（devlog/098 §三 记过："用户 2026-09-16 明确暂缓，留待后续批次补章节"）。
本批按 R18 那一节的同款格式补齐：

- **原文照抄**用户写法（"一句话需求 / 期望效果"），一行一条，别的不改；
- **落地结论**按批次写全（R21 三批 / R22 九个 devlog / R23 四批 / R24 收窄口径），
  并把当时的**关键约束**一起留在表里 —— 例如 R24 的"隐藏后降频**不做**"（这是 R25 推送时效的前提）、
  R23 的"端到端验证要等第一个 Release 建好"；
- 顺手把该节上方那句交叉引用改准：R12–R17 节的"受理记录见 `docs/TODO.md` §0 的**历史行**"
  → 改指 §0 的「**编号说明**」（历史行已经被本批删掉，不改就成了悬空引用）。

## 二、再清 §0：只留未落地项

`docs/TODO.md` §0 的表从 8 行（R18–R25）变成 **1 行（R25）**：

- 删掉 R18–R24 七行（它们的状态本来就是 `✅ 已落地（devlog/…）→ 详见 ROADMAP-DONE.md`）；
- 上方的汇总说明从"R12–R19 已全部落地（R18/R19 **见上表**）"改成
  "**R12–R24 已全部落地，并已按本文件规则移出本表**"，并把每条的 devlog 号一并列出 ——
  这样 §0 仍然能一眼看到"哪条需求在哪篇 devlog"，但不再堆着已完成的条目；
- 编号模板那行（`下一条怎么写`）里的示例从 `R12` 改成 **`R26`**（R25 是当前最后一条），
  并把"把上面那行占位删掉"改成"编号接在最后一条之后"（表里已经没有占位行了）。

> ⚠️ 这条口径本来就在本文件的「用法」里写着 —— `已落地` = **移进 `docs/ROADMAP-DONE.md`
> 的「需求清单」章节**（原文照抄 + 落地结论 + devlog 指向）。R18–R24 当时只做到"改状态"，
> 没做"移"，于是 §0 越堆越长、`ROADMAP-DONE` 却缺章节。本批把欠的搬家补齐。

## 三、搬家后各处的现状（复查过）

| 位置 | 现状 |
|---|---|
| `TODO.md` §0 | 表内只剩 **R25**（待排期）；R12–R24 与 R1–R11 各有一行汇总说明指向 `ROADMAP-DONE` |
| `TODO.md` §1 | 未完成项四组不动（1.1/1.2/1.3/1.4 都是"还没做的"） |
| `ROADMAP-DONE.md` 需求清单 | R1–R10 / R11 / R12–R17 / R18 / **R19–R24**（本批）—— 五节覆盖 R1–R24 全部 |
| 批次 → devlog 索引 | 不动（本批无新批次编号，索引行加在文末，编号 `123`） |

## 四、门禁

`python scripts/doc_check.py` → **0 FAIL**（1 WARN：devlog 索引的历史缺口，长期存在）；
改动文档的**表格列数自检 0 异常**（本批大段改表，这条比 doc_check 更能抓住串列）。
本批只改文档，未跑 pytest / vitest / cargo。
