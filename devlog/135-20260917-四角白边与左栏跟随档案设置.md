# 135-20260917-四角白边与左栏跟随档案设置

> 用户报两条（各附截图）：
> **①** 「窗口的四个角上还是有几个像素点的白边，虽然我只截了一个角但四个角都有」；
> **②** 「如果在 card 视图右上角自定义弹窗中设置过签名和头像，那么 vtuber 左栏应该也对应
> 这个签名和头像」。
> 两条都不是"加功能"，而是**两处早就该一致却没一致**的地方 —— 修法都是"把口径收到一处"。

---

## 一、四角白边：白色来自 **WebView 自己的背景**，不是 DWM

### 取证（这一步花了力气，但没它就只能猜）

`.app-shell` 只有 4px 圆角（`--radius-window: 4px`）且**没有任何 border/box-shadow**，
所以"白边"只可能是圆角抗锯齿像素**背后那一层**的颜色。做法：把窗口挪到**第二块显示器**
（第一块被 DSH Desktop 自己的窗口压着，截出来的是别人的窗口 —— 第一版"证据"就是这么作废的），
置顶后按屏幕坐标逐像素读四角：

| | 角上那 1~2 个像素 | 说明 |
|---|---|---|
| **修前**（rail 底色 `#4B5A6F`） | `#657183` / `#727B8A` | 比底色**更亮**，且**偏冷（蓝灰）** |
| **修前**（顶栏粉 `#FFA2B4`） | `#FDB3C1`（G 179 vs 粉 162） | 粉里**混进了白** |
| **修后** | 与桌面同温的暗色（`#675250` 这类） | 只是正常的抗锯齿 |

两处都能用"**底色按 ~85% alpha 压在白底上**"精确复现
（`#FFA2B4`→`#FFB2C1`≈实测 `#FDB3C1`；`#4B5A6F`→`#667384`≈实测 `#657183`）
⇒ 圆角抗锯齿像素背后是**一层白**。

**先把 DWM 排掉**：Win11 会给窗口画圆角与 1px 描边，本是头号嫌疑。
用 `DwmSetWindowAttribute`（`CORNER_PREFERENCE=DONOTROUND` + `BORDER_COLOR=NONE`，
并补 `SWP_FRAMECHANGED` 强制生效）改完**逐像素数值一字不变** ⇒ 与 DWM 无关。
（顺带确认：窗口左缘**中段**是纯底色、没有亮线 —— 亮线只出现在 4px 圆角那一段。）

### 修法

`transparent(true)` 只让**窗口**透明，**WebView 自己的背景仍是白色**（Tauri 2.11 的默认行为）。
所以两处补上"连 WebView 一起透明"：

| 位置 | 改动 |
|---|---|
| `lib.rs::setup()` | `w.set_background_color(Some(tauri::window::Color(0, 0, 0, 0)))` —— 该 API **同时**设窗口与 WebView 两层 |
| `lib.rs::rebuild_main_window()` | builder 加 `.background_color(Color(0, 0, 0, 0))`（深休眠唤醒重建窗口那条路，必须与配置等价） |

⚠️ 设这一步的时机安全：窗口 `visible: false`，等前端 `present_window` 才显示 ⇒ 看不到闪烁。

### 补第二刀：**壳层自己的底色也是白边来源**（用户复测"白边还在"暴露的）

改完壳侧我请用户复测 —— 他回"还在"，但"我中途开的那个窗口看着没有"（那是新编的调试壳，
A/B 正好成立）。等他给了一张 **1:1 全屏截图**（分辨率 1920×1080、窗口 1456×809），
才量到真相：

```
左上角（粉 #FFA2B4）: 桌面 → #AB9F86 → #F1B3BA → #FEB4C2 → 粉      ← 比粉**更亮**
左下角（rail #4B5A6F）: 桌面 → #676664 → #777F8B → #657183 → rail    ← 比 rail**更亮**
```

两处都能用"**区域色 78~80% 压在白底上**"精确复现，但这次的白不是 WebView 的 ——
**是 `.app-shell` 自己的 `background: var(--c-bg-page)` ＝ 近白 `#fffbfb`**：
子层被 `overflow: hidden` 按 4px 圆角裁切时，边缘像素 = 子层色 × 覆盖度 + **壳底色** × (1−覆盖度)。

**实验坐实**（vite 热更，10 秒）：把 `--c-bg-page` 临时改成 `#00ff00`，
角上像素立刻变绿调（`#707A5E` / `#347B4B`）⇒ 就是它 ✓

**修法（CSS）**：`.app-shell` 不再画底，页面底挪到 `.app-main`（顶栏 / rail / 侧栏本来就各有底色）。

**但立刻踩到第二个坑**：壳层一透明，**揭幕期间会闪桌面** ——
`rise-in` 让各区域带延迟渐显（最晚 0.45s 延迟 + 0.35s 时长），那段时间窗口是**透的**
（实测揭幕时窗口中心是暗的 `#0C0C0C`，而改之前那里是近白兜底）。
所以改成**两段式**：

```css
.app-shell { background: var(--c-bg-page); }               /* 启动期：兜底，观感与改前一致 */
html.shell-settled .app-shell { background: transparent; } /* 揭幕完成后：透明，四角不混白 */
```

`shell-settled` 由 `main.tsx` 在 `state === 'done'`（"揭幕完成（应用壳可见）"）时挂到 `<html>` ——
**现成的信号**，不用新造时机；此时幕已收完、壳已完全画出，撤兜底不会闪。

**验证**（都是像素/计算样式，不靠肉眼）：
- 修后四角：粉角 `#69575F → #C58594 → #F89EB1 → 粉`、rail 角 `#25282C → #3C4656 → #49586C → rail`
  —— **全部比底色暗**（往桌面混），再没有"比底色亮"的像素 ✓
- 内部接缝扫过（rail↔侧栏、顶栏↔内容）：无桌面透出 ✓
- 启动取样（150ms 一帧、连采 9 秒）：`粉 → #FFFBFB（兜底）→ 粉/白` ，**没有暗帧** ✓
- 新护栏：默认探针（三档宽度）断言 `html.shell-settled` 存在 + `.app-shell` 的 computed
  `background-color` 是透明；**反向验证过**（临时停用那条规则 ⇒ 报 `rgb(255, 251, 251)` 判红）✓

## 二、左栏跟随档案设置：两处各写了一遍取值链

**根因**：卡片（`HeroCardsView`）用的是 `resolveSign`（`sign_override` → 来源账号 → 主账号）
与 `vtubers.avatar`；而左栏 `VtuberSidebar` 自己拼了一条
`bili.avatar_path ?? bili.avatar_url` + `bili.sign`（**只看平台字段**）——
于是"档案设置里换过头像/签名"在卡片上生效、在左栏上看着像没生效。

**修法**：把头像口径抽成 `utils/avatarSource.ts::resolveAvatar`（与 `signSource.ts` 对称），
**卡片与左栏都调它**；左栏签名改用 `resolveSign(...).text`。口径链：

```
vtubers.avatar（档案设置点选的那枚头像 URL，原样用）
  → B 站账号的本地缓存 avatar_path → 任一账号的本地缓存
  → B 站账号的 avatar_url → 任一账号的 avatar_url → undefined（回落到名字首字）
```

## 三、这条 UI 行为怎么"看见"（新探针 `--profile-sync`）

我看不到界面（DSH 自己的窗口压在上面），所以**只有探针能当眼睛**。
新增第 16 个探针模式：CLI 先往**副本 DB** 种 `sign_override` + `avatar`（绝不碰开发库），
再断言**左栏那一行实际渲染出来的**签名文本与头像 URL，并与卡片交叉对账；
另设一个**没有 override 的 V 当对照组**（防"永远渲染自定义值"的假绿）。

**踩到两个"尺子"的坑（都留档）**：

1. **虚拟时间下图片永远加载不完** ⇒ Radix `AvatarImage` 只在加载成功后挂 `<img>` ⇒
   左栏头像量成 `None`，看着像"没接线"其实是尺子量不到。
   修法：给左栏 `Avatar` 与 hero 容器各挂一个 **`data-src` / `data-avatar-src`**
   （**为可测性存在**，同 `.stat-sets[data-hover]` 的先例，别当冗余删掉）。
2. **种的头像必须有区分度**：第一版种的是 B 站账号自己的缓存，
   而**旧代码恰好也取那一枚** ⇒ 头像半条断言永远绿（假绿）。改成种**非 B 站账号**的头像。

**反向验证**（把左栏改回旧写法再跑）：

```
- 左栏签名是 '虚拟艺人团体VirtuaReal成员 七海Nana7mi…'，不是种下的自定义签名（左栏没跟随）
- 左栏头像是 '…/static/avatars/434334701.jpg'，不是自定义头像 '…/weibo_7198559139.jpg'
[exit 1]
```
改回修复版 ⇒ `[ok] 左栏与卡片同源：自定义签名/头像都到位，对照组未被污染`（exit 0）。

## 四、门禁

pytest · vitest **277 → 283**（+6 `avatarSource.test.ts`）· cargo 31 · tsc 0 · eslint 0 ·
语法扫描 107 · `doc_check` 0 FAIL · 探针 `--profile-sync` 通过（且**反向验证会红**）·
默认探针三档的"壳层透明"断言通过（同样反向验证会红）。

## 五、这一批的教训（写给下次的自己）

1. **"我改了" ≠ "你看到的就是我改的"**：用户复测时跑的是 v1.0.2 产物，而壳侧修复要先重新构建 ——
   A/B 现象（新的干净、旧的还有）本身就是证据。以后凡是**动壳/动产物**的修复，
   交付时要说清"这条要看哪个构建"，并主动提供可看的那个（这次就是重开调试壳）。
2. **"量到的白"要问清是哪一层的白**：同一处白边有**两层**来源（WebView 背景 + 壳层底色），
   修掉一层另一层还在。判据是**混色数学**：把候选底色代进"区域色 × α + 底色 × (1−α)"，
   哪个能对上就是它（这次两层都是这么定下来的）。
3. **透明是一把双刃剑**：撤掉兜底底色能修白边，但会把"渐显期间的空窗"暴露成**闪桌面** ——
   所以透明必须**卡在某个状态之后**，而那个状态最好是**已经存在的**（这里是 `state === 'done'`）。
